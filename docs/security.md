# Sicurezza

Riferimento completo: [`SPECIFICATION.md`](./SPECIFICATION.md) §52-58. Vedi anche [`reuse-vs-new.md`](./reuse-vs-new.md) per cosa è concettualmente ispirato a BitChat e cosa va progettato da zero.

## Stato attuale (Milestone 1-13, 15, 16, 20)

Implementato:

- **Identità crittografica per nodo** (§14, §53): coppia di chiavi Ed25519 generata con `node:crypto`. Il node id esposto (`node/src/identity.ts`) è l'hex della chiave pubblica; la chiave privata non lascia mai il processo e non viene mai serializzata nei pacchetti.
- **Firma del publisher sui contenuti** (§55): ogni contenuto pubblicato tramite `NomadNode.publishContent()` viene firmato (`node/src/content.ts`, `contentSigningPayload`/`verifyContentSignature`). Il payload firmato copre `contentId, name, mimeType, size, publisherId` — non solo l'hash del contenuto — proprio per impedire che un relay possa "rietichettare" un contenuto genuino (stessi byte, nome/tipo diversi) mantenendo la firma valida.
- **Verifica applicata coerentemente su tutti i percorsi che accettano contenuto dalla rete**: sia il completamento di un trasferimento richiesto da questo nodo, sia la cache passiva di un relay che inoltra chunk altrui, sia una voce di catalogo ricevuta via sync — tutti passano per `ContentStore.putVerified()` / `verifyContentSignature()` prima di fidarsi di qualunque cosa dichiarata da un peer.
- **Content id immutabile per costruzione** (§24, §33): poiché `content_id = sha256(bytes)`, una volta accettata una voce di catalogo valida per un dato id non c'è nulla da "aggiornare" — `RemoteCatalog.record()` tiene la prima voce valida ricevuta.
- **Trust levels** (§54): `UNKNOWN/SEEN/VERIFIED/TRUSTED/ADMIN` per node id (`node/src/trust.ts`). `SEEN` alla connessione diretta; `VERIFIED` quando una firma di quel node id verifica correttamente (evidenza crittografica reale che quel nodo controlla la chiave privata corrispondente — non un giudizio su quanto sia affidabile *il contenuto* firmato). `TRUSTED`/`ADMIN` sono assegnazioni manuali dell'operatore (`trust.set(nodeId, level)`), pensate per reti controllate (RIFUGIO-NET, SOCCORSO-NET, §54). Un nodo può opzionalmente rifiutarsi di fare da relay per un peer sotto una soglia (`minTrustToRelay`), senza che questo influisca sulle richieste dirette di quel peer verso di lui.
- **Rate limiting / admission control** (§57): budget di pacchetti per finestra temporale, per singolo peer (`node/src/rate-limit.ts`) — un peer che genera più traffico del budget vede i pacchetti in eccesso scartati, senza impattare gli altri peer.
- **Relay policy legata a batteria/carica** (§51, §58): un nodo può configurare quando è disposto a fare da relay (`off`/`always`/`when-charging`/`battery-above`, `node/src/relay-policy.ts`) in base al proprio stato di risorse auto-dichiarato — non hardware reale in questo prototipo, ma il meccanismo è pronto per riceverlo.
- **Cifratura end-to-end dei messaggi privati** (§52, §56): `node/src/encryption.ts` — chiavi X25519 dedicate (`EncryptionIdentity`), separate deliberatamente dalle chiavi Ed25519 di firma/identità (best practice standard: compromettere una non compromette l'altra). Lo scambio di chiave usa ECDH (`crypto.diffieHellman`), il segreto condiviso viene ridotto con SHA-256 a 32 byte e usato come chiave AES-256-GCM (cifratura autenticata, nonce casuale a 12 byte per messaggio). Ogni nodo pubblica un `IdentityAnnouncement` — il proprio node id Ed25519 legato alla propria chiave X25519, autofirmato con la chiave Ed25519 (stesso pattern di `contentSigningPayload`/`verifyContentSignature`) — propagato con un pattern di sync bilaterale peer-to-peer (`node/src/peer-directory.ts`, pacchetti `IDENTITY_REQUEST`/`IDENTITY_RESPONSE`, mai flood-ati) analogo al catalog sync. `sendPrivateMessage()`/`PRIVATE_MESSAGE` sono additivi rispetto a `sendData()`/`DATA` — quest'ultimo resta deliberatamente in chiaro, usato dal resto della test suite per la meccanica di routing/relay, non per la privacy — così zero rischio di regressione sui test esistenti. Ogni `PRIVATE_MESSAGE` porta con sé l'`IdentityAnnouncement` autofirmato del mittente: il destinatario può quindi verificare e imparare la chiave del mittente anche se non l'ha mai sincronizzata prima (l'identity sync, come il catalog sync, è punto-a-punto al momento della connessione, non riannunciato retroattivamente ai peer già connessi — vedi bug #4 sotto), senza dover dipendere dall'ordine con cui la mesh si è formata.
- **Routing a costo (distance-vector)** (§22): `node/src/routing-table.ts`, propagato tramite `ROUTE_ANNOUNCE` scambiati punto-a-punto tra vicini con triggered update (mai flood-ati). Non è un meccanismo di sicurezza in sé — vedi la limitazione nota sotto.

Testato esplicitamente contro tentativi di manomissione (non solo contro errori accidentali): impersonificazione di un altro publisher, contenuto rietichettato dopo la firma, voci di catalogo forgiate iniettate via socket grezzo, contenuto con hash corretto ma firma falsa sul percorso di recupero principale, gate di fiducia/relay-policy aggirati tramite la coda di store-and-forward, claim di identità forgiate iniettate via `IDENTITY_RESPONSE` grezzo, e — per la cifratura E2E — un `senderAnnouncement` piggybacked su `PRIVATE_MESSAGE` con `nodeId` non corrispondente al `source` dichiarato del pacchetto o con firma non valida. Vedi `tests/unit/content.test.ts`, `tests/integration/content-integrity-security.test.ts`, `tests/integration/catalog-sync-security.test.ts`, `tests/integration/store-and-forward-relay-gate.test.ts`, `tests/integration/identity-sync-security.test.ts`, `tests/integration/private-message-security.test.ts`, `tests/integration/malformed-packet-robustness.test.ts`.

**Non implementato**:

- Privacy/minimizzazione degli identificatori (§56) — il node id resta visibile in chiaro in ogni pacchetto, cifrato o meno.
- **Autenticazione delle rotte distance-vector** (§22): un vicino connesso può annunciare un `ROUTE_ANNOUNCE` con qualunque `{destination, cost}` e viene creduto sulla parola — non c'è modo di provare crittograficamente che quel vicino possa davvero raggiungere quella destinazione a quel costo. Questa è la stessa fiducia implicita già insita nel controlled flooding (che inoltra a chiunque sia connesso, senza validare l'affidabilità del percorso) — ma con una differenza pratica rilevante: instradare significa inviare a un solo next hop invece che a tutti i vicini, quindi un vicino malevolo che si dichiara falsamente il percorso più economico verso una destinazione può fare da blackhole silenzioso per quel traffico, senza la ridondanza che il flooding offrirebbe come rete di sicurezza. Non risolto in questa milestone (richiederebbe attestazioni di percorso crittografiche e/o instradamento multi-path con conferma end-to-end, entrambi fuori scope per un routing "sperimentale" come lo definisce la stessa specifica al §22).
- **Binding crittografico tra identità e connessione di trasporto**: il campo `source` di un pacchetto (incluso l'`HELLO` iniziale) è dichiarato dal peer, non provato crittograficamente in fase di handshake. Un nodo può dichiarare qualunque node id come mittente — ciò che *non* può fare è produrre una firma valida per contenuto, voci di catalogo o identity announcement che non ha realmente controllato, perché la firma è verificata contro la chiave pubblica corrispondente al node id dichiarato. Istanza concreta di questo limite: `CONTENT_NOT_FOUND` (introdotto nel seguito dell'audit, vedi bug #15 sotto) non porta alcuna firma — un nodo che riesce a far arrivare un pacchetto con `source` uguale al fornitore attivo di una richiesta in corso può forzarne il fallimento immediato sul candidato successivo. Non è una capacità nuova rispetto a quanto già possibile spoofando `CONTENT_CHUNK`/`CONTENT_COMPLETE` dallo stesso `source` (che può già corrompere un trasferimento in corso) — anzi `handleContentNotFound` degrada in modo più controllato: passa al prossimo candidato noto invece di far fallire l'intera richiesta come farebbe un hash finale non valido dopo dati corrotti. Risolvere richiederebbe la stessa autenticazione crittografica del trasporto che manca a tutto il resto del protocollo — fuori scope per questo prototipo, come già annotato sopra.

## Bug di sicurezza reali trovati e corretti durante lo sviluppo (Milestone 13/15, prima passata)

Utile registrarli qui perché illustrano bene la differenza tra "il codice compila e i test passano" e "il codice fa davvero quello che promette" — nessuno era visibile finché non è stato specificamente cercato con un review mirato:

1. **Catalogo remoto senza verifica**: la prima versione del catalog sync (Milestone 13) registrava qualunque voce dichiarata da un peer in una `SYNC_RESPONSE`, senza controllare la firma — un solo nodo malevolo poteva iniettare "questo contenuto esiste, firmato da `<vittima>`" e vederlo propagarsi transitivamente ad ogni sync successivo. Corretto verificando la firma prima di accettare qualunque voce, esattamente come già avveniva per i byte reali.
2. **Firma che copriva solo l'hash**: la prima versione della firma (Milestone 15) firmava solo `content_id`, non nome/tipo/dimensione — un relay poteva rietichettare un contenuto genuinamente firmato (es. da "note.txt" a "aggiornamento-sicurezza.exe") mantenendo la firma valida. Corretto includendo l'intero set di metadata rilevante nel payload firmato.
3. **Il più grave: verifica della firma ignorata sul percorso principale**: in `handleContentComplete` (il codice che gestisce la fine di un trasferimento richiesto da questo nodo), l'esito di `ContentStore.putVerified()` — che include il controllo della firma — veniva calcolato ma **mai controllato**: la richiesta di contenuto (`getContent()`) veniva comunque risolta con i byte ricevuti, indipendentemente dal fatto che la firma fosse valida o meno. Un nodo con hash corretto ma firma falsa/assente avrebbe comunque visto il proprio contenuto accettato come fidato. Corretto controllando esplicitamente il risultato prima di risolvere la richiesta del chiamante.

Un quarto problema, meno grave ma dello stesso spirito, è stato corretto insieme ai livelli di fiducia: i pacchetti rimessi in coda per store-and-forward (Milestone 12) venivano ritentati **senza ripassare** dai controlli di fiducia/relay-policy — un modo silenzioso per aggirare una revoca di fiducia o una policy "non fare da relay" per qualunque cosa fosse già in coda al momento del cambiamento.

## Bug trovati e corretti durante cifratura E2E e routing (Milestone 15/16, seconda passata)

5. **Denial-of-service a un pacchetto**: `decodePacket()` valida solo l'involucro del pacchetto (id/type/source/ttl) — `payload` è JSON arbitrario, non tipizzato. Gli handler di `PRIVATE_MESSAGE`, `ROUTE_ANNOUNCE`, `IDENTITY_REQUEST`/`RESPONSE` (e i preesistenti `SYNC_REQUEST`/`RESPONSE`) destrutturavano/iteravano campi del payload assumendone la forma, senza controllare che esistessero — un singolo pacchetto malformato da un peer connesso (es. `PRIVATE_MESSAGE` senza `senderAnnouncement`, o `ROUTE_ANNOUNCE` senza `routes`) lanciava un'eccezione non gestita che **terminava l'intero processo del nodo**. Corretto validando la forma di ogni campo del payload prima di usarlo, in tutti e sei gli handler citati — vedi `tests/integration/malformed-packet-robustness.test.ts` per la prova diretta che il nodo sopravvive e resta funzionante dopo ciascun tipo di pacchetto malformato.
6. **Auto-eviction dell'identità del nodo**: la prima versione registrava l'`IdentityAnnouncement` del nodo stesso come prima voce nella struttura FIFO limitata `PeerDirectory` — essendo la prima voce inserita, era anche la prima a essere evitta una volta raggiunto il limite (`maxPeerDirectoryEntries`), rendendo silenziosamente il nodo non più scopribile via identity sync (e rompendo `sendPrivateMessage()`, che dipende dalla propria voce per il piggyback). Corretto tenendo l'annuncio del proprio nodo in un campo dedicato (`ownAnnouncement`), mai nella struttura evittabile — stesso pattern già usato da `routingTable`, che non memorizza mai una "rotta verso se stessi".
7. **Race condition di scrittura TCP non gestita**: un peer che si disconnette mentre un altro nodo gli sta scrivendo (scenario ordinario in una rete inaffidabile, proprio quello per cui questo software esiste) poteva generare un `EPIPE`/`ECONNRESET` che bypassava sia la callback di scrittura sia il listener `'error'` del socket, terminando l'intero processo worker — non solo la connessione. Il caso più grave: l'oggetto `readline.Interface` usato per leggere i pacchetti in arrivo emette un proprio evento `'error'` separato da quello del socket sottostante, e non aveva **nessun listener** — un `ECONNRESET` in lettura durante la chiusura di un peer bastava a far crashare l'intero processo di test. Corretto con gestione ridondante della scrittura (callback + listener `'error'` temporaneo + try/catch), una guardia di processo mirata per i soli codici di errore di rete transitori e ben noti (mai per errori generici), e un listener `'error'` no-op sull'interfaccia `readline` — coerente con la filosofia dell'intero progetto (spec §2, §30-31): un link inaffidabile è un evento atteso, non un'eccezione.

## Bug trovati e corretti durante l'audit completo del codice (14 agosto 2026)

Un audit mirato dell'intero `node/src` (non solo del codice nuovo) — vedi anche la sezione "Problemi noti, non ancora risolti" sotto per ciò che è stato trovato ma lasciato aperto:

8. **Riassemblaggio chunk senza limite di memoria**: `ChunkAssembler` (usato per ricostruire un contenuto da `CONTENT_CHUNK` in arrivo) era l'unica struttura alimentata da rete in tutto il codice a non avere un tetto dimensionale (a differenza di `SeenCache`, `RemoteCatalog`, `PeerDirectory`, `TrustManager`, `RoutingTable`, `PendingDeliveryQueue` — tutte limitate per spec §57). `handleContentChunk` alimenta questa struttura per **qualunque** `contentId` dichiarato da un peer connesso, anche per contenuti mai richiesti da questo nodo (necessario per il caching opportunistico dei relay, §27) — un peer poteva quindi inviare un flusso di chunk fabbricati per id sempre diversi, senza mai completare il trasferimento, facendo crescere la memoria del nodo senza limite (denial-of-service da esaurimento memoria, raggiungibile da un singolo peer connesso). Corretto con un doppio limite: numero massimo di trasferimenti concorrenti tracciati (eviction FIFO, come le altre strutture) e un tetto sul numero di chunk dichiarabili per singolo contenuto (un `chunkIndex`/`totalChunks` fuori limite viene scartato subito, prima di allocare qualunque memoria).
9. **Buffer di lettura TCP senza limite**: l'interfaccia `readline` usata per leggere i pacchetti (newline-delimited) non ha un limite di lunghezza riga integrato — un peer che scrive dati senza mai inviare un `\n` fa accumulare byte indefinitamente in memoria, un problema indipendente da (e precedente a) qualunque controllo sul pacchetto decodificato o sul rate limiter, che vedono solo pacchetti già interi. Corretto con un controllo parallelo sui byte ricevuti dall'ultimo a-capo: oltre una soglia generosa (2 MB, ben oltre qualunque pacchetto legittimo di questo protocollo) la connessione viene chiusa.
10. **Descrizione fuorviante nel comportamento di `PeerDirectory`**: il commento del codice dichiarava che, a directory piena, un nuovo annuncio di identità venisse scartato — il comportamento reale (corretto, coerente con `RemoteCatalog`/`RoutingTable`) è invece l'eviction della voce più vecchia. Non era un bug di comportamento, ma un commento sbagliato che avrebbe fuorviato chiunque avesse dovuto ragionarci sopra in futuro — corretto allineando la descrizione al codice reale, e rendendo esplicito il limite noto che ne consegue (vedi sotto).

## Bug corretti nel seguito dell'audit (implementazione dei "prossimi passi in puro software")

11. **Un fornitore di contenuto lento bloccava gli altri** *(risolto)*: quando due nodi diversi rispondevano entrambi `CONTENT_FOUND` per lo stesso contenuto, il richiedente si agganciava alla **prima** risposta arrivata e ignorava ogni risposta successiva — anche se quel primo fornitore risultava poi irraggiungibile (es. una rotta appena diventata invalida) e il secondo sarebbe stato perfettamente raggiungibile; la richiesta scadeva semplicemente per timeout invece di tentare il secondo fornitore. Corretto in `node/src/node.ts` (`requestFromProvider`/`retryNextProvider`): il richiedente ora ricorda ogni fornitore che ha risposto e passa automaticamente al successivo se quello attivo non fa progressi entro `contentProviderTimeoutMs` — un timer di "attività", non una scadenza assoluta sul trasferimento intero, così un fornitore lento-ma-vivo non viene abbandonato a metà. Una risposta tardiva di un fornitore già abbandonato viene scartata esplicitamente (mai può interrompere un tentativo in corso con un altro), e i dati parziali del fornitore abbandonato vengono scartati prima di riprovare, per non mescolare byte di provenienza diversa. Vedi `tests/integration/content-provider-retry.test.ts` (5 test, inclusi due scenari di correttezza scoperti dalla revisione: un fornitore lento-ma-vivo non deve essere abbandonato, e un candidato arrivato in ritardo dopo l'esaurimento di tutti gli altri deve essere tentato subito, non solo messo in coda).
12. **Pattern di struttura dati limitata duplicato cinque volte** *(risolto)*: la stessa logica ("se pieno, evita la voce più vecchia") era reimplementata quasi identica in cinque file diversi. Unificata in `node/src/bounded-map.ts` (classe `BoundedFifoMap`), usata ora da `SeenCache`, `RemoteCatalog`, `PeerDirectory`, `PendingDeliveryQueue` e `RoutingTable` — quest'ultima è il caso più delicato perché la sua logica di aggiornamento rotte (`offer()`) è più complessa di un semplice inserimento, ma la revisione ha confermato che il refactor preserva esattamente il comportamento originale in ogni caso. La stessa revisione ha anche trovato un caso limite genuino introdotto dal nuovo helper (una capacità impostata a zero non si comportava più come "non conservare nulla"), corretto e coperto da un test dedicato prima di considerare la modifica conclusa.
13. **Un peer poteva "sfrattare" identità e contenuti legittimi con identità usa-e-getta** *(risolto)*: `PeerDirectory` e `RemoteCatalog` evitavano sempre la voce più vecchia quando erano pieni, senza considerare quanto fosse "a buon mercato" fabbricare la voce che stava entrando — a differenza di `TrustManager`, che evita già prima le voci a fiducia più bassa proprio per resistere a questo scenario. Corretto estendendo lo stesso meccanismo alle altre due strutture, reso possibile dall'unificazione del punto precedente: `BoundedFifoMap` ha guadagnato uno "score di eviction" opzionale (rivalutato dal vivo a ogni eviction, mai calcolato una sola volta all'inserimento, perché la fiducia di un nodo può cambiare dopo che la sua voce è già stata registrata), e `node.ts` collega `PeerDirectory`/`RemoteCatalog` alla fiducia reale tramite una nuova funzione `trustRank()` esportata da `trust.ts`. Punto importante confermato dai test: una firma valida da sola non basta a proteggere una voce, perché chiunque può generare una chiave usa-e-getta e firmare validamente con quella (guadagnando comunque `VERIFIED` in automatico) — la vera protezione arriva solo da un livello di fiducia assegnato esplicitamente da un operatore (`TRUSTED`/`ADMIN`, spec §54), che un'identità usa-e-getta non può mai raggiungere da sola. Vedi `tests/integration/trust-aware-eviction.test.ts`.
14. **La priorità dei pacchetti era solo un campo dati, senza scheduling reale** *(risolto)*: un burst di traffico BULK verso lo stesso peer (es. un trasferimento a chunk in corso) poteva far attendere un pacchetto EMERGENCY dietro tutto il burst, solo perché quest'ultimo era stato accodato per primo — la priorità veniva letta ma non usata per decidere l'ordine di invio. Corretto in `node/src/transports/tcp.ts`: ogni socket per-peer ha ora una coda a priorità dedicata (`PriorityQueue`, `node/src/priority-queue.ts`, 6 bucket FIFO — uno per livello di `Priority`) e un loop di drain che serializza gli invii svuotando sempre il bucket non vuoto a priorità più alta prima di passare al successivo; `send()` risolve solo quando il proprio pacchetto è stato effettivamente scritto, anche se ha dovuto aspettare dietro ad altri con priorità più alta arrivati dopo. La coda per peer resta comunque limitata (`MAX_QUEUED_SENDS_PER_PEER = 500`, già presente prima di questo intervento) contro una crescita illimitata in memoria; la revisione ha verificato empiricamente sia questo limite (505 invii concorrenti → esattamente 4 rifiutati) sia che nessun invio resta bloccato indefinitamente se il socket muore mentre è ancora in coda (rifiutato esplicitamente, mai lasciato pending) — comportamento ora coperto anche da test di regressione dedicati, non solo verificato ad-hoc. Vedi `tests/unit/priority-queue.test.ts` e `tests/integration/tcp-priority-scheduling.test.ts`.
15. **Un fornitore che aveva perso il contenuto restava silenzioso invece di dirlo** *(risolto)*: un nodo che rispondeva `CONTENT_FOUND` e poi, prima del `CONTENT_REQUEST` effettivo, perdeva il contenuto (evitato da una cache limitata, o scaduto — vedi sotto) non aveva modo di segnalarlo — il richiedente doveva sempre aspettare l'intero `contentProviderTimeoutMs` prima di provare il candidato successivo, anche quando il fornitore avrebbe potuto rispondere "non ce l'ho più" nell'istante stesso in cui riceveva la richiesta. Aggiunto un nuovo tipo di pacchetto `CONTENT_NOT_FOUND` (`node/src/packet.ts`): `handleContentRequest` lo invia esplicitamente al posto di restare silenzioso, e il nuovo `handleContentNotFound` fa fallire immediatamente la richiesta pendente sul prossimo candidato — con lo stesso confine di fiducia già usato da `handleContentChunk`/`handleContentComplete` (onorata solo una risposta dal fornitore attualmente attivo, spec §90-92). Questo pacchetto non porta alcuna firma (non è una rivendicazione di contenuto, solo un segnale di instradamento) — vedi la nota sul binding crittografico sopra per l'implicazione pratica, considerata e non peggiorativa rispetto a quanto già possibile spoofando `CONTENT_CHUNK`/`CONTENT_COMPLETE`. Contestualmente implementata anche la **scadenza dei contenuti** (spec §24, campo `expiresAt` mai implementato prima d'ora): coperta dalla firma del publisher esattamente come gli altri campi (nessun relay può manometterla), applicata "pigramente" a ogni lettura in `ContentStore` e `RemoteCatalog` — vedi `docs/protocol.md` per i dettagli. Un caso limite trovato dalla revisione e corretto prima di considerare la modifica conclusa: `RemoteCatalog.record()` confrontava l'esistenza di una voce con la mappa grezza sottostante invece che con la propria logica di scadenza, per cui una voce scaduta ma non ancora "toccata" da una lettura bloccava per sempre una ri-annuncio valido dello stesso content id. Vedi `tests/integration/content-not-found.test.ts`, e i nuovi test di scadenza in `tests/unit/content.test.ts`/`tests/unit/catalog.test.ts`.
16. **Service discovery** (spec §35-37, mai implementata prima d'ora) *(bug trovati durante lo sviluppo, tutti risolti)*: nuovi pacchetti `SERVICE_ANNOUNCE`/`SERVICE_QUERY`/`SERVICE_REQUEST`/`SERVICE_RESPONSE`, `ServiceAnnouncement` firmata dal fornitore (`node/src/service.ts`, stesso schema di `ContentMetadata`) e `ServiceDirectory` (`node/src/service-directory.ts`, stesso schema di `RemoteCatalog` con eviction pesata sulla fiducia). La revisione ha trovato 4 problemi reali, tutti corretti prima di considerare la modifica conclusa:
    - **Crash del processo su `SERVICE_REQUEST` malformato** *(il più grave, corretto)*: `handleServiceRequest` accedeva a `packet.payload.serviceId` senza `?.` in un ramo raggiungibile con `packet.payload` interamente assente — `decodePacket()` valida solo l'involucro del pacchetto (`id`/`type`/`source`/`ttl`), mai la forma del payload, quindi un singolo pacchetto malformato da un qualunque peer connesso bastava a far crashare l'intero processo (DoS non autenticato). Corretto risolvendo `serviceId` in modo difensivo prima di ogni uso. Vedi i nuovi casi `SERVICE_*` in `tests/integration/malformed-packet-robustness.test.ts` (che include esplicitamente lo scenario che prima crashava).
    - **`discoverService()` ignorava la disponibilità della propria registrazione locale**: se un nodo aveva registrato localmente un servizio e poi lo aveva reso non disponibile (`setServiceAvailability(id, false)`), `discoverService()` restituiva comunque quell'annuncio locale invece di cercare un fornitore remoto realmente disponibile — una chiamata finiva per tentare d'invocare se stesso, fallire silenziosamente (il pacchetto verso se stesso non può mai essere consegnato) e scadere per timeout invece di trovare l'alternativa remota. Corretto filtrando anche il percorso locale per `availability`, come già faceva `providersFor()`.
    - **`ServiceDirectory.record()` scartava un annuncio più recente a parità di `createdAt`**: due annunci firmati a distanza di pochi millisecondi (es. `registerService()` seguito subito da `setServiceAvailability()`) possono avere lo stesso `Date.now()` — il confronto `>=` scartava sempre il secondo, lasciando la mesh convinta che un servizio appena reso non disponibile lo fosse ancora. Corretto: a parità di `createdAt` vince l'annuncio in arrivo, non quello già registrato (una vera replica byte-identica resta comunque innocua). Vedi il nuovo test dedicato in `tests/unit/service-directory.test.ts`.
    - **Chiamate in corso non venivano mai liberate da `stop()`**: una `callService()` già oltre la fase di discovery restava pendente (promise, closure e timer vivi) fino al proprio `timeoutMs`, anche se il nodo era già stato fermato e non avrebbe mai potuto ricevere una `SERVICE_RESPONSE`. Corretto: `stop()` ora rifiuta esplicitamente ogni chiamata ancora in `pendingServiceCalls`. Limite noto non risolto qui, della stessa forma di una lacuna preesistente già presente per `waitForPeerKey()`/`pendingContentRequests`: una chiamata ancora dentro `discoverService()` (non ancora registrata in `pendingServiceCalls`) non viene liberata da `stop()` e attende comunque il proprio timeout — accettato come fuori scope per questo intervento, non una regressione introdotta da esso. Vedi il nuovo test in `tests/integration/service-discovery.test.ts`.

    Punto di design intenzionale (non un bug): a differenza di `ContentMetadata`, `ServiceAnnouncement` include `createdAt` nel payload firmato — il contenuto è immutabile per costruzione (l'id è l'hash dei byte) quindi una data di creazione non serve mai a decidere quale versione tenere, ma la disponibilità di un servizio è per natura mutevole nel tempo, e firmare `createdAt` è ciò che permette a `ServiceDirectory` di respingere un annuncio più vecchio "rigiocato" invece di uno realmente più recente. `SERVICE_RESPONSE` non porta firma propria (è un segnale di instradamento/risposta, come `CONTENT_NOT_FOUND`) tranne quando incorpora una `ServiceAnnouncement`, che è firmata; il ramo che correla una risposta a una chiamata in corso (`requestId`) è comunque vincolato a `packet.source === pendingCall.providerId`, stesso confine già usato da `handleContentChunk`/`handleContentComplete`. Vedi `docs/protocol.md` per il flusso completo e `tests/unit/service.test.ts`, `tests/unit/service-directory.test.ts`, `tests/integration/service-discovery.test.ts`.
17. **Interfaccia web locale di stato/ricerca** (spec §59, mai implementata prima d'ora): `WebUiServer` (`node/src/web-ui.ts`), un server HTTP minimale (`node:http`, nessuna nuova dipendenza) che espone `GET /` (pagina HTML statica, nessuna interpolazione lato server — il rendering dei dati arriva via `fetch()` lato client e usa sempre `textContent`, mai `innerHTML`, proprio per non introdurre XSS attraverso nomi di contenuto o altri dati appresi dalla rete), `GET /api/status` e `GET /api/search`. Spenta di default nel CLI (`--web-port`, `node/src/cli.ts`), e quando attiva resta bound su `127.0.0.1` (nessuna autenticazione su questi endpoint — un'esposizione più ampia richiederebbe prima di aggiungerne una). Il campo "Internet" è auto-dichiarato (come lo stato batteria di `RelayPolicy`) — questo prototipo non ha una sonda di connettività reale, e il punto della spec §60 è proprio dimostrare "Internet: OFFLINE / Nomad-Net: ONLINE", non rilevarlo. La ricerca non mostra mai un percorso multi-hop completo come nel mockup della spec (`NODE-B -> NODE-C -> NOMAD`): il routing distance-vector di questo prototipo (spec §22) conosce solo il prossimo salto migliore verso una destinazione, mai il percorso intero, quindi mostrarne uno sarebbe inventato — riporta invece "next hop: NODE-xxxxxxxx" quando una rotta è nota, nient'altro quando non lo è. La revisione ha trovato 2 problemi reali nella logica di conteggio, corretti prima di considerare la modifica conclusa: `cachedContentPercent` sommava `contentStore.size + remoteCatalog.size` invece di deduplicare per content id come fa già `listKnownContent()`, sottostimando la percentuale ogni volta che un contenuto imparato via sync viene poi effettivamente recuperato (la voce di catalogo corrispondente non viene mai rimossa); e il conteggio "Services" includeva servizi esplicitamente resi non disponibili (`setServiceAvailability(id, false)`), mentre il resto della codebase (`providersFor()`/`discoverService()`) li esclude già — corretto filtrando per `availability` solo nella pagina di stato, senza restringere il contratto generale di `listKnownServices()` (che resta volutamente "tutto ciò che si conosce", come `listKnownContent()`). Vedi `tests/integration/web-ui.test.ts`.
18. **Transport BLE simulato** (roadmap Milestone 8, seguito audit): `BleSimulatedTransport` (`node/src/transports/ble.ts`) implementa l'interfaccia `Transport` esistente senza alcun radio reale — solo oggetti JS e `setTimeout` — ma con le proprietà di BLE che contano davvero: MTU piccola (20 byte di default, il caso peggiore reale) che forza una frammentazione a livello di transport genuinamente esercitata (non solo dichiarata), un limite di connessioni simultanee, e latenza per-frammento simulata. La revisione ha trovato 3 problemi reali, tutti corretti prima di considerare la modifica conclusa:
    - **`connect()` poteva restare pendente per sempre** *(il più grave, corretto)*: se una connessione veniva chiusa (es. `stop()` del peer remoto) prima che il proprio handshake identificasse l'altro lato, nessun codice rifiutava mai la promise di `connect()` in corso — restava pendente indefinitamente, a differenza di `TcpTransport`, che ha un `CONNECT_TIMEOUT_MS` esplicito come rete di sicurezza. Corretto tracciando esplicitamente la callback di risoluzione/rifiuto per ogni connessione ancora in attesa di identificazione (`pendingConnect`), rifiutata sia da una chiusura esplicita sia da un nuovo timeout di connessione dedicato (mirror di quello TCP).
    - **Il controllo del limite di connessioni contava anche gli handshake ancora in corso, bloccando un reconnect legittimo**: un peer già connesso che si riconnetteva (stessa logica di "courier" già gestita da `TcpTransport`) prima che la propria connessione precedente fosse esplicitamente chiusa veniva rifiutato per limite raggiunto, anche se il reconnect sarebbe dovuto risultare a costo netto zero (rimpiazza la connessione precedente, non la somma). Corretto separando due contatori: un tetto generoso e indipendente sulle connessioni *pendenti* (handshake in corso, anti-DoS puro — spec §57), e il vero `maxConnections` applicato solo al numero di peer *identificati*, verificato in `handleIdentified()` una volta noto se la connessione in arrivo è un peer nuovo o un reconnect che rimpiazzerà la propria voce obsoleta.
    - **Correggere il punto precedente ha introdotto una race scoperta e chiusa nella stessa passata**: poiché il lato che accetta una connessione inviava il proprio `HELLO` immediatamente, prima ancora di decidere se avrebbe poi rifiutato la connessione per limite raggiunto, chi si connetteva poteva identificare l'altro lato e risolvere `connect()` con successo anche in un run in cui l'altro lato stava per rifiutare — lasciando i due lati in disaccordo su se la connessione fosse davvero attiva. Corretto ritardando l'invio del proprio `HELLO` fino a quando quel lato non ha effettivamente deciso di accettare la connessione (`handleIdentified`), invece di inviarlo eagerly come fa invece il lato che inizia la connessione.

    Inoltre, `FragmentReassembler` (il riassemblatore di frammenti per connessione) riusa `BoundedFifoMap` invece di reimplementare da zero la stessa logica "se pieno, evita la voce più vecchia" già centralizzata dal punto #12 sopra — evitando una sesta reimplementazione quasi identica. Vedi `tests/unit/ble-transport.test.ts` (15 test, inclusi i due scenari di regressione scoperti dalla revisione) e `tests/integration/ble-relay.test.ts` (5 test, che dimostrano lo stesso comportamento di `NomadNode` — relay multi-hop, content discovery/cache, chunking — completamente invariato passando da TCP a questo transport). Vedi anche `docs/transport.md` per il dettaglio del design.
19. **Gateway NOMAD mockato** (roadmap Milestone 10-11, `docs/next-steps.md` Opzione B, seguito audit — ultima delle 9 slice originariamente pianificate): `gateway/nomad/` è una directory sorella di `node/` (non parte del workspace npm, stesso precedente di `tools/simulator/`), che importa `node/src/*` con percorsi relativi. `KiwixGateway` (`gateway/nomad/kiwix-gateway.ts`) avvolge un `NomadNode` e traduce le due API astratte della spec §37 verso l'API HTTP di (un finto) Project NOMAD: `syncCatalog()` (`content://...`) recupera il catalogo articoli e pubblica ognuno via `publishContent()` — reso scopribile/recuperabile attraverso il ciclo `CONTENT_QUERY`/`FOUND`/`REQUEST`/`CHUNK`/`COMPLETE` già esistente, senza toccare `node.ts` — mentre `registerSearchService()` (`CALL service://...`) registra `service://kiwix-search`, il cui handler proxya dal vivo una chiamata HTTP a NOMAD a ogni singola invocazione, senza mai cache. `FakeNomadServer` (`gateway/nomad/fake-nomad-server.ts`) sostituisce un'istanza Docker/Project NOMAD reale (bloccata, prerequisito ambientale non disponibile in questa sessione) con un server HTTP minimale avviato nello stesso processo di test/demo, che imita la sola API Kiwix di sola lettura effettivamente usata (`GET /api/articles`, `GET /api/articles/:path`, `GET /api/search`), incluso un meccanismo (`addBrokenListing()`) per simulare l'instabilità che la spec §4 attribuisce esplicitamente a NOMAD ("non è dichiarato stabile": un articolo elencato nel catalogo che poi risulta 404 alla richiesta effettiva). La revisione ha trovato 3 problemi reali, tutti corretti prima di considerare la slice conclusa:
    - **Crash del processo su un percorso articolo malformato** *(il più grave, corretto)*: `decodeURIComponent()` sul segmento di path richiesto non era protetto — una sequenza di percent-encoding malformata (es. un singolo `%` non seguito da due cifre esadecimali) fa lanciare `URIError` in modo sincrono dentro il listener della richiesta HTTP, e un throw non catturato lì abbatte l'intero processo Node, non solo quella richiesta. Corretto avvolgendo la chiamata in un `try`/`catch` che risponde `400` invece di propagare l'eccezione — stessa disciplina difensiva richiesta altrove nel codebase per qualunque input di rete, anche se questo server esiste solo dentro un processo di test/demo.
    - **`syncCatalog()` non riportava mai correttamente cosa fosse effettivamente cambiato tra due sincronizzazioni**: una riesecuzione contro un catalogo invariato restituiva di nuovo tutte le voci come se fossero nuove, e il commento di design affermava (erroneamente) un comportamento di delta che il codice non implementava affatto. Corretto aggiungendo `publishedByPath` (una mappa path → ultimo content id pubblicato da *questa* istanza di `KiwixGateway`): il valore restituito da `syncCatalog()` ora contiene solo le voci nuove o il cui contenuto è realmente cambiato dall'ultima sincronizzazione di questa stessa istanza — non un delta "gratuito" fornito da NOMAD, perché il content-addressing (spec §24: l'id è l'hash dei byte) impone comunque di recuperare il corpo intero di ogni articolo per sapere se è cambiato, non essendoci nulla di equivalente a un ETag in questa API. La docstring è stata riscritta per non affermare più nulla che il codice non faccia davvero. Vedi il nuovo test `syncCatalog() only reports new or changed entries on a second call, not everything again` in `tests/integration/nomad-gateway.test.ts`, che la revisione aveva esplicitamente notato mancare ("nessun test esercita una seconda chiamata a `syncCatalog()`").
    - **Recupero articoli seriale invece che concorrente**: `syncCatalog()` recuperava un articolo alla volta con un `for` sequenziale, senza motivo — nessuna dipendenza tra le richieste. Corretto con un nuovo helper `mapWithConcurrency()` (worker pool con un tetto di `DEFAULT_SYNC_CONCURRENCY = 8` richieste in volo contemporaneamente verso NOMAD, non tutte insieme).

    Limite noto, non un bug, lasciato esplicitamente documentato nel codice: un articolo che cambia allo stesso path NOMAD viene pubblicato con un content id nuovo (il contenuto è immutabile per costruzione, l'id è l'hash dei byte), ma i byte della versione **precedente** non vengono mai rimossi da `node.contentStore` — questo prototipo non ha, in nessun punto del codebase, un meccanismo di eliminazione di contenuto già pubblicato. Un gateway che risincronizza periodicamente contro un catalogo i cui articoli vengono modificati nel tempo accumula quindi ogni versione storica di ogni articolo modificato, senza limite — un deployment reale avrebbe bisogno di un `ttlMs` su `publishContent()` per il contenuto proveniente dal gateway, o di un meccanismo di eviction esplicito che questo prototipo non ha ancora.

    Riscontro di refactoring non legato alla sicurezza ma emerso dalla stessa revisione: `WebUiServer` (punto #17) e `FakeNomadServer` duplicavano lo stesso bootstrap HTTP (`createServer`/`listen`/`close`-come-promise, risoluzione della porta effettivamente assegnata) quasi identico. Estratto in `node/src/loopback-http-server.ts` (`LoopbackHttpServer`), usato ora da entrambi — verificato non aver alterato il comportamento di `WebUiServer` ri-eseguendo `tests/integration/web-ui.test.ts` (11/11) dopo il refactor. Verificato anche (nessuna modifica necessaria) che il percorso di errore di `SERVICE_RESPONSE` quando NOMAD è irraggiungibile non fa trapelare informazioni sensibili: il catch di `handleServiceRequest` (introdotto nella Slice 6) inoltra sempre e solo `err.message`, mai uno stack trace, e in questo contesto mockato gli URL coinvolti sono comunque sempre loopback locali. Vedi `tests/integration/nomad-gateway.test.ts` (5 test).
20. **Gateway AI mockato, `service://ai`** (spec §37, esempio esplicito della specifica: un dispositivo poco potente chiede alla rete una risposta generata da un'IA locale, la rete trova un nodo più capace in grado di fornirla — seguito audit, su richiesta esplicita dell'utente dopo la chiusura delle 9 slice originarie): estende il pattern del gateway NOMAD (punto #19) a un secondo sotto-servizio NOMAD, Ollama, che `docs/reuse-vs-new.md` elenca esplicitamente come distinto da Kiwix ("esiste, containerizzato — consumato tramite gateway"). `AiGateway` (`gateway/nomad/ai-gateway.ts`) è una classe a sé, non un metodo aggiunto a `KiwixGateway`: stessa forma (`node` + `baseUrl`, un solo metodo `registerAiService()` che chiama `node.registerService()`), ma un `baseUrl` indipendente e un fallimento indipendente, perché un vero Project NOMAD può servire Kiwix e Ollama su porte/host diversi pur essendo "la stessa istanza NOMAD" concettualmente. Il punto centrale della scoperta di `service://ai` — che un dispositivo debole non deve mai sapere quale nodo risponde — non richiede nulla di nuovo: è esattamente `discoverService()`/`callService()` già esistenti dalla Slice 6, dimostrato con un test a tre nodi (debole → relay → gateway, il debole non si connette mai direttamente al gateway) che rispecchia lo schema di `three-node-relay.test.ts`. `FakeOllamaServer` (`gateway/nomad/fake-ollama-server.ts`) sostituisce un'istanza Ollama reale (bloccata, nessun Docker disponibile in questa sessione) con un unico endpoint `POST /api/generate` e un dizionario di risposte pre-registrate per parola chiave — non un modello linguistico di alcun tipo, solo quanto basta a dimostrare il giro completo. La revisione (livello alto, trattandosi di una nuova superficie di rete che parla con un backend esterno) ha trovato 2 problemi reali, entrambi corretti:
    - **Triplicazione di `sendJson()`**: `fake-ollama-server.ts` ne conteneva una terza copia quasi identica a quelle già presenti in `web-ui.ts` e `fake-nomad-server.ts`, contraddicendo la convenzione che lo stesso `LoopbackHttpServer` documenta esplicitamente di aver seguito ("estratto dopo che questo pattern è comparso due volte"). Corretto esportando un `sendJson()` condiviso da `node/src/loopback-http-server.ts` (la stessa infrastruttura HTTP minimale già condivisa), usato ora da tutti e tre i chiamanti.
    - **`readBody()` etichettava qualunque errore di stream come "corpo troppo grande" (413)**: un client che interrompe la connessione a metà upload (es. reset TCP) veniva segnalato con lo stesso errore di chi supera davvero il limite di dimensione, un'etichetta fuorviante se mai osservabile. Corretto distinguendo esplicitamente l'eccezione dedicata (`BodyTooLargeError`, sollevata solo al superamento della soglia) da qualunque altro errore di stream, con una risposta HTTP diversa per ciascun caso; aggiunto anche un controllo che evita di scrivere una risposta su una connessione già chiusa dal client. Due scenari di crash "candidati" suggeriti dalla storia di bug reali del progetto (#16, #19) — `res.end()` dopo `req.destroy()`, e un abort del client a metà body — sono stati verificati empiricamente con piccoli script Node/tsx dedicati: nessuno dei due fa effettivamente crashare il processo in questa versione di Node, quindi non sono stati corretti oltre alla mislabeling già descritta.

    Vedi `tests/integration/ai-gateway.test.ts` (5 test, incluso lo scenario centrale a tre nodi). Dopo questa slice non risultano altri candidati aperti realizzabili in puro software nella specifica — vedi `docs/next-steps.md` per l'elenco aggiornato.
21. **Interfaccia web locale ampliata** (spec §59, su richiesta esplicita dell'utente — non una delle 10 slice numerate, ma stesso workflow riapplicato): `WebUiServer` passava solo conteggi aggregati (numero di vicini, numero di servizi disponibili); ora espone tre nuovi endpoint di sola lettura — `GET /api/peers` (nodeId, trust level da `node.trust.get()`, `connectedAt`/`lastSeen`), `GET /api/services` (elenco completo, incluse le entry non disponibili, con `isLocal`/`providerLabel`), `GET /api/content` (tutto il contenuto conosciuto, senza dover passare da `/api/search` che con query vuota resta deliberatamente `[]`, comportamento invariato) — più un nuovo campo `relaying` in `/api/status`, dietro un nuovo metodo pubblico `NomadNode.canRelayNow()` (delega a `RelayPolicy.canRelayNow()`, che era raggiungibile solo da un campo privato). La pagina servita da `/` è stata riscritta da una lista `<dl>` monospace minimale a un layout a card con badge per trust level/disponibilità e ricerca "live" con debounce, restando `textContent`-only (mai `innerHTML`) e a sola lettura (nessuna azione di scrittura/controllo esposta — principio già documentato nella classe, preservato deliberatamente). La revisione (livello medio-alto: superficie letta da un browser reale) ha trovato 3 problemi reali, tutti corretti:
    - **Un `ServiceAnnouncement` validamente firmato ma con `capabilities` non-array mandava in crash il pannello servizi lato browser**: una firma Ed25519 valida dimostra solo che il `providerId` dichiarato ha prodotto esattamente quel JSON, non che la sua *forma* sia sensata — un peer che genera una propria coppia di chiavi (economico, chiunque può farlo) può autofirmare un annuncio con `capabilities: "qualcosa"` invece di un array, e `verifyServiceAnnouncement()` lo accetta comunque perché verifica solo la firma, non la forma. `recordServiceAnnouncement()` in `node.ts` controllava già `typeof serviceId`/`typeof providerId` ma non `Array.isArray(capabilities)` — corretto aggiungendo quel controllo alla stessa guardia, così l'annuncio malformato non entra mai in `ServiceDirectory` né arriva a `/api/services`. Aggiunta anche una guardia lato client (`Array.isArray(...) ? ... : []` prima del `.map()`) come seconda linea di difesa. Nuovo test di regressione in `tests/integration/service-discovery.test.ts` che costruisce un annuncio validamente firmato ma malformato via socket grezzo (stesso pattern di `malformed-packet-robustness.test.ts`) e verifica che non venga mai memorizzato.
    - **Race condition nella ricerca "live"**: il debounce (250ms) sull'input di ricerca non teneva traccia di quale richiesta fosse la più recente — una risposta più lenta per una query più vecchia poteva arrivare dopo quella per una query più nuova (specialmente in corsa con il refresh periodico ogni 5s) e sovrascriverne il risultato a schermo. Corretto con un contatore di richiesta incrementato a ogni chiamata: solo la risposta corrispondente alla richiesta più recente viene effettivamente renderizzata.
    - **Il campo di ricerca aveva perso la possibilità di un invio esplicito**: il redesign aveva rimosso il `<form>`/submit sostituendolo solo con il debounce, contraddicendo l'intento dichiarato di mantenerlo "oltre al submit del form" — premere Invio non faceva più nulla di diverso da qualunque altro tasto. Corretto ripristinando il `<form>` con un handler di submit che cancella il debounce pendente e cerca subito.

    Vedi `tests/integration/web-ui.test.ts` (15 test, 4 nuovi) e `tests/integration/service-discovery.test.ts` (9 test, 1 nuovo).
22. **`POST /api/call` — primo endpoint di scrittura su `WebUiServer`** (spec §36-37 `CALL service://...`, base server-side per la Fase 1 dell'app mobile — `docs/next-steps.md` Opzione H, su richiesta esplicita dell'utente): fino a questa voce `WebUiServer` era rigorosamente di sola lettura. Il nuovo endpoint invoca `NomadNode.callService()` per conto di un chiamante esterno (pensato per un telefono sulla stessa rete Wi-Fi), quindi richiede una postura di sicurezza diversa dal resto della classe:
    - **Disattivato di default** (`allowServiceCalls: boolean`, default `false`) — se attivato senza passare anche `pairingToken`, il costruttore lancia subito un errore invece di un default silenzioso pericoloso.
    - **Token di pairing obbligatorio, confrontato a tempo costante** (`node:crypto.timingSafeEqual`, con un controllo preliminare sulla lunghezza dei buffer prima del confronto, dato che `timingSafeEqual` richiede buffer di pari lunghezza — leggere la lunghezza del token è un rischio accettato molto più piccolo di leggerne i byte). Generato fresco a ogni avvio da `cli.ts` (`--allow-service-calls`), stampato una sola volta in console, mai servito indietro via HTTP.
    - **404, non 401, quando l'endpoint è disattivato** — l'obiettivo è non confermare nemmeno l'esistenza della capacità a chi non ha già il token.
    - `host` resta configurabile (nuovo flag `--web-host` in `cli.ts`, prima impossibile impostarlo) per permettere il bind su un indirizzo LAN quando un telefono deve raggiungere il nodo — ma il pairing token protegge `/api/call` indipendentemente da dove è in ascolto il server, non solo quando non è su loopback.
    - **CORS**: un'app mobile è un'origine diversa dal server (una WebView Capacitor, non una pagina servita da questo stesso processo), quindi le sue `fetch()` sono cross-origin e verrebbero bloccate dal browser/WebView senza un'intestazione `Access-Control-Allow-Origin` esplicita. Legato deliberatamente allo stesso flag `allowServiceCalls` invece che a un terzo interruttore separato: quando disattivato, nessuna intestazione CORS viene mai aggiunta (comportamento identico a prima di questa funzionalità, zero rischio per chi non la usa); quando attivo, ogni risposta la porta, e le richieste `OPTIONS` di preflight per `/api/call` (necessarie perché l'header `Authorization` personalizzato rende la richiesta POST "non semplice" per le regole CORS) ricevono una risposta `204` dedicata — anch'essa un `404` quando la funzionalità è disattivata, stessa postura "non confermare l'esistenza della capacità" del resto della voce.

    La revisione (livello alto: prima superficie di scrittura mai esposta da questo componente) ha trovato 4 problemi reali, tutti corretti prima di considerare la funzionalità conclusa:
    - **Un `serviceId` arbitrario e sconosciuto poteva far floodare `SERVICE_QUERY` a tutta la mesh**: `callService()` (`node.ts`), se il servizio non è già conosciuto localmente, cade nel percorso di `discoverService()` che floooda una query a tutti i vicini (spec §21 controlled flooding), a sua volta rifloodata da ogni peer che non la riconosce. Una singola richiesta HTTP autenticata poteva quindi generare traffico di rete su scala mesh, a differenza di ogni altro endpoint della classe (che legge solo lo stato locale). Corretto restringendo `POST /api/call` ai soli servizi già conosciuti *e* disponibili (`isKnownAvailableService()`, la stessa definizione già usata dal conteggio `services` di `/api/status`) — lo stesso insieme che `/api/services` mostra già al chiamante, coerente con il flusso reale previsto (il telefono sfoglia i servizi noti, poi ne chiama uno).
    - **Timeout doppio invece che singolo**: `callService()` applica lo stesso `timeoutMs` in modo indipendente sia alla fase di scoperta sia a quella di invocazione (`node.ts`), quindi il caso peggiore reale era fino al doppio del tetto dichiarato (`MAX_CALL_TIMEOUT_MS`, 15 secondi → fino a 30 reali). Corretto avvolgendo l'intera `node.callService()` in una corsa (`callWithHardTimeout()`) contro un singolo timer al livello dell'endpoint HTTP: la connessione non resta mai aperta più a lungo di quanto dichiarato, indipendentemente da quanti timer interni `callService()` usa — la chiamata sottostante non viene annullata (nessun `AbortController` collegato a `callService()` oggi) e può comunque completarsi o fallire più tardi, in background, senza effetti visibili.
    - **Nessun limite di tempo sulla lettura del body**: `readRequestBody()` non aveva alcun proprio limite temporale — l'unico argine era il `requestTimeout` di default di `http.Server` (300 secondi), molto più permissivo del tetto di 15 secondi che l'endpoint dichiara. Un chiamante già autenticato (in possesso del token) che invia il body a scaglioni minuscoli restando sotto il limite di byte poteva tenere una connessione (e il socket/memoria collegati) aperta per minuti. Corretto aggiungendo un parametro `timeoutMs` opzionale a `readRequestBody()` (condiviso in `loopback-http-server.ts`, usato anche da `FakeOllamaServer` che però non lo passa, restando invariato) — `WebUiServer` lo imposta a 10 secondi per `/api/call`.
    - **Ternario morto dopo il refactor di `handleRequest()`**: la condizione `req.method === "GET" || req.method === "HEAD" ? ... : "/"` non poteva più raggiungere mai il ramo `else`, dato che a quel punto nel flusso `req.method` è già garantito essere `GET`/`HEAD` (i rami POST e "altro metodo" ritornano prima). Non un bug di comportamento, solo un residuo fuorviante — rimosso.

    Vedi `tests/unit/web-ui.test.ts` (4 test su `resolveCallTimeoutMs()`, puri) e la nuova sezione `describe("WebUiServer POST /api/call")` in `tests/integration/web-ui.test.ts` (15 test: costruttore che lancia senza `pairingToken`, 404 quando disattivato, chiamata riuscita, 401 senza/con token errato, 400 su `serviceId` non stringa, 404 su servizio sconosciuto, 404 su servizio noto ma non disponibile, 502 con l'errore del servizio, 400 su JSON malformato, timeout onorato su una chiamata remota — a due nodi reali, perché una chiamata a un servizio registrato *localmente* prende la scorciatoia in-process di `callService()` che salta qualunque timeout — più 4 test sull'intestazione CORS/preflight OPTIONS, assente/presente a seconda di `allowServiceCalls`).
23. **App mobile Fase 1** (`mobile/`, `docs/next-steps.md` Opzione H, su richiesta esplicita dell'utente): client Capacitor (vanilla JS, nessun framework) verso un `WebUiServer` esistente — setup/pairing (indirizzo + token), dashboard (vicini/servizi/contenuti), invocazione servizi tramite `POST /api/call`. Verificato end-to-end con un browser reale (Playwright, viewport 390×844, non un telefono fisico) contro un `NomadNode` vero: pairing riuscito, dashboard con dati reali, chiamata a `service://ai` con risposta a schermo, gestione dell'errore quando il gateway non è raggiungibile. La revisione (livello alto: prima interfaccia mai esposta a un utente reale su un dispositivo reale in questo progetto) ha trovato 6 problemi reali, tutti corretti prima di considerare la fase conclusa:
    - **Il traffico HTTP in chiaro verso il gateway sarebbe stato bloccato di default da Android**: con `targetSdkVersion 36` (`mobile/android/variables.gradle`), Android blocca il traffico "cleartext" (non-TLS) dalle API 28 in su, e `WebUiServer`/`LoopbackHttpServer` servono solo `http://` semplice, mai TLS. Su un telefono Android reale — l'unico scenario hardware reale che questa fase copre — ogni `fetch()` dell'app verso il gateway sarebbe stata bloccata a livello di sistema operativo prima ancora di uscire dal dispositivo, con un errore di rete generico indistinguibile da un gateway irraggiungibile. Non trovato prima perché la verifica era avvenuta solo in un browser desktop (Playwright), non soggetto alla stessa policy. Corretto aggiungendo `mobile/android/app/src/main/res/xml/network_security_config.xml` (`cleartextTrafficPermitted="true"`, deliberatamente non ristretto a host specifici perché l'indirizzo del gateway è inserito dall'operatore e non è noto in anticipo) e referenziandolo da `AndroidManifest.xml`.
    - **Il form "Chiama" veniva distrutto dal refresh periodico ogni 5s**: `renderServices()` svuotava e ricostruiva l'intera lista servizi a ogni tick, perdendo un prompt a metà digitazione o il risultato di una chiamata appena arrivata se l'utente aveva un form aperto in quel momento — senza alcun indizio visibile che fosse successo qualcosa. Corretto tracciando quale servizio ha un form aperto (`openCallServiceId`) e riusando lo stesso nodo DOM `<li>` per quel servizio invece di ricostruirlo, lasciando tutti gli altri invariati. Verificato riaprendo il form, scrivendo un prompt, aspettando più di 5 secondi (il refresh periodico), e confermando che il testo digitato fosse ancora lì e che l'invio funzionasse normalmente.
    - **Nessuna guardia contro risposte superate per stato/vicini/servizi**: solo la ricerca contenuti aveva un contatore anti-race (`contentRequestId`); il resto della dashboard (`refreshAll()`) poteva mostrare una risposta più vecchia arrivata dopo una più recente su una connessione instabile. Corretto con lo stesso pattern (`refreshCycleId`), esteso a tutto il ciclo di refresh.
    - **Nessun timeout lato client su nessuna `fetch()`**, incluso `POST /api/call`: una connessione bloccata (es. il telefono che perde il Wi-Fi a metà richiesta) lasciava l'interfaccia bloccata indefinitamente, perché il tetto lato server (`MAX_CALL_TIMEOUT_MS`) non ha modo di scattare se la richiesta non arriva mai al server. Corretto con un `AbortController` e un timeout dedicato per le letture (10s) e per le chiamate a servizio (20s, volutamente sopra il tetto server-side di 15s così l'errore "pulito" del server arriva per primo nel caso comune).
    - **Il pairing verificava solo che il gateway fosse raggiungibile, mai che il token fosse corretto**: un token digitato male veniva comunque salvato (la verifica usa `GET /api/status`, che non richiede token), e l'errore si manifestava solo più tardi come un generico 401 alla prima chiamata a un servizio, perdendo il contesto che il problema era il token inserito in fase di setup. Corretto propagando lo status HTTP dell'errore fino al messaggio mostrato nel form di chiamata, con un testo dedicato per il 401 che rimanda esplicitamente alla schermata di setup.
    - **La persistenza di indirizzo/token in `localStorage`, in chiaro, non era documentata**: compromesso ragionevole per un prototipo di validazione, ma non dichiarato esplicitamente. Documentato in `mobile/README.md` con il rischio concreto (accesso allo storage della WebView su un dispositivo condiviso/compromesso) e le alternative per un uso reale futuro.

    Vedi `mobile/README.md` per il dettaglio completo (incluso lo stato della build nativa: `npx cap add android` genera correttamente il progetto Gradle, ma la compilazione reale non è completabile in questo ambiente — `dl.google.com`, da cui si scarica l'Android Gradle Plugin, è bloccato dalla policy di rete della sessione remota, `403` dal proxy, non un problema di codice).
24. **Pairing "come una rete Wi-Fi" — rinominato da indirizzo+token a nome rete+password** (su richiesta esplicita dell'utente: "vorrei che l'utente abbia l'impressione di connettersi ad un normale Wi-Fi", seguito della voce #23): il modello concettuale del punto #22/#23 ("indirizzo del gateway" + "token di pairing", una stringa esadecimale lunga) è stato rinominato in `WebUiOptions.networkName` (cosmetico, come un SSID — default `node.displayName`) e `WebUiOptions.networkPassword` (rinominato da `pairingToken`, stessa semantica di autenticazione), con un formato breve e digitabile (`generateNetworkPassword()`, es. `K7XM-2QRT`) al posto di un hex lungo. Due cambi non solo nominali:
    - **Nuovo endpoint `GET /api/pairing`** (`handlePairing()`, `node/src/web-ui.ts`): restituisce `{networkName, networkPassword}` in chiaro, deliberatamente **senza autenticazione** — richiedere la password per leggere la password sarebbe circolare — quindi esposto esattamente come ogni altro endpoint di sola lettura di questa classe: chiunque possa raggiungere questo server LAN-bound può leggerlo, non solo l'utente umano davanti allo schermo. Popolato anche nella dashboard web del gateway stessa (nuovo pannello "Collega un telefono" in `PAGE_HTML`), su richiesta esplicita dell'utente ("questi dati si possono esporre anche su interfaccia pubblica e non solo da terminale?") — `cli.ts` continua comunque a stamparli anche in console.
    - **UX mobile "indirizzo una tantum"**: `mobile/www/app.js` mostra il campo indirizzo del gateway solo al primo avvio (nessun indirizzo ancora in `localStorage`) o dietro un link esplicito "Cambia indirizzo"; ai riavvii successivi resta nascosto e si chiede solo la password, e la dashboard non mostra mai più l'indirizzo grezzo (solo "Connesso a: `<nome rete>`", da `StatusPayload.networkName`). Un 401 su una chiamata di servizio dal vivo (es. il gateway è stato riavviato e ha generato una password nuova) riporta l'utente alla schermata di setup in modalità "solo password" (`handlePasswordRejected()` — l'indirizzo resta salvato), mentre il bottone "Cambia rete" nella dashboard è un reset completo (indirizzo *e* password cancellati) — asimmetria intenzionale: il primo caso è "la stessa rete, password cambiata", il secondo è "voglio unirmi a una rete diversa", esattamente come un telefono distingue "la password Wi-Fi è cambiata" da "dimentica questa rete".

    La revisione (livello medio-alto: cambia il modello di autenticazione di un endpoint di scrittura già in produzione concettuale) ha trovato 4 problemi reali, tutti corretti prima di considerare la modifica conclusa:
    - **L'alfabeto della password aveva 31 simboli, non i 32 dichiarati dal proprio stesso commento** *(il più grave, corretto)*: `"ABCDEFGHJKMNPQRSTUVWXYZ23456789"` esclude 0/O/1/I/L (5 caratteri) da 36 alfanumerici, non 4 — 31 simboli. Poiché `256 % 31 !== 0`, `randomBytes()[i] % 31` era misurabilmente distorto verso i primi 8 simboli dell'alfabeto (probabilità 9/256 invece di 8/256), contraddicendo esplicitamente l'assenza di bias che il commento del codice affermava. Corretto passando all'alfabeto Base32 di Crockford (`"0123456789ABCDEFGHJKMNPQRSTVWXYZ"`, esclude I/L/O/U invece — con I/O assenti, le cifre 0/1 restano inequivocabili pur non essendo escluse), esattamente 32 simboli, nessun bias residuo. Coperto ora da `tests/unit/web-ui.test.ts` (che fissa lunghezza dell'alfabeto, formato, e assenza di I/L/O/U — non c'era alcun test sulla generazione della password prima di questa revisione).
    - **La verifica della password durante il setup mobile poteva dare un falso positivo contro un gateway con pairing disattivato**: `validateNetworkPassword()` (`mobile/www/app.js`) considera "password accettata" qualunque risposta diversa da 401 a una `POST /api/call` di sonda — ma `handleCall()` risponde 404 (mai 401) quando `allowServiceCalls` è `false`, indipendentemente da qualunque header `Authorization`, quindi qualunque password digitata veniva "accettata" contro un gateway che non supporta affatto il pairing. In pratica il CORS di un vero browser cross-origin blocca già questo scenario a monte (`GET /api/status` fallisce con un errore di rete generico prima di arrivare alla sonda, perché un gateway con `allowServiceCalls` disattivato non invia mai l'header CORS — verificato con Playwright), ma la correzione resta comunque necessaria come difesa in profondità per ogni contesto che non applichi CORS altrettanto rigidamente. Corretto controllando `status.networkName` (presente in `/api/status` solo quando il gateway ha il pairing attivo) subito dopo la verifica di raggiungibilità, prima di provare la password, con un messaggio d'errore dedicato.
    - **`handlePairing()` e `handleCall()` avevano nozioni diverse di "pairing abilitato"**: `handlePairing()` controllava `!this.networkName` (falsy), che tratta un `displayName` impostato a stringa vuota come "pairing disattivato" e risponde 404 anche quando `POST /api/call` (il cui controllo non tocca affatto `networkName`) funziona regolarmente — un'inconsistenza tra i due endpoint. Corretto controllando `this.networkName === undefined` (l'unico caso in cui il costruttore lo lascia davvero non impostato), non la sua verità booleana. Coperto da un nuovo test in `tests/integration/web-ui.test.ts` che usa esplicitamente un `displayName` vuoto.
    - **Il bottone "Cambia rete" non ripuliva il campo indirizzo visibile nel DOM**: cancellava `localStorage` e le variabili JS ma lasciava il valore precedente nell'`<input>` dell'indirizzo (scritto lì solo dal ramo "indirizzo già noto" di `showSetupScreen()`, mai ripulito da un reset completo), incoerente con la semantica di "ricomincia da capo" del bottone. Corretto svuotando esplicitamente il campo nel gestore del click, prima di richiamare `showSetupScreen()`.

    Verificato end-to-end con Playwright (browser reale, non solo unit test): primo avvio mostra il campo indirizzo, password sbagliata mostra errore senza salvare nulla, password corretta connette e la dashboard mostra "Connesso a: `<nome rete>`", reload della pagina salta la setup screen (credenziali persistite), "Cambia rete" resetta tutto visibilmente (incluso il campo indirizzo), un 401 simulato durante una chiamata riporta alla setup screen in modalità solo-password con l'indirizzo preservato, un gateway con pairing disattivato non fa più accettare falsamente nessuna password, e il pannello "Collega un telefono" sulla dashboard del gateway mostra nome rete e password corretti. Vedi `tests/unit/web-ui.test.ts` (4 nuovi test su `generateNetworkPassword()`) e `tests/integration/web-ui.test.ts` (nuova sezione `describe("WebUiServer GET /api/pairing")`, 4 test).
25. **Pairing via QR code** (su richiesta esplicita dell'utente, seguito della voce #24: "implementa anche opzione con QR code"): il pairing manuale nome-rete+password resta, ma la dashboard del gateway mostra ora anche un QR — inquadrarlo con l'app mobile compila indirizzo/nome/password da sola, azzerando la digitazione anche per l'indirizzo "una tantum" della voce #24.
    - **`node/src/qrcode.ts` (nuovo, encoder QR scritto da zero)**: non esiste alcuna libreria QR nel progetto e la convenzione del repo vieta dipendenze esterne non necessarie (`node:http` puro invece di Express, niente framework di parsing in `tcp.ts`, ecc.) — scrivere l'encoder (ISO/IEC 18004) da zero era l'opzione coerente. Solo byte mode, versioni 1-10 (fino a ~272 byte a EC level L, ampiamente sufficiente per un URI di pairing), EC level M con fallback a L quando necessario per la capacità; `encodeQr()` restituisce `undefined` (mai un'eccezione) quando il testo non entra in nessuna combinazione versione/EC level, cioè quando un operatore ha scelto un `--network-name` molto lungo — la dashboard degrada silenziosamente al solo pannello testuale in quel caso, il pairing manuale resta comunque disponibile.
    - **Verifica**: costruire un encoder QR a mano è un'area in cui un bug silenzioso (un QR che sembra corretto ma non si legge, o peggio legge dati sbagliati) è facile e difficile da notare "a occhio" solo dal codice. Verificato durante lo sviluppo contro un decoder indipendente (`jsQR`, installato **solo** in una directory di scratch fuori dal repository, mai come dipendenza — niente `package.json`, niente `npm install` nel repo) su centinaia di round-trip (ogni versione 1-10, entrambi gli EC level, tutte le 8 maschere, contenuto UTF-8, casi limite di capacità) prima e durante la revisione di codice — hanno trovato **due bug reali**, entrambi nel posizionamento delle informazioni format/version (non nella logica dati/Reed-Solomon), corretti prima di considerare l'encoder pronto:
        1. Lo split della seconda copia del format-info (i 15 bit ridondanti vicino ai finder pattern in basso-a-sinistra/alto-a-destra) era 8/7 invece di 7/8 — sovrapponeva esattamente il "dark module" fisso della spec, la cui riga coincide sempre con `size-8` per costruzione (`size = 17+4·versione`), quindi non un caso limite raro ma un bug presente in **ogni** versione.
        2. Il version-info (richiesto solo dalla versione 7 in su) usava un ordinamento sbagliato dei 18 bit nel blocco 6×3: column-major invece di row-major, e MSB-first invece di LSB-first — una convenzione diversa (e corretta per coincidenza strutturale) da quella già usata per il format-info.
      Dopo i fix: 108+ combinazioni verificate durante lo sviluppo, poi altre 189+ (boundary di lunghezza, contenuto casuale) durante la revisione di codice — tutte decodificate correttamente. `tests/unit/qrcode.test.ts` (nuovo) copre invece solo proprietà strutturali via la sola API pubblica (determinismo, versione/dimensione corrette, forma dei finder pattern, alternanza del timing pattern) — dichiarato esplicitamente nel commento del file che non è un vero test di decodifica, dato che nessun decoder QR è nel repository.
    - **`node/src/web-ui.ts`**: nuovo `WebUiOptions.publicHost` (l'indirizzo LAN che un telefono userebbe per raggiungere questo nodo), con auto-rilevamento via `node:os.networkInterfaces()` (prima interfaccia IPv4 non-interna) quando `host` è un wildcard/loopback e `publicHost` non è specificato — un tentativo "best effort" che può sbagliare su una macchina con più interfacce (VPN, bridge Docker...), motivo per cui l'opzione esplicita esiste per sovrascriverlo. `GET /api/pairing` include ora `address` (`host:porta`) e `qrDataUri` (un `data:image/svg+xml;base64,...` che codifica `nomadnet://pair?h=<address>&n=<networkName>&p=<networkPassword>`) quando un host pubblico è risolvibile — entrambi omessi silenziosamente (non un errore) altrimenti. Risposta memoizzata (`cachedPairingInfo`) dopo la prima chiamata: nome rete/password/host pubblico/porta sono tutti fissi per la vita del processo, quindi ricalcolare l'encoding QR (prova di 8 maschere + Reed-Solomon) a ogni richiesta sarebbe stato lavoro sprecato — problema di efficienza reale trovato dalla revisione, corretto.
    - **`mobile/www/app.js`**: nuovo bottone "Scansiona QR" nella schermata di setup, mostrato solo se `"BarcodeDetector" in window` (Shape Detection API nativa del browser, zero dipendenze — disponibile su Chrome/Android WebView da Chrome 83, **non** su Firefox/Safari, e nemmeno nel Chromium headless sandboxato di questa sessione di sviluppo, quindi il percorso di scansione live via fotocamera reale non è verificabile end-to-end in questo ambiente, stessa classe di limite già documentata per la build Android nativa). `parsePairingUri()` (funzione pura, testata iniettando stringhe sintetiche) interpreta l'URI scansionato; `applyScannedPairing()` compila i campi e richiama lo stesso `requestSubmit()` del form manuale — il contenuto del QR non è mai fidato ciecamente, passa dalla stessa sonda di verifica password di un inserimento a mano. `mobile/android/app/src/main/AndroidManifest.xml` ha guadagnato `<uses-permission android:name="android.permission.CAMERA">` e `<uses-feature android:name="android.hardware.camera" required="false">` (quest'ultimo per non escludere l'app dal Play Store su un dispositivo senza fotocamera — il bottone resta comunque un'opzione, mai un requisito).
      La revisione ha trovato 2 problemi reali qui, entrambi corretti:
        - **Race condition su "Annulla" durante `getUserMedia()` in sospeso**: se l'utente toccava "Annulla" mentre il prompt di permesso della fotocamera era ancora a schermo (o l'hardware era lento a inizializzarsi), `stopScanner()` era un no-op (né `scannerStream` né `scannerRafId` erano ancora impostati) — quando `getUserMedia()` risolveva più tardi, la fotocamera veniva comunque agganciata e il loop di scansione avviato, con l'overlay ormai nascosto e nessun indicatore visibile, recuperabile solo ricaricando l'app. Corretto con un contatore di sessione (`scannerSessionId`, incrementato da `stopScanner()`): `startScanner()` verifica di essere ancora la sessione corrente sia dopo un fallimento sia dopo un successo di `getUserMedia()`, e se non lo è più rilascia immediatamente lo stream appena ottenuto invece di usarlo.
        - **Il campo `name` (nome rete) veniva estratto da `parsePairingUri()` ma non letto da nessuna parte dell'app** — dati analizzati e mai usati. Rimosso dall'oggetto restituito (il campo resta nell'URI per uso futuro lato server/QR, semplicemente questa versione dell'app non lo consuma) invece di tenerlo come stato morto.
    - Verificato end-to-end con Playwright: pannello QR sulla dashboard del gateway renderizzato correttamente (screenshot), bottone di scansione assente quando l'API non c'è (nessun buco nel layout), scansione simulata (chiamando `parsePairingUri()`/`applyScannedPairing()` direttamente, dato che la fotocamera/BarcodeDetector non sono disponibili in questo ambiente) sia con password corretta (pairing riuscito) sia sbagliata (rifiutato con lo stesso errore del flusso manuale, nessun bypass), e il fix della race di cancellazione (fotocamera finta iniettata via `getUserMedia` sostituito, risolto deliberatamente dopo la cancellazione — mai agganciata, traccia rilasciata).

    Vedi `tests/unit/qrcode.test.ts` (10 test) e `tests/integration/web-ui.test.ts` (nuova sezione `describe("WebUiServer GET /api/pairing — address and QR")`, 4 test).
26. **Passata libera di qualità/sicurezza su `node/src/`** (su richiesta esplicita dell'utente, non seguito di una feature specifica — dopo che tutte le slice pianificate e i follow-up successivi risultavano completi): `code-review --level xhigh` senza un diff specifico da rivedere, sull'intero package `node/src/`. Ha trovato 3 problemi reali, tutti corretti:
    - **Ogni handler `CONTENT_*` (`handleContentQuery`/`handleContentFound`/`handleContentNotFound`/`handleContentRequest`/`handleContentChunk`/`handleContentComplete`/`observeRelayedContent`, `node.ts`) accedeva ai campi di `packet.payload` senza alcuna guardia** *(il più grave, corretto)* — a differenza di ogni altro handler (`SYNC_*`/`IDENTITY_*`/`ROUTE_ANNOUNCE`/`SERVICE_*`/`PRIVATE_MESSAGE`), che segue già la disciplina `packet.payload?.campo` + controllo `typeof`/`Array.isArray` (vedi bug #16, la stessa classe di problema, mai estesa a questa famiglia di handler quando introdotta). Un singolo `CONTENT_QUERY` senza `payload` da un qualunque peer TCP connesso — non serve essere il destinatario, `observeRelayedContent` prende lo stesso pacchetto anche solo per il caching passivo durante il relay, quindi il bug era raggiungibile pure da chi non è la destinazione finale — bastava a far crashare l'intero processo (DoS non autenticato). Verificato empiricamente prima e dopo il fix (`git stash` del file corretto, ri-eseguiti i nuovi test, confermato il crash esatto previsto — `TypeError: Cannot read properties of undefined (reading 'contentId')` — poi ripristinato). Corretto estendendo la stessa disciplina difensiva a tutti e sette gli handler; `chunkIndex`/`totalChunks` passano attraverso senza validazione propria perché `ChunkAssembler.addChunk()` (`content.ts`) già li valida a runtime indipendentemente dal tipo dichiarato.
    - **Il case `PEER_LIST` in `deliver()` faceva `for (const entry of packet.payload as Array<...>)`** — solo un type assertion, mai un controllo a runtime — quindi un payload non-array (`for...of` su un non-iterabile) o un'entry `null`/non-oggetto (`entry.id` su `null`) crashava allo stesso modo. Corretto filtrando esplicitamente `Array.isArray(packet.payload)` e ogni entry per forma prima di usarla.
    - **`RoutingTable` non aveva eviction pesata sulla fiducia** (a differenza di `PeerDirectory`/`RemoteCatalog`, corrette nella Slice 3, bug #13) — una rotta non porta alcuna firma propria (non può: rappresenta "il costo da qui a X", cambia continuamente, non è una rivendicazione di X su se stesso), quindi la sua unica base di fiducia è il livello di fiducia del peer che l'ha annunciata (`nextHop`, sempre `fromPeerId`, mai falsificabile via payload). Senza eviction pesata, un peer appena connesso e mai fidato (`TrustLevel.SEEN`) poteva sfrattare rotte legittime verso destinazioni genuinamente raggiungibili semplicemente annunciandone abbastanza in un solo `ROUTE_ANNOUNCE` — nessun limite esisteva nemmeno sul numero di entry che un singolo pacchetto poteva contenere. Corretto in due parti: nuova opzione `RoutingTableOptions.trustRank?: (nextHop: string) => number` (routing-table.ts), cablata nel costruttore di `NomadNode` come `(nextHop) => trustRank(this.trust.get(nextHop))`, stesso pattern già usato da `RemoteCatalog`/`PeerDirectory`; più una nuova costante `MAX_ROUTES_PER_ANNOUNCE = 512` che limita quante entry di un singolo `ROUTE_ANNOUNCE` vengono effettivamente processate (le altre scartate silenziosamente, non l'intero pacchetto rifiutato) — difesa aggiuntiva perché anche con eviction pesata un singolo pacchetto sovradimensionato avrebbe comunque speso CPU/memoria non necessarie prima che l'eviction avesse voce in capitolo. Verificato empiricamente anche questo fix con lo stesso schema stash/rerun/pop.

    Vedi `tests/integration/malformed-packet-robustness.test.ts` (15 nuovi casi: uno per ciascuno dei sei handler `CONTENT_*` più `PEER_LIST`, più una funzione helper dedicata `expectRelaySurvives` per esercitare `observeRelayedContent()` attraverso il percorso di relay — `destination` diverso dal nodo vittima, cosicché `deliverLocally` sia `false` e il pacchetto prenda il ramo di caching passivo invece dei normali handler), `tests/unit/routing-table.test.ts` (2 nuovi test su `trustRank`), e `tests/integration/trust-aware-eviction.test.ts` (1 nuovo test che dimostra una rotta fidata sopravvivere a un flood da un peer non fidato, più un nuovo `describe` separato che verifica `MAX_ROUTES_PER_ANNOUNCE` limitare davvero le entry effettivamente applicate da un singolo pacchetto sovradimensionato). 314 test totali, tutti verdi, girati ripetutamente; typecheck e build puliti.

27. **`service://news` (`NewsGateway`) + UI mobile "motore di ricerca"** (su richiesta esplicita dell'utente: "un interfaccia che mostri i servizi disponibili come fosse un motore di ricerca, stile Google, con collegamenti rapidi ai servizi principali [...] esiste un servizio per le news? Che funzioni allo stesso modo, aggiornato ogni volta che si può"): due pezzi correlati, stesso workflow a doppio check.

    **Backend — `gateway/nomad/news-gateway.ts`**, un terzo sotto-servizio NOMAD accanto a Kiwix/Ollama (spec §37), design ibrido perché le news hanno bisogno di entrambi i pattern esistenti: come `KiwixGateway.syncCatalog()`, ogni notizia viene pubblicata via `publishContent()` (deduplicata per id, stessa forma di `publishedByPath`), così è raggiungibile/propagabile attraverso il normale ciclo `CONTENT_QUERY` e lo store-and-forward anche verso nodi che non toccano mai questo gateway direttamente; come `AiGateway.registerAiService()`, è anche raggiungibile come `service://news`, ma a differenza di `service://ai`/`service://kiwix-search` il suo handler non blocca mai su una chiamata HTTP live — risponde sempre dalla cache in memoria (`cachedHeadlines`), aggiornata solo da `syncNews()`/il nuovo `startAutoSync(intervalMs, onError?)` — una vera capacità nuova, non presente in `KiwixGateway`, che risponde a "aggiornato ogni volta che si può" con un timer di sync periodico invece di un fetch on-demand a ogni richiesta. **Scelta esplicita dell'utente, non una svista**: `NewsGateway` non ha nessun server fittizio spedito (a differenza di `FakeNomadServer`/`FakeOllamaServer`) — `cli.ts` registra `service://news` solo se `--news-url` è dato esplicitamente, senza fallback sintetico. Confermato inoltre che l'accesso a internet reale è bloccato in questa sandbox (richieste dirette a feed RSS ANSA/BBC, entrambe respinte con 403 dalla policy del proxy — solo pochi host in allowlist), quindi questo gateway non è comunque verificabile end-to-end contro un backend reale in questa sessione, stesso limite di "scritto secondo spec, non verificabile qui" già documentato per Docker/NOMAD e la build Android nativa — solo per un motivo diverso (policy di rete invece di assenza di Docker). Il test in `tests/integration/news-gateway.test.ts` usa un proprio responder HTTP minimale locale (`node:http` grezzo, mai esportato, mai presentato come mock riusabile) proprio per rispettare "niente fake server" pur mantenendo test automatici significativi.

    **UI mobile — `mobile/www/index.html`/`app.js`/`styles.css`**: la dashboard è stata ristrutturata spostando il wordmark "NOMAD-NET" e la barra di ricerca dei contenuti in una nuova sezione centrata `#home-hero` (wordmark + casella di ricerca a pillola + riga di "collegamenti rapidi", uno per ogni servizio attualmente disponibile, con icona da una mappa `SERVICE_ICONS` — 🤖 AI, 📚 Kiwix, 📰 news, 🔧 fallback generico — e etichetta abbreviata), posizionata tra l'header e le statistiche. Toccare un collegamento rapido apre/scrolla al modulo di chiamata di quel servizio nel pannello Servizi esistente, tramite un nuovo helper condiviso `openServiceCallForm(svc, li, callButton)` (stessa funzione già usata dal pulsante "Chiama" manuale, così i due percorsi si comportano in modo identico). Tutti gli id degli elementi preesistenti (`search-form`, `search-input`, `content`) sono stati preservati, solo spostati. Verificato visivamente ed end-to-end con Playwright (viewport 390×844) contro un `NomadNode` reale con tre servizi registrati: schermata di setup invariata, dashboard con i tre collegamenti rapidi e le icone corrette, tocco su un collegamento che apre il modulo di chiamata giusto, ricerca contenuti ancora funzionante, resa corretta sia in tema chiaro che scuro.

    La revisione (`code-review`, dedicata a entrambi i pezzi) ha trovato 5 problemi reali, tutti corretti:
    - **`publishedById` (mappa headline id → contentId) e, di riflesso, `node.contentStore` erano genuinamente illimitati** *(il più serio, corretto per la parte di competenza di questa slice)* — a differenza di `RemoteCatalog`/`PeerDirectory`/`ServiceDirectory`/`RoutingTable`, che passano tutte un `maxSize` a `BoundedFifoMap`, `publishedById` era una `Map` semplice; con `startAutoSync()` — il primo scheduler automatico/incustodito mai introdotto in un gateway di questo repository — un backend `--news-url` malevolo o malfunzionante che restituisse un elenco di id sempre diversi ad ogni tick avrebbe fatto crescere `publishedById` all'infinito. Corretto trasformandola in una `BoundedFifoMap<string, string>` con lo stesso default (`4096`) usato da `RemoteCatalog`/`PeerDirectory` (`node/src/catalog.ts`). **Nota**: lo stesso pattern di `Map` semplice esiste anche in `KiwixGateway.publishedByPath`, e `ContentStore` stesso (`node/src/content.ts`) non ha *alcun* limite di dimensione pur essendo alimentato dalla rete via `putVerified()` — entrambi pre-esistenti a questa slice, non introdotti da essa, e **non corretti qui**: la portata di una modifica a `ContentStore` (struttura centrale usata ovunque, sia dal percorso locale che da quello di rete) è nettamente più ampia di una mappa di bookkeeping locale a un gateway, e merita una propria slice dedicata con la stessa profondità di revisione — candidato aperto per un prossimo passo, vedi `docs/next-steps.md`.
    - **`startAutoSync()` non aveva alcuna protezione contro risposte fuori ordine**: se un tick lento (richiesta iniziata prima, risposta arrivata dopo) si sovrapponeva a un tick successivo più veloce, la risposta lenta e superata poteva sovrascrivere `cachedHeadlines`/`publishedById` con dati più vecchi *dopo* che quelli più recenti erano già stati applicati — `service://news` avrebbe risposto con dati stantii fino al tick successivo. Corretto con un contatore `syncGeneration` incrementato a ogni `syncNews()`: una chiamata il cui numero di generazione non è più quello corrente al momento di applicare il risultato viene scartata silenziosamente (ritorna `[]`), stesso principio già usato da `contentRequestId`/`refreshCycleId` nel `app.js` mobile per lo stesso tipo di corsa.
    - **`syncNews()` si fidava ciecamente della forma della risposta JSON del backend** (`(await res.json()) as NewsHeadline[]`, un semplice cast, mai un controllo a runtime), in contraddizione con la convenzione del progetto di validare difensivamente ogni dato proveniente dalla rete. Un corpo non-array avrebbe fatto lanciare un `TypeError` dentro il `for...of`; un array con una sola entry malformata in mezzo a entry valide avrebbe lasciato `publishedById` parzialmente aggiornato (le entry buone già processate prima di quella cattiva) nonostante l'intero sync fosse fallito, causando un falso "non è nuovo" per quella stessa notizia in un sync futuro completamente riuscito. Corretto validando l'intero corpo (forma array + ogni campo di ogni entry) *prima* di processare qualunque entry — o l'intera risposta è valida e viene applicata, o nessuna parte di essa lo è.
    - **Il placeholder della barra di ricerca prometteva più di quanto la ricerca facesse**: cambiato in "Cerca contenuti o servizi..." durante il redesign, ma `/api/search` (`web-ui.ts`) filtra solo i contenuti — non esiste alcun percorso di ricerca per i servizi. Un utente che cercasse "news" aspettandosi di trovare `service://news` avrebbe ottenuto zero risultati senza spiegazione. Corretto ripristinando un placeholder accurato ("Cerca contenuti...").
    - **CSS morto**: `.quick-link.is-unavailable` era definita in `styles.css` ma mai applicata — `renderQuickLinks()` filtra già a monte solo i servizi disponibili (`svc.availability`), quindi un servizio non disponibile non genera mai un collegamento rapido a cui applicare quella classe. Rimossa.

    Vedi `tests/integration/news-gateway.test.ts` (11 test: i 7 originari più 4 nuovi di regressione — corpo non-array, entry malformata in mezzo ad entry valide, risposta lenta superata da una più veloce, e `publishedById` che resta effettivamente limitato dopo aver sincronizzato 5000 id distinti contro un tetto di 4096). 325 test totali, tutti verdi, girati ripetutamente; typecheck e build puliti.

28. **Passata di design della UI mobile** (`mobile/www/`, su richiesta esplicita dell'utente — primo dei tre punti aperti elencati in `docs/next-steps.md`/`CLAUDE.md` "Priorità per chi riprende questo lavoro", scelto per primo): fino a questa voce l'app aveva ricevuto solo iterazioni funzionali (pairing, dashboard, QR, home "motore di ricerca" — voce #27), mai una vera passata di design. Nessun cambio al protocollo/rete o agli endpoint di `web-ui.ts`: solo `mobile/www/index.html`/`styles.css`/`app.js`, sugli otto punti elencati nel debito di design.

    **Icone coerenti**: sostituite le emoji (`SERVICE_ICONS`, checkmark testuali, ecc.) con uno sprite di ~20 icone SVG scritte a mano (stroke-based, `viewBox` 24×24 coerente), definito una sola volta in `index.html` e referenziato con `<svg class="icon"><use href="#icon-nome"></use></svg>` — stessa disciplina "niente dipendenze esterne non necessarie" di `node/src/qrcode.ts`. `iconEl()` (`app.js`) costruisce l'elemento via `document.createElementNS`; `name` è sempre un letterale hardcoded ai call site, mai dato di rete, quindi non introduce alcuna superficie XSS nuova rispetto alla disciplina esistente (ogni valore che arriva dalla rete resta solo `textContent`).

    **Gerarchia visiva**: intestazioni di sezione con icona + contatore (`Vicini connessi 3`), badge/pill con icona oltre al testo (`disponibile`/`in cache`/`remoto`), ombre leggere sulle card (`--shadow-sm`/`--shadow-md`, diverse per tema chiaro/scuro), `title` sul testo troncato (nome servizio/contenuto/vicino) per il tooltip nativo al passaggio del mouse.

    **Transizioni**: dissolvenza/slide tra le schermate (`screen-in`), e un rendering "diff-based" per le liste di vicini/servizi/contenuti — un `Set` degli id visti nell'ultimo render (`peerSeenIds`/`serviceSeenIds`/`contentSeenIds`) fa sì che solo le righe genuinamente nuove tra un refresh e l'altro (ogni 5s) ricevano l'animazione di ingresso, non l'intera lista ogni volta. Rispettato `prefers-reduced-motion: reduce` a livello CSS (collassa tutte le durate di animazione/transizione).

    **Stati di caricamento**: skeleton (barre shimmer) per statistiche/vicini/servizi/contenuti tra `showDashboard()` e il primo `refreshAll()` andato a buon fine (flag `firstLoadDone`, mai più mostrati dopo, anche se un refresh successivo è lento — i dati già mostrati restano in vista con solo il banner d'errore sovrapposto); spinner nel pulsante "Connetti" durante la validazione della password; mini-spinner nella barra di ricerca durante una query in corso.

    **Feedback tattile**: `navigator.vibrate()` best-effort (mai lancia — alcuni embedder lo espongono ma rifiutano la chiamata) su connessione riuscita, invio di una chiamata a un servizio (esito/errore con pattern diversi) e tocco di un collegamento rapido; scala/opacità su `:active` per bottoni, link e collegamenti rapidi; toast "Connessione ristabilita" quando il banner d'errore del gateway si chiude dopo essere stato mostrato.

    **Layout tablet**: breakpoint a 700px — i tre pannelli (vicini/servizi/contenuti) passano da colonna singola a riga di tre colonne fianco a fianco, la card di statistiche a 4 colonne, larghezza massima della dashboard aumentata.

    **Accessibilità**: `role="alert"`/`aria-live` sui banner d'errore, `role="status"`/`aria-live="polite"` su toast e risultato di una chiamata a servizio, landmark `<header>`/`<main>`/`<nav>`, skip-link visibile al focus da tastiera, focus programmatico su `#dashboard-main` al cambio schermata e sul primo campo utile all'apertura del setup, `:focus-visible` esplicito ovunque (mai rimosso l'outline senza sostituirlo), `aria-pressed`/`aria-label` dinamici sul toggle mostra/nascondi password.

    La revisione (`code-review --level high`, prompt dedicato a icone/animazioni/skeleton/toast/focus/tema) ha trovato 5 problemi reali, tutti corretti:
    - **`.error`/`.toast` restavano visibili-ma-vuoti nonostante l'attributo `hidden`** *(il più serio, trovato durante la verifica visiva con Playwright prima ancora della revisione formale — poi confermato dalla revisione non essersi ripetuto altrove per queste due classi, ma trovato ripetuto per altre due)*: `.error { display: flex; ... }`/`.toast { display: flex; ... }` non erano guardate da `:not([hidden])`, e un foglio di stile d'autore batte lo user-agent stylesheet (`[hidden] { display: none }`) a parità di specificità — esattamente lo stesso principio già documentato nel commento esistente su `#setup-screen:not([hidden])`, ma non ancora generalizzato come convenzione. Un banner d'errore o un toast nascosti via `.hidden = true` restavano quindi disegnati (vuoti, uno sfondo colorato senza testo) invece di sparire.
    - **Lo stesso pattern era sfuggito anche a `.link-button` e `.btn-spinner`/`.mini-spinner`**: `#scan-qr` (nascosto quando `BarcodeDetector` non esiste) e `#change-address` (nascosto quando l'indirizzo è già noto) usano solo la classe `.link-button`, che impostava `display: inline-flex` senza guardia — su un browser senza `BarcodeDetector` (es. desktop Chrome/Firefox, iOS Safari) il bottone "Scansiona QR" sarebbe rimasto visibile e cliccabile, aprendo la fotocamera per poi fallire subito su `new BarcodeDetector(...)`, lasciando il flusso della camera aperto indefinitamente (l'unico `.catch()` a valle è vuoto). Stessa correzione (`:not([hidden])`) applicata a entrambe le coppie di classi.
    - **Gli spinner non avevano larghezza/altezza effettive**: `.btn-spinner`/`.mini-spinner` sono `<span>` (inline di default) senza `display` impostato — `width`/`height` non si applicano a un box inline non sostituito, quindi lo spinner di caricamento che la modifica introduceva non veniva mai disegnato. Corretto impostando `display: inline-block` (guardato da `:not([hidden])`, stesso motivo sopra) — verificato visivamente con un ritardo di rete artificiale che entrambi gli spinner ora disegnano il cerchio rotante atteso.
    - **Il focus programmatico sul cambio schermata era un no-op**: `app.js` chiama `document.getElementById("dashboard-main").focus(...)` per annunciare il cambio schermata a chi usa uno screen reader, ma `<main id="dashboard-main">` non aveva `tabindex` — un elemento non interattivo senza `tabindex` non può ricevere focus programmatico. Lo stesso problema rompeva silenziosamente anche il nuovo skip-link (`href="#dashboard-main"`): saltare a un target non focalizzabile fa cadere il focus del browser su `<body>` invece che sulla destinazione prevista. Corretto spostando `tabindex="-1"` da `#dashboard-screen` (dove non serviva a nulla) a `#dashboard-main` (il vero bersaglio di entrambi).
    - **Un doppio timer nel toast poteva troncarne uno nuovo mostrato durante la dissolvenza del precedente**: `showToast()` tracciava solo il timer "inizia a sparire" (2600ms) in una variabile, non quello interno "nascondi per davvero" (220ms dopo) — un secondo toast mostrato durante quella finestra di 220ms veniva `clearTimeout`-ato solo nel primo timer, lasciando il secondo (quello interno, non tracciato) libero di nascondere il nuovo toast prima del tempo. Corretto tracciando entrambi i timer separatamente e cancellandoli entrambi a ogni nuova chiamata.

    Un sesto punto sollevato dalla revisione (`novalidate` sul form di setup senza alcun consumatore reale dell'`aria-invalid` per cui la nuova regola CSS esisteva) è stato risolto rimuovendo `novalidate` — ripristina la validazione nativa del browser sui campi obbligatori, comportamento invariato rispetto a prima di questa voce, invece di introdurre una gestione `aria-invalid` mai effettivamente cablata in `app.js`.

    Verificato end-to-end con Playwright (viewport 390×844 e 820×1180, tema chiaro e scuro) contro un `NomadNode` reale con un vicino connesso, due servizi registrati e due contenuti pubblicati: setup, dashboard popolata, ricerca, apertura del modulo di chiamata da un collegamento rapido, banner d'errore simulato (route intercettate) con pulsante "Riprova" funzionante e toast di riconnessione, tutto verificato sia prima sia dopo le correzioni della revisione, nessuna regressione. 325 test della suite esistente (invariata da questa voce, che tocca solo `mobile/www/`) rieseguiti e verdi; typecheck e build di `node/` puliti.

29. **Servizi spostati in cima alla dashboard, collegamenti rapidi rimossi** (`mobile/www/`, follow-up immediato alla voce #28 su richiesta esplicita dell'utente: "i due bottoni link rapidi ai servizi sono troppo piccolo, [...] meglio spostare la sezione 'servizi' immediatamente sotto la barra di ricerca e rimuovere i bottoni rapidi [...] un utente comune deve vedere subito cosa può fare, non una serie di dati poco chiari"): la riga di collegamenti rapidi sotto la ricerca (`#quick-links`/`.quick-link`, introdotta nella voce #27) duplicava già il pannello "Servizi" più sotto — la stessa informazione due volte, la prima volta compressa in bottoni da 4.6em troppo piccoli per essere chiari. Rimossa interamente (`renderQuickLinks()`, `#quick-links`, `.quick-link`/`.quick-link-icon`, `MAX_QUICK_LINKS`, `shortServiceLabel()`), e il pannello Servizi spostato dal fondo di `panels-grid` (terzo pannello dopo Vicini/Contenuti) a subito sotto la barra di ricerca — il primo pannello che un utente vede, prima ancora delle statistiche del nodo.

    Non solo riposizionato: ristrutturato da righe di dati compatte (`service://ai` in monospace + pill + tag) a **card**, una per servizio — icona in un cerchio colorato, nome leggibile (`SERVICE_LABELS`: "Assistente AI", "Enciclopedia", "Notizie"; fallback per servizi sconosciuti derivato dal serviceId), il `serviceId` grezzo retrocesso a sottotitolo piccolo/muto (resta visibile per chi lo cerca, ma non è più l'elemento principale), tag delle capacità, e un bottone "Chiama" a piena larghezza in fondo alla card — coerente con la richiesta di rendere subito ovvio "cosa puoi fare" invece di una lista tecnica. Il pannello Vicini/Contenuti (`panels-grid`) ora ne ha due invece di tre; a schermo largo (≥700px) restano affiancati in due colonne invece di tre.

    La revisione (`code-review`, livello medio) ha trovato 2 problemi reali, entrambi corretti prima di considerare la modifica conclusa:
    - **La prima card perdeva bordo e padding superiori**: la regola generica preesistente `.panel li:first-child { border-top: none; padding-top: 0 }` (specificità 0,2,1 — una classe + uno pseudo-classe + un elemento) batteva la nuova `.service-cards .service-card` (0,2,0 — due classi), esattamente lo stesso tipo di conflitto di specificità già affrontato nella voce #28 ma non ancora generalizzato come controllo sistematico. La prima card di ogni riga risultava visibilmente diversa dalle altre (icona attaccata al bordo superiore, nessun bordo in cima). Corretto con `.service-cards .service-card:first-child` alla stessa specificità della regola base, che ripristina esplicitamente bordo e padding.
    - **Il modulo di chiamata si apriva dentro una card larga solo 13em**: `openServiceCallForm()` appende il form (`buildCallForm()`) dentro la `<li class="service-card">` stessa, ma nulla allargava la card una volta aperto il form — la textarea per la domanda AI o il payload JSON restava compressa in ~180px di larghezza utile, una regressione reale rispetto alla riga a piena larghezza di prima. Corretto con una nuova classe `.is-open` (aggiunta da `openServiceCallForm()` alla card che si apre) e una regola `.service-cards .service-card.is-open { flex-basis: 100%; max-width: 100% }` che fa crescere solo quella card alla larghezza dell'intera riga, lasciando le altre al loro formato compatto.

    Verificato end-to-end con Playwright (390×844 e 820×1180, chiaro/scuro) contro lo stesso `NomadNode` popolato della voce #28, prima e dopo le correzioni: card uniformi (incluso il primo elemento), apertura del modulo di chiamata a piena larghezza su entrambi i viewport, stato vuoto ("Nessun servizio conosciuto.") ancora leggibile a piena larghezza nella nuova posizione in cima. 325 test della suite esistente (invariata, tocca solo `mobile/www/`) rieseguiti e verdi; typecheck e build di `node/` puliti.

30. **Icone tagliate, uniformità dei tile dei servizi, sezioni tecniche collassabili** (`mobile/www/`, terzo giro sulla stessa passata di design — voci #28/#29 — su feedback esplicito dell'utente dopo aver visto gli screenshot del giro precedente: "le icone sono tagliate", "i tiles devono essere tutti della stessa dimensione", "rendi espandibili/collassabili le sezioni più tecniche"): tre cambi.

    **Bug reale trovato e corretto — icone tagliate**: nessuno degli `<svg class="icon">` (né quelli statici in `index.html` né quelli generati da `iconEl()` in `app.js`) aveva l'attributo `viewBox="0 0 24 24"`. Senza `viewBox`, un `<use>` che referenzia un `<g>`/path disegnato in coordinate 0-24 viene renderizzato **senza scaling**: il contenuto da 24 unità veniva quindi tagliato dal riquadro più piccolo (`.icon { width: 1.2em; height: 1.2em }`, ≈19px) invece di adattarvisi — esattamente l'effetto "icone tagliate" segnalato. Il bug esisteva da quando lo sprite è stato introdotto (voce #28) ma non era emerso dagli screenshot precedenti abbastanza chiaramente da essere notato prima di questo giro. Corretto aggiungendo `viewBox="0 0 24 24"` a ogni istanza, statica (`sed` su `index.html`) e dinamica (`iconEl()`).

    **Tile dei servizi non uniformi**: le card introdotte nella voce #28 (spostate in cima nella #29) usavano flexbox (`flex: 1 1 9em; max-width: 13em`), che non garantisce larghezze uniformi tra le righe quando il contenuto varia, e quando si apriva il modulo di chiamata la card cresceva a piena larghezza (fix della voce #29) rompendo l'uniformità delle altre. Sostituito con **CSS Grid** (`grid-template-columns: repeat(auto-fill, minmax(9.5em, 1fr))`): ogni colonna di un `repeat()` è forzata alla stessa larghezza indipendentemente dal contenuto della singola card, a differenza dell'aritmetica flex-basis/grow — questo è ciò che rende ogni tile genuinamente della stessa dimensione. Aggiunta anche una `min-height` condivisa e il bottone "Chiama"/pill ancorato al fondo via `margin-top: auto` (dentro il layout a colonna flex interno alla card), così l'altezza resta uniforme anche quando nome/tag variano leggermente in lunghezza. Il modulo di chiamata è stato spostato **interamente fuori dalla card**, in un nuovo pannello condiviso `#service-call-panel` sotto la griglia (mai più dentro la card, che quindi non deve mai più crescere) — la card "attiva" riceve solo un bordo evidenziato (`.is-open`), non un cambio di dimensione. Conseguenza collaterale positiva: `renderServices()` si è semplificata, perdendo la logica di "riuso del nodo `<li>` aperto" tra un refresh e l'altro (non più necessaria dato che il form non vive più dentro la card).

    **Sezioni tecniche collassabili**: "Vicini connessi" e "Contenuti" (id tecnici, trust level, mime type/byte size — dati meno immediati di "Servizi") convertite da `<section class="panel">` a `<details class="panel">` con `<summary>` nativo, collassate di default, icona chevron che ruota all'apertura. Nessuno stato JS da gestire: tastiera (Invio/Spazio) e screen reader già supportati nativamente dal browser (`aria-expanded` implicito su `<summary>`). Una ricerca contenuti non vuota apre automaticamente "Contenuti" (altrimenti i risultati resterebbero nascosti dietro un toggle che l'utente non ha ancora motivo di conoscere), ma solo alla **prima** digitazione di una query — non a ogni refresh periodico a 5s con la stessa query invariata, altrimenti riaprirebbe la sezione anche dopo che l'utente l'ha ricollassata manualmente mentre la ricerca è ancora attiva (bug trovato dalla revisione, vedi sotto).

    La revisione (`code-review`, livello medio) ha trovato 2 problemi reali, entrambi corretti:
    - **Il refresh periodico riapriva "Contenuti" a ogni ciclo**: la prima versione del fix sopra impostava `content-panel.open = true` ogni volta che la query di ricerca non era vuota, senza distinguere "la query è appena cambiata" da "è la stessa query del ciclo precedente" — il refresh a 5s chiama comunque `refreshContent()` con la stessa query invariata, quindi riapriva la sezione ogni volta, annullando silenziosamente una chiusura manuale dell'utente entro 5 secondi. Corretto confrontando la query con `lastContentQuery` (già tracciata per un altro scopo, la logica di animazione "solo elementi nuovi") *prima* di aggiornarla, e forzando l'apertura solo quando la query è genuinamente nuova.
    - **Una card restava "attiva" e con un modulo ancora inviabile dopo che il servizio diventava non disponibile**: `renderServices()` chiudeva il pannello di chiamata condiviso solo quando il servizio spariva del tutto dall'elenco, non quando restava presente ma con `availability: false` (es. il provider si disconnette) — la card mostrava contemporaneamente l'evidenziazione `.is-open` *e* la pill "non disponibile", con il modulo ancora aperto e apparentemente inviabile per un servizio che il server avrebbe ormai rifiutato. Corretto includendo `svc.availability` nella condizione di chiusura automatica. Verificato end-to-end con un nodo demo che disattiva `service://ai` a tempo (7s dopo l'avvio) mentre il modulo è aperto: il pannello si chiude e l'evidenziazione sparisce entro il primo refresh successivo.

    Verificato end-to-end con Playwright (390×844 e 820×1180, chiaro/scuro) contro lo stesso `NomadNode` popolato delle voci precedenti: icone nitide ovunque (sprite di sezione, pill, card, chevron), tile uniformi anche con nomi/id di lunghezza diversa, apertura/chiusura di "Vicini connessi"/"Contenuti" via mouse e da tastiera (focus + Invio), ricerca che apre "Contenuti" solo alla prima query e non lo forza più a riaprirsi dopo una chiusura manuale, chiusura automatica del pannello di chiamata quando il servizio diventa indisponibile — prima e dopo le correzioni della revisione. 325 test della suite esistente (invariata, tocca solo `mobile/www/`) rieseguiti e verdi; typecheck e build di `node/` puliti.

31. **Identità visiva "Waypoint" — brand book + applicazione a `mobile/www/`** (su richiesta esplicita dell'utente, con un brief di direzione creativa completo: logo, palette, tipografia, redesign concettuale delle schermate — non un'altra iterazione estetica sulle voci #28-30, ma un rifacimento del linguaggio visivo da zero): due parti.

    **Il brand book** (`Nomad-Net Field Guide`, Artifact pubblicato — non nel repository, riferimento vivo per lavoro futuro): un concept originale, il "Waypoint" — un marchio che si legge insieme come nodo di rete, stella di orientamento e segnale, disegnato asimmetrico (mai un fiocco di neve simmetrico), costruito attorno a una metafora coerente da carta topografica/escursionistica (connessione→sentiero, nodo→punto d'appoggio, store-and-forward→quaderno di vetta) applicata sistematicamente a palette, tipografia (Overpass/Fraunces/Overpass Mono), iconografia, componenti (l'angolo tagliato come firma strutturale al posto del solito `border-radius`) e mockup concettuali delle 7 schermate richieste (le 4 esistenti nell'app più Mappa della rete/Messaggi/Modalità emergenza come direzioni non ancora implementate). Contiene un momento generativo in hero (canvas: punti sparsi che si trovano e formano il marchio) e un'intera sezione di verifica del contrasto colore (WCAG AA) fatta a mano con calcolo effettivo del rapporto, non a occhio.

    Sul primo giro, la revisione ha trovato 2 problemi reali, entrambi corretti prima di considerare il documento pronto per la revisione dell'utente: le 7 plate delle schermate erano invisibili in uno screenshot a pagina intera perché dipendevano da un reveal-on-scroll (IntersectionObserver) che non si attivava in modo affidabile durante una cattura non incrementale — rimosso, contenuto essenziale non può dipendere da un'animazione; e diversi accoppiamenti testo/sfondo delle pillole di stato erano sotto la soglia WCAG AA — corretti con token `--*-text` dedicati (distinti dai token "identità" usati per swatch/icone, che restano più vividi perché per elementi grafici basta 3:1).

    **Su feedback esplicito dell'utente dopo aver visto il documento — "mi convince, procedi ma usa meno arancione (troppo Claude-like)"** — la palette è stata rivista due volte prima di essere applicata al codice: il colore d'azione ("beacon") è stato spostato verso un ottone/oro più desaturato (`#C9A227`, tonalità ~46° contro il ~15-20° di un arancio-corallo) e soprattutto **usato con parsimonia**, non come tinta diffusa — principio applicato rigorosamente: eyebrow, numeri di sezione, il quadrante segnale e lo stato attivo della rail sono passati da beacon a un neutro/blu segnale, lasciando il beacon solo dove c'è davvero un'unica azione in corso (il bottone primario, la card/pannello aperti in quel momento).

    **Applicazione concreta a `mobile/www/`** (Setup, Dashboard, Servizi, Vicini, Contenuti — le uniche schermate reali, coerentemente con la portata concordata: niente nuove funzionalità, solo il nuovo linguaggio visivo sulle schermate esistenti):
    - **Palette**: tutti i token colore sostituiti con la stessa palette topografica del brand book; introdotto un nuovo token `--signal` (blu, mai esistito prima in questo file) proprio per rendere possibile la stessa disciplina "beacon con parsimonia" — ogni uso precedente di `--accent` è stato riassegnato singolarmente: resta beacon solo il bottone "Connetti", il pannello di chiamata aperto e il bordo della card attiva; diventano signal il focus ring globale, i link, l'icona di connessione in header, le icone delle card servizio, i bottoni "Chiama" non ancora aperti, lo spinner.
    - **Marchio**: nuova icona SVG `#icon-waypoint` (monocromatica — stroke+dot pieno, coerente con la disciplina "un solo currentColor" di ogni altra icona dello sprite, a differenza del marchio a due colori del brand book) sostituisce la vecchia icona a tre nodi nella schermata di setup; wordmark "NOMAD·NET" con il separatore centrale colorato beacon, che richiama il nodo centrale del marchio.
    - **Angolo tagliato**: `clip-path` al posto di `border-radius` su pannelli, card servizio, pannello di chiamata e i due bottoni primari — la firma strutturale del sistema.
    - **Quadrante "condizione del sentiero"**: la griglia di 4 tile numeriche (Vicini/Servizi attivi/Cache/Relay) sostituita con un'unica lettura radiale — un anello SVG generato in `app.js` (`gaugeReading()`/`buildGaugeRing()`) che deriva una categoria onesta (Isolato/Debole/Solido) **solo** dal numero di vicini reali, mai una percentuale di segnale inventata (questo prototipo non misura latenza/perdita, spec §22) — con tutti i numeri reali (vicini, servizi, cache, relay) mantenuti come riga di dettaglio testuale sotto l'anello, nulla è andato perso rispetto alle 4 tile precedenti.

    La revisione (`code-review --level high`) ha trovato 4 problemi reali su questo secondo giro, tutti corretti prima di considerare la voce conclusa:
    - **Il focus da tastiera spariva su entrambi i bottoni primari**: `clip-path` ritaglia l'intero box dipinto, contorno di focus incluso — l'anello generico (`outline-offset` positivo, fuori dal bordo) cadeva proprio nell'area che il `clip-path` del bottone aveva già tagliato via vicino all'angolo, quindi diventava invisibile (violazione WCAG 2.4.7) pur essendo definito correttamente nel CSS. Corretto disegnando il contorno **verso l'interno** (`outline-offset: -0.65em`, che supera il taglio di 0.6em/0.5em di entrambi i bottoni) solo su questi due elementi — verificato con una vera navigazione da tastiera (Tab) e lettura degli stili calcolati, non solo a schermo.
    - **Due combinazioni testo/sfondo col nuovo `--signal` sotto la soglia WCAG AA**: il colore dei link (`.link-button`, su `--bg`) e il testo dei bottoni "Chiama" non ancora aperti (su `--signal-soft`) risultavano rispettivamente 4.35:1 e 4.31:1 in tema chiaro — sotto 4.5:1, mentre le stesse coppie in tema scuro passavano già. Corretto scurendo `--signal` chiaro (`#3E6E8E` → `#35607C`), portando entrambe le coppie sopra 5.3:1.
    - **CSS morto**: una regola del breakpoint tablet (`.stats { grid-template-columns: repeat(4, 1fr) }`) era rimasta dopo che la griglia a 4 tile era stata sostituita dal quadrante — l'elemento non porta più quella classe, la regola non si applicava più a nulla. Rimossa.
    - **Un commento in contraddizione con un altro**: il commento CSS sul contenitore neutro dell'icona di setup affermava che il marchio al suo interno fosse "già multicolore", contraddicendo il commento HTML adiacente che descrive la stessa icona come deliberatamente monocromatica. Corretto allineando la spiegazione alla scelta reale.

    Verificato end-to-end con Playwright (390×844 e 820×1180, chiaro/scuro) contro lo stesso `NomadNode` popolato delle voci precedenti, prima e dopo le correzioni della revisione, incluso un controllo dedicato del contorno di focus da tastiera sui due bottoni con `clip-path`. 325 test della suite esistente (invariata, tocca solo `mobile/www/`) rieseguiti e verdi; typecheck e build di `node/` puliti.

32. **`ContentStore` senza limite di dimensione** (candidato aperto isolato dalla revisione della voce #27 e documentato in `docs/next-steps.md`/`CLAUDE.md` come uno dei due punti aperti dopo la parte estetica — su richiesta esplicita dell'utente "passiamo agli altri 2 punti aperti"): a differenza di ogni altra struttura dati alimentata dalla rete (`RemoteCatalog`, `PeerDirectory`, `RoutingTable`, `ServiceDirectory`, `TrustManager`, `SeenCache`, `PendingDeliveryQueue`, tutte dietro `BoundedFifoMap` con `maxSize`), `ContentStore` (`node/src/content.ts`) era ancora una `Map` semplice e illimitata, pur essendo alimentata dalla rete via `putVerified()` (contenuto ricevuto/relayato da peer non fidati, dopo verifica di firma) — un vero divario rispetto alla convenzione del progetto (spec §57), non solo teorico: un peer poteva far crescere la cache di un nodo senza limite semplicemente inviando contenuto genuinamente firmato (economico da produrre: basta una nuova chiave Ed25519 usa-e-getta).

    **Fix**: `ContentStore` ora usa `BoundedFifoMap<string, StoredContent>` con una nuova `ContentStoreOptions { maxSize?, trustRank? }`, stesso schema di `RemoteCatalog` (`catalog.ts`). `DEFAULT_MAX_SIZE = 256`, deliberatamente più basso del 4096 usato dalle strutture puramente metadata (qui ogni entry porta i byte veri del contenuto, non solo qualche centinaio di byte di metadata). `NomadNode` collega un `trustRank` come per `remoteCatalog`/`peerDirectory`/`routingTable`/`services`, con un'aggiunta specifica a `ContentStore`: `publisherId === this.nodeId ? OWN_CONTENT_TRUST_RANK : trustRank(this.trust.get(publisherId))`. Necessaria perché un nodo non registra mai fiducia per il proprio node id nel proprio `TrustManager` (non c'è nulla da "guadagnare" da se stessi) — senza questo caso speciale, il contenuto pubblicato localmente da `NomadNode.publishContent()` (che passa sempre per `putVerified()`, mai per il `put()` "grezzo") avrebbe lo stesso rank UNKNOWN di un'identità usa-e-getta appena generata da un attaccante, e potrebbe essere sfrattato da uno store pieno con la stessa facilità. `OWN_CONTENT_TRUST_RANK` è un valore finito (`trustRank(ADMIN) + 1`), deliberatamente non `Infinity` — vedi il bug in `bounded-map.ts` sotto.

    Nuova opzione `NomadNodeOptions.maxContentStoreEntries` (spec §57), stesso schema di `maxRemoteCatalogEntries`/`maxPeerDirectoryEntries`/ecc.

    **Bug latente trovato ragionando sul caso `OWN_CONTENT_TRUST_RANK` (non ancora sfruttato da nessun chiamante reale prima di questa voce, ma reale)**: `BoundedFifoMap.pickEvictionCandidate()` cerca il punteggio più basso partendo da `bestScore = Infinity` con un confronto `<` stretto — se un `evictionScore` ritornasse esattamente `Infinity` per *ogni* entry, nessuna avrebbe punteggio strettamente minore del sentinella iniziale, `bestKey` resterebbe `undefined`, e `set()` non sfratterebbe nulla pur inserendo comunque la nuova entry — violando silenziosamente il limite `maxSize`. Per questo `OWN_CONTENT_TRUST_RANK` usa un valore finito. Corretto comunque `pickEvictionCandidate()` con un fallback difensivo (se nessuna entry batte il sentinella, ricade sul FIFO più vecchio) per rendere la garanzia di dimensione massima incondizionata indipendentemente da cosa ritorni una `evictionScore` futura — struttura condivisa da 7+ store, vale la pena blindarla anche per chiamate non ancora scritte. Test di regressione dedicato in `tests/unit/bounded-map.test.ts`.

    Nuovi test: `tests/unit/content.test.ts` (eviction FIFO e trust-ranked diretta su `ContentStore`, con identità reali generate e firme vere — a differenza di `RemoteCatalog.record()`, `putVerified()` verifica davvero la firma, quindi il test non può usare alias di comodo come fa `catalog.test.ts`), `tests/integration/trust-aware-eviction.test.ts` (nuovo test end-to-end via `NomadNode` reale: un attaccante flooda contenuto relay-cached con identità usa-e-getta tramite `observeRelayedContent` — pacchetti `CONTENT_CHUNK`/`CONTENT_COMPLETE` indirizzati a un terzo id, non alla vittima, così passano dal percorso di cache passiva del relay senza dover orchestrare una vera `getContent()` — verificando che il contenuto pubblicato localmente dalla vittima sopravviva a un pieno store).

    **La prima revisione** (`code-review --level high`) ha trovato 2 problemi reali, entrambi in `gateway/nomad/`, non nel codice centrale: sia `KiwixGateway.syncCatalog()` che `NewsGateway.syncNews()` avevano commenti che dichiaravano esplicitamente "il contenuto pubblicato da questo gateway non viene mai sfrattato da `node.contentStore`" — un'invariante vera prima di questa voce (`ContentStore` era illimitato) e resa falsa dal nuovo limite di dimensione. Con il default di 256 entry, un gateway che sincronizza un catalogo NOMAD reale (tipicamente migliaia di articoli) o che accumula cronologia di notizie via `startAutoSync()` oltre 256 content id distinti avrebbe iniziato a perdere silenziosamente byte di contenuto ancora corrente, non solo versioni storiche superate come i commenti lasciavano intendere.

    **Fix**: `gateway/nomad/cli.ts` ora passa `maxContentStoreEntries` al `NomadNode` del gateway demo, con un default `DEFAULT_MAX_CONTENT_ENTRIES = 8192` (nettamente più alto del default generico di `content.ts`, dato che un gateway *è* il proprio catalogo) e un nuovo argomento `--max-content-entries` per dimensionarlo esplicitamente su un catalogo reale. I commenti di `kiwix-gateway.ts`/`news-gateway.ts` sono stati riscritti per non dichiarare più "mai sfrattato": spiegano ora che `ContentStore` è limitato (spec §57), che il contenuto pubblicato da un gateway è preferito nella permanenza rispetto al contenuto solo relay-cached (`OWN_CONTENT_TRUST_RANK`) ma **non** rispetto ad altro contenuto pubblicato dallo stesso nodo (eviction FIFO tra pari, storico e corrente allo stesso modo), e che un operatore deve dimensionare `maxContentStoreEntries` (o aggiungere un `ttlMs`) in base al proprio catalogo reale. Aggiornato anche `docs/development.md`.

    **La seconda revisione** (mirata solo a questo fix, `code-review --level medium`) ha trovato altri 2 problemi reali, entrambi in `cli.ts`, entrambi corretti: il parsing di `--max-content-entries` con `Number()` non validato produceva silenziosamente `NaN` per un valore mancante/non numerico (`NaN <= 0` e `NaN >= maxSize` sono sempre `false`, quindi l'eviction non scattava mai più — la stessa crescita illimitata che questa voce doveva eliminare, reintrodotta dalla porta sul retro) o un valore negativo/zero passava invariato a `BoundedFifoMap`, che a quel punto scarta silenziosamente ogni inserimento pur restituendo successo a `putVerified()`/`publishContent()`. Corretto con un nuovo `parsePositiveInt()` dedicato che valida intero positivo e ricade sul default con un avviso in console altrimenti — verificato manualmente con `--max-content-entries abc`/`-1`/un valore valido, oltre a `npx tsx gateway/nomad/cli.ts --port 0` senza l'opzione (comportamento invariato). La revisione ha inoltre segnalato che questa voce di `docs/security.md` non era ancora stata scritta al momento della verifica — la stessa svista di processo già capitata e corretta nella voce #26.

    Verificato: `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti; l'intera suite (329 test, 4 nuovi rispetto ai 325 precedenti) rieseguita ripetutamente, sempre verde; demo gateway (`npx tsx gateway/nomad/cli.ts --port 0`) avviato manualmente senza crash, pubblica correttamente gli articoli demo.

33. **Parser RSS/Atom scritto da zero + schema notizia arricchito** (pezzo 1 di 4 della proposta "Nomad News evoluto", `docs/next-steps.md` Opzione I — valutata nella voce precedente, ora in implementazione un pezzo alla volta con ok esplicito dell'utente tra un pezzo e il successivo; per il pezzo 4, il meccanismo di priorità/broadcast per contenuto, l'utente ha scelto in anticipo un nuovo pacchetto di annuncio firmato parallelo a `SERVICE_ANNOUNCE`, non ancora implementato): `NewsGateway` smette di parlare con l'API JSON ad-hoc mai esistita davvero (`GET /api/news`, un'invenzione di questo prototipo) e ingerisce un vero feed RSS 2.0/Atom.

    **`gateway/nomad/rss-feed.ts` (nuovo)**: parser scritto a mano, nessuna dipendenza esterna (stesso precedente di `node/src/qrcode.ts`). `parseFeed(xml): ParsedFeed | undefined` — non un parser XML generico, uno scanner di tag/attributi difensivo sul sottoinsieme di RSS 2.0/Atom che serve per un feed di notizie. Validazione atomica (un item/entry senza campi richiesti invalida l'intero feed, stesso contratto "tutto o niente" che l'API JSON precedente aveva). `NewsHeadline` (`news-gateway.ts`) arricchita con `source`/`category`/`language`/`updatedAt`.

    **Quattro giri di revisione** (`code-review`, il numero più alto finora per una singola voce), ciascuno ha trovato problemi reali:
    - **Giro 1** (`--level high`): 6 problemi. Il confine di un item/entry (`</item>`/`</entry>`) poteva essere troncato prematuramente da un testo simile presente dentro il contenuto CDATA di un item precedente; `String.fromCodePoint()` lanciava `RangeError` su un riferimento numerico di carattere fuori range, rompendo il contratto "non lancia mai" di `parseFeed()`; il titolo del feed (`ParsedFeed.source`) veniva estratto senza scoping e prendeva il primo `<title>` di un item quando il canale/feed non ne aveva uno proprio; in Atom, `<link href>` veniva preso sempre il primo trovato ignorando `rel`, potendo restituire un URL di modifica/API invece dell'articolo vero; `MAX_FEED_BYTES` veniva controllato solo dopo che `res.text()` aveva già bufferizzato l'intera risposta, e confrontava unità UTF-16 invece dei byte reali; `decodeText()` gestiva solo il caso "l'intero valore è un unico blocco CDATA", perdendo testo semplice quando mescolato a CDATA nello stesso campo.
    - **Giro 2** (`--level medium`, mirato al fix del giro 1): 3 problemi. Il fix del giro 1 aveva reso CDATA-aware solo la ricerca del tag di *chiusura*, non quella di *apertura* — un `<item>`/`<title>`-shaped substring dentro un CDATA precedente poteva ancora corrompere confini e campi, stessa classe di bug del giro 1 sull'altro lato; `decodeEntities()` faceva 5 `.replace()` sequenziali, causando un doppio-decode quando un riferimento numerico decodificato formava per concatenazione un nuovo pattern intercettato da un passaggio successivo (`&#38;amp;` → `&` invece di `&amp;`); il ramo di fallback `!res.body` di `fetchTextBounded()` non applicava alcun limite di dimensione, riproponendo la stessa vulnerabilità appena chiusa.
    - **Giro 3** (`--level medium`, mirato al fix del giro 2): 2 problemi, il più serio di tutta la voce. La centralizzazione CDATA-aware del giro 2 era corretta ma **algoritmicamente quadratica**: un feed con molte piccole sezioni CDATA, ognuna contenente un finto tag di chiusura, faceva scansionare posizione per posizione dentro ogni sezione scartata — confermato empiricamente, un feed di 1.8MB (sotto il limite di 2MB) impiegava oltre 7 secondi contro pochi millisecondi di un documento di controllo della stessa dimensione, un vero DoS a consumo di CPU dato che `--news-url` è esplicitamente un backend potenzialmente ostile ri-interrogato a ogni tick di `startAutoSync()` (spec §57). Corretto sostituendo la scansione lineare degli span CDATA con una ricerca binaria (`findSpanContaining`) e facendo saltare `lastIndex` direttamente alla fine di uno span scartato invece che avanzare di un carattere per volta — elimina la riscansione posizione-per-posizione. Il secondo problema: `findClosingTag` era diventato dead code dopo la centralizzazione, corretto rendendola il punto unico condiviso da `extractTagContent`/`matchAllTagBlocks`.
    - **Giro 4** (`--level medium`, mirato al fix del giro 3): nessun problema bloccante — un solo rilievo di efficienza non urgente (calcolo ripetuto degli span CDATA una volta per campo invece che una volta per blocco, costo lineare limitato dalla dimensione dell'item, non quadratico) risolto correggendo solo il commento che sovrastimava cosa fosse già ottimizzato, senza modificare la logica.

    Test: `tests/unit/rss-feed.test.ts` (nuovo, 33 test — RSS 2.0, Atom, CDATA, entità, casi limite, e un test di regressione dedicato per ciascuno dei problemi trovati dai 4 giri, incluso un test di performance con 20.000 sezioni CDATA che verifica il parsing sotto i 2s), `tests/integration/news-gateway.test.ts` (riscritto per servire XML invece di JSON nei server di test, stessa copertura comportamentale di prima più un test per lo streaming-bound fetch). 363 test totali (suite intera) rieseguiti ripetutamente, sempre verdi; typecheck ad-hoc su `gateway/nomad/*.ts` (nessun `tsconfig.json` dedicato in quella cartella, verificato con un'invocazione diretta di `tsc` con le stesse opzioni di `tsconfig.base.json`) e `npx tsc --noEmit -p node/tsconfig.json` puliti; build di `node/` pulita; demo gateway (`npx tsx gateway/nomad/cli.ts --port 0 --news-url ...`) verificato manualmente più volte contro un server RSS locale, prima e dopo ogni giro di correzioni.

34. **`CONTENT_ANNOUNCE` — meccanismo di annuncio broadcast per contenuto singolo** (prerequisito architetturale condiviso da "Nomad News evoluto" pezzo 4, Opzione I, e dai canali pubblici di chat, Opzione J — vedi `docs/next-steps.md`, "Piano d'insieme"; sviluppato seguendo un piano d'esecuzione a slice esplicitamente pianificato e approvato con l'utente prima di iniziare, su sua richiesta di "programmare come procedere, step by step e in slice con verifica anti-regressione prima di pushare"): oggi la priorità è fissa **per tipo di pacchetto** (ogni `CONTENT_*` usa sempre `Priority.CONTENT`) e la scoperta dei contenuti è **pull-based** (`CONTENT_QUERY`/`CONTENT_FOUND`), senza un annuncio broadcast come `SERVICE_ANNOUNCE`. Questa voce chiude il divario: un nuovo `MessageType.CONTENT_ANNOUNCE` (broadcast, nessun `destination`) attraversa gratuitamente il meccanismo di forward/dedup generico già esistente (`decideForward`/`SeenCache`/`floodExcept`, lo stesso che già serve `SERVICE_ANNOUNCE`) — nessuna nuova logica di TTL/dedup/forwarding scritta.

    `NomadNode.publishContent(name, mimeType, data, options)` guadagna `options.announce?: boolean` (default `false` — nessun cambiamento per i chiamanti esistenti: `KiwixGateway`, `NewsGateway`, ogni test) e `options.priority?: Priority` (default `Priority.CONTENT`). Quando `announce` è `true`, dopo `putVerified()` origina e flooda un `CONTENT_ANNOUNCE` con quella priorità — il gancio che userà `service://emergency-news` (`Priority.EMERGENCY`) e i canali di chat (`Priority.MESSAGING`) quando quelle voci verranno implementate. Il nuovo handler `handleContentAnnounce()` (`node.ts`) porta solo metadata firmata, mai i byte — chi riceve l'annuncio recupera comunque il contenuto vero tramite il ciclo `CONTENT_QUERY`→`CONTENT_COMPLETE` già esistente.

    **Limite noto, accettato e documentato, non "risolto" da questa voce**: `packet.priority` non è autenticato più di quanto lo sia già `packet.source` (limite già noto, `CLAUDE.md`) — un peer può già dichiarare qualunque priorità su qualunque pacchetto che origina, non solo su `CONTENT_ANNOUNCE`. Il rate limiting per-peer esistente (`rate-limit.ts`, applicato in `handlePacket()` prima dello switch sui tipi) resta la difesa principale contro un flood a priorità `EMERGENCY`; nessuna nuova mitigazione introdotta qui, stesso trattamento già riservato al binding di `packet.source`.

    La revisione (`code-review`) ha trovato 2 problemi reali, entrambi corretti: un commento di documentazione rimasto "orfano" sopra la funzione sbagliata quando il nuovo handler è stato inserito tra il commento di `handleSyncResponse()` e la funzione che descriveva; e la logica di validazione (controllo forma, `verifyContentSignature()`, scadenza, `remoteCatalog.record()` + `trust.markVerified()`) duplicata verbatim tra `handleContentAnnounce()` e il loop di `handleSyncResponse()` — le due funzioni gestiscono lo stesso tipo di rivendicazione non fidata ("questo contenuto esiste, firmato da X") arrivata per due strade diverse (annuncio non richiesto vs. risposta di sync), quindi entrambe ora condividono un unico helper privato `acceptCatalogEntry()`, stesso principio "un solo posto da cambiare" già seguito per `BoundedFifoMap`. Una seconda revisione mirata al fix non ha trovato altri problemi.

    Test nuovi: `tests/integration/content-announce.test.ts` (3 test — propagazione immediata a un nodo a 2 hop già connesso prima della pubblicazione, così l'unica spiegazione possibile è l'annuncio e non il sync a tempo di connessione; nessuna trasmissione quando `announce` non è passato, il default per ogni chiamante esistente; un `CONTENT_ANNOUNCE` con firma non valida iniettato via socket grezzo viene scartato), più 3 nuovi casi in `tests/integration/malformed-packet-robustness.test.ts` (stesso pattern difensivo già usato per gli altri handler `CONTENT_*`/`SERVICE_*`). 369 test totali (suite intera) rieseguiti ripetutamente, sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` pulito.

35. **Livelli headline/summary — pezzo 2 di 4 "Nomad News evoluto"** (`docs/next-steps.md` Opzione I, subito dopo il pezzo 1 — voce #33; numerata dopo la #34 sopra perché sviluppata in parallelo da un'altra sessione su questo stesso repository e integrata via merge — vedi la nota di processo in fondo a questa voce): `NewsGateway` pubblicava titolo+riassunto come un unico blob JSON per notizia; separato in due livelli come descritto dalla proposta originale, motivato dall'efficienza su link a banda stretta (BLE, spec §46-47) — un catalog sync non deve più muovere il testo completo di ogni articolo solo per far scoprire a un nodo che la notizia esiste.

    **Cosa cambia**: `NewsHeadline` (`gateway/nomad/news-gateway.ts`) non ha più il campo `summary` — resta solo metadata piccola sempre pubblicata/sincronizzata (title/id/url/publishedAt/source/category/language/updatedAt) più un nuovo `articleContentId`, il contentId di un `NewsArticleBody` (`{ summary }`) pubblicato separatamente. `syncNews()` pubblica ora due content per ogni item del feed: prima il corpo articolo (ottenendo `articleContentId`), poi la headline (che lo incorpora). Il recupero del corpo articolo resta il normale percorso di `getContent()`/`CONTENT_REQUEST` già esistente (spec §90-92) — nessun nuovo endpoint. Dato che la headline incorpora `articleContentId`, un resync che cambia solo il testo dell'articolo (stesso titolo/id) produce comunque un nuovo contentId per la headline e viene correttamente segnalato come "changed" dal dedup esistente basato su `publishedById` — nessuna logica di change-detection separata serve per il livello articolo.

    **Nota accettata, non un bug**: dato che ogni notizia ora costa due entry di `ContentStore` invece di una, un operatore che sincronizza un catalogo/cronologia più grande deve dimensionare `maxContentStoreEntries` di conseguenza (`--max-content-entries`, voce #32) — già documentato nel commento di `syncNews()`.

    La revisione (`code-review --level high`) ha trovato 1 problema reale, non bloccante ma reale, corretto: il corpo articolo viene ripubblicato (nuova firma Ed25519 + serializzazione JSON) a ogni tick di `syncNews()` per ogni item, anche quando il testo non è cambiato — la deduplicazione via `publishedById` si applica solo al livello headline, non a quello articolo, raddoppiando il lavoro di firma su un percorso periodico (`startAutoSync()`) rispetto a prima di questo pezzo (solo la headline pagava questo costo). Non un problema di correttezza (content-addressing rende la ripubblicazione idempotente, nessuna crescita illimitata), ma un costo reale non documentato — corretto con un commento esplicito nel loop, non una ristrutturazione (la deduplicazione a monte richiederebbe un confronto separato sui byte dell'articolo, portata più ampia di quanto valga per questo pezzo).

    Test aggiornati in `tests/integration/news-gateway.test.ts`: nuovo helper `expectedHeadline()`/`articleContentIdFor()` (quest'ultimo usa `computeContentId` da `node/src/content.ts` per calcolare l'hash atteso del corpo articolo, permettendo assert strutturali esatti invece di trattare `articleContentId` come opaco); il test "publishes each headline as retrievable content" è stato sostituito con uno che verifica esplicitamente la pubblicazione a due livelli (2 content pubblicati, headline senza `summary`, corpo articolo recuperabile via `getContent(articleContentId)`); nuovo test per il caso "resync che cambia solo il corpo articolo". 364 test rieseguiti ripetutamente, sempre verdi (prima del merge con la #34 — vedi sotto per il conteggio combinato); typecheck ad-hoc su `gateway/nomad/*.ts` e `npx tsc --noEmit -p node/tsconfig.json` puliti; build di `node/` pulita; demo gateway riverificato manualmente.

    **Nota di processo — lavoro in parallelo sullo stesso repository**: questo pezzo (2 di 4, backend `NewsGateway`) e la voce #34 sopra (pezzo 4, protocollo core) sono stati sviluppati in due sessioni Claude Code diverse in parallelo sullo stesso repository, entrambe partite dallo stesso commit dopo la voce #33. Nessuna sovrapposizione di file tra le due (questa voce tocca solo `gateway/nomad/news-gateway.ts`/i suoi test; la #34 tocca solo `node/src/`/i suoi test), quindi il merge è stato pulito nel codice — l'unico conflitto reale è stato proprio in questo file (`docs/security.md`), per la numerazione duplicata delle due voci (entrambe scritte come "34." prima del merge), risolto rinumerando questa in #35. Verificato dopo il merge: l'intera suite (370 test) rieseguita ripetutamente, sempre verde; typecheck e build puliti.

36. **Chat 1:1 — cronologia messaggi + UI mobile (Slice 2a del piano approvato)** (`docs/next-steps.md` "Piano d'insieme", punto 1 — sviluppata seguendo lo stesso piano d'esecuzione a slice della voce #34; costruita **sopra** la Slice 1/`CONTENT_ANNOUNCE`, ma senza dipenderne — la messaggistica 1:1 resta unicast, instradata da `sendPrivateMessage()`/`PRIVATE_MESSAGE`, già E2E cifrata, multi-hop e delay-tolerant da prima di questa voce): la crittografia/instradamento erano già pronti, ma `handlePrivateMessage()` si limitava a decifrare ed emettere `"private-message"`, senza persistere nulla — un client mobile "thin" basato su polling HTTP (non un socket diretto alla mesh) non aveva modo di ricostruire una conversazione.

    Nuovo modulo `node/src/message-history.ts` (`MessageHistory`): cronologia locale, mai firmata né ripropagata ad altri peer (a differenza di ogni altra struttura bounded di questo codebase), bounded su due assi come da convenzione spec §57 — `maxPeers` conversazioni tracciate (`BoundedFifoMap`, con lo stesso `trustRank` di eviction pesata sulla fiducia già usato da `PeerDirectory`/`RemoteCatalog`) e `maxMessagesPerPeer` messaggi per conversazione (trim FIFO). `NomadNode` guadagna il campo `messageHistory`; un nuovo helper a livello di modulo `extractChatText()` (`node.ts`) decide se un payload `PRIVATE_MESSAGE` (tipizzato `unknown`, perché `sendPrivateMessage()` ha altri chiamanti non-chat) è forma-di-messaggio-chat (`{ text: string }`) — usato sia da `sendPrivateMessage()` (invio) sia da `handlePrivateMessage()` (ricezione) per decidere cosa registrare, così la cronologia resta esattamente "la chat", non un log grezzo di ogni pacchetto privato.

    Due nuovi endpoint in `web-ui.ts`, dietro lo stesso schema di sicurezza di `POST /api/call` (password di rete via Bearer token, mai esposti di default) — una scelta deliberata diversa da `/api/peers`/`/api/services`/`/api/content` (sempre leggibili, nessun dato sensibile): `GET /api/messages?peer=<nodeId>` e `POST /api/messages` (`{ to, text }`). `/api/peers` guadagna anche `canMessage` (`node.peerDirectory.has(peer.id)`) così la UI mobile sa quando mostrare il bottone "Messaggia" per un peer connesso (la sincronizzazione dell'identità può restare indietro di un istante rispetto alla connessione).

    UI mobile (`mobile/www/`): un pannello `#chat-panel` condiviso, stesso schema di `#service-call-panel` — una conversazione aperta alla volta, polling indipendente a 3s (separato dal ciclo dashboard a 5s, perché `/api/messages` richiede un header di auth che gli altri endpoint non hanno), reso deliberatamente come fratello *fuori* dai pannelli `<details>` collassabili così non sparisce visivamente se "Vicini connessi" viene richiuso, e senza auto-chiusura alla disconnessione del peer (a differenza di `closeServiceCallForm()`) — i messaggi privati sono delay-tolerant (store-and-forward li mette in coda), quindi una conversazione con un peer offline resta significativa.

    La revisione (`code-review`) ha trovato 3 problemi reali, tutti corretti prima di considerare la slice conclusa:
    - **Il più serio: nessun limite sulla lunghezza di un singolo messaggio sul percorso di ricezione via rete** — `MessageHistory` limitava conversazioni e messaggi-per-conversazione, ma non la lunghezza del *testo* di un singolo messaggio; solo la validazione HTTP di `POST /api/messages` applicava `MAX_MESSAGE_TEXT_LENGTH`. Un peer connesso poteva però chiamare `sendPrivateMessage()`/inviare un `PRIVATE_MESSAGE` grezzo direttamente, bypassando quel livello HTTP (fino al tetto di `MAX_LINE_BYTES` del transport, 2MB) — stessa classe di DoS per crescita di memoria già documentata per `ContentStore` (voce #32). Corretto esportando `MAX_MESSAGE_TEXT_LENGTH` da `message-history.ts` come unico limite canonico, con `extractChatText()` (il gate condiviso da invio e ricezione) che rifiuta un testo troppo lungo trattandolo come "non un messaggio chat", e `web-ui.ts` che importa la stessa costante invece di tenerne una copia locale che avrebbe potuto divergere silenziosamente. Test di regressione dedicato: `tests/integration/message-history.test.ts` (due nodi reali connessi via TCP, un messaggio oltre il limite viene comunque consegnato/decifrato correttamente ma non registrato in nessuna delle due cronologie; un messaggio esattamente al limite viene registrato in entrambe).
    - `handleSendMessage` mappava ogni eccezione di `sendPrivateMessage()` a `404`, non solo il caso "chiave di cifratura non ancora nota" — non sfruttabile oggi (quello resta l'unico punto di `throw` sincrono in `sendPrivateMessage()`, confermato leggendone il sorgente), ma fragile per un futuro secondo motivo di errore. Corretto distinguendo il messaggio d'errore specifico (`404`) da qualunque altro (`502`, stessa convenzione già usata da `handleCall()`).
    - Il controllo Bearer-auth (~4 righe) era duplicato identico tre volte (`handleCall`, `handleGetMessages`, `handleSendMessage`). Estratto in un metodo privato condiviso `WebUiServer.isAuthorized(req, expectedPassword)`.

    Una seconda revisione mirata ai tre fix non ha trovato altri problemi. Test nuovi: `tests/unit/message-history.test.ts` (8 test — ordine, isolamento per peer, eviction FIFO e pesata sulla fiducia, trim per messaggi-per-peer), `tests/integration/web-ui.test.ts` (8 nuovi test per `/api/messages` — auth, round-trip invio/ricezione, validazione, 404 su peer sconosciuto), `tests/integration/message-history.test.ts` (2 test, vedi sopra). UI verificata end-to-end con Playwright contro due gateway reali connessi (invio, ricezione sulla dashboard del destinatario, tema chiaro/scuro, apertura/chiusura pannello, svuotamento campo dopo l'invio). 390 test totali (suite intera) rieseguiti ripetutamente, sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti.

37. **Digest AI per `service://news` — pezzo 3 di 4 "Nomad News evoluto"** (`docs/next-steps.md` Opzione I, ultimo pezzo backend rimasto dopo il parser RSS #33 e i livelli headline/summary #35 — chiude la parte backend della proposta, resta solo `service://emergency-news` costruito sopra `CONTENT_ANNOUNCE` #34): nuovo `NewsGateway.generateDigest()` (`gateway/nomad/news-gateway.ts`) compone `NewsGateway` con `service://ai` (`AiGateway`/`FakeOllamaServer` in questo codebase, un vero Ollama dietro Project NOMAD in produzione) per produrre un breve riassunto delle headline in cache — "digest generati da un'IA locale" della proposta originale. Accoppiamento deliberatamente debole: il metodo non importa mai `AiGateway` né sa quale nodo risponde, chiama `service://ai` tramite `node.callService()` esattamente come qualunque altro chiamante nel codebase (lo stesso pattern "un nodo debole chiama service://ai senza sapere chi risponde" già descritto dal commento di `ai-gateway.ts`); un nodo che ha anche `AiGateway` registrato localmente (come la demo `cli.ts`) risponde alla propria chiamata senza un giro di rete (`callService()`'s local-provider shortcut). Il digest generato è pubblicato come proprio contenuto (`text/plain`, via `publishContent()`) — indirizzabile per content id, non per il path stabile `content://news/digest/latest` descritto dalla proposta (vedi la nota architetturale in `docs/next-steps.md`) — e riportato nella risposta di `service://news` (`digest`/`digestContentId`, assenti finché nessuna generazione non è ancora riuscita). Nuovi `startDigestAutoRefresh()`/`stopDigestAutoRefresh()`, timer indipendente da `startAutoSync()` (una generazione digest è una chiamata IA dal vivo, un operatore può ragionevolmente volerla meno frequente del poll del feed) — `cli.ts` collega entrambi nella demo con intervalli separati (`NEWS_SYNC_INTERVAL_MS` 5 min, `NEWS_DIGEST_INTERVAL_MS` 15 min).

    La revisione (`code-review`, due giri) ha trovato 7 problemi reali, tutti corretti prima di considerare il pezzo concluso. Primo giro (5 problemi):
    - **`generateDigest()` senza guardia anti-superamento** — a differenza di `syncGeneration` in `syncNews()`, una risposta lenta di `service://ai` arrivata dopo una più recente poteva sovrascrivere un digest più fresco già commesso (stesso "le risposte non arrivano garantite in ordine di richiesta" già risolto per `syncNews()`). Corretto con un nuovo `digestGeneration`, stesso pattern esatto: incrementato all'inizio della chiamata, controllato dopo l'await e prima di `publishContent()`/`cachedDigest`, scarta il risultato (ritorna `undefined`) se superato.
    - **Accesso non difensivo al risultato di `callService()`** — `result.response` letto con un cast diretto senza controllare che `result` fosse un oggetto; un provider malevolo/malfunzionante di `service://ai` che risponde con `result` assente/`null` avrebbe fatto crashare con un `TypeError` grezzo invece del messaggio d'errore pulito previsto — violazione della convenzione "mai fidarsi della forma di un valore proveniente dalla rete" (`CLAUDE.md`). Corretto con un controllo esplicito (`result && typeof result === "object" ? ... : undefined`) prima di leggere `.response`.
    - **`MAX_DIGEST_HEADLINES` limitava solo il numero di headline nel prompt, non la lunghezza di ciascun titolo** — un feed altrimenti valido (nessun limite di lunghezza titolo in `rss-feed.ts`) poteva comunque rendere ciascuno dei 20 titoli ammessi enorme, con il prompt risultante inviato come payload di un `SERVICE_REQUEST` (nessun limite di dimensione a livello di pacchetto oltre al tetto di 2MB per riga TCP, bug #9). **Un titolo contenente un `\n` poteva inoltre "rompere" il formato a elenco puntato del prompt, iniettando quella che sembra una riga/istruzione aggiuntiva** — un caso concreto e limitato di prompt injection strutturale (non una difesa generale contro il prompt injection, fuori portata per questa feature). Corretto con una nuova `sanitizeTitleForPrompt()` (collassa `\r`/`\n` in uno spazio, tronca a `MAX_DIGEST_TITLE_CHARS` = 200), usata da `buildDigestPrompt()` per ogni titolo. Test di regressione dedicati per entrambi i casi (titolo con newline, titolo eccessivamente lungo).
    - **Test del prompt fragile** — verificava il contenuto del prompt inviato a `service://ai` con un monkey-patch di `globalThis.fetch` a livello di processo. Corretto aggiungendo una capacità di registrazione diretta a `FakeOllamaServer` (`gateway/nomad/fake-ollama-server.ts`, nuovi getter `prompts`/`lastPrompt`, una copia difensiva dell'array interno) — riusabile anche da `ai-gateway.test.ts` in futuro.

    Secondo giro, mirato ai 5 fix sopra (nessun problema sui fix stessi, ma 2 problemi reali trovati nel resto del diff, entro lo stesso ambito):
    - **`startDigestAutoRefresh()` non veniva mai avviato se il primo tentativo di `generateDigest()` in `cli.ts` falliva** — un backend AI momentaneamente irraggiungibile all'avvio lasciava `service://news` senza digest, e senza alcuna possibilità di ottenerne uno in futuro nella vita del processo, anche dopo che il backend fosse tornato disponibile — in contraddizione con la postura "tollera un upstream instabile" che il commento adiacente dichiarava (e che `startAutoSync()` rispetta davvero, dato che il suo timer di retry parte incondizionatamente). Corretto avviando `startDigestAutoRefresh()` incondizionatamente, fuori dal blocco `try` del primo tentativo.
    - **Test "non chiama mai service://ai quando non ci sono headline in cache" senza un'asserzione che lo verificasse davvero** — controllava solo il valore di ritorno e `gateway.digest`, mai `fakeOllama.prompts`; una futura regressione che spostasse il controllo `cachedHeadlines.length === 0` dopo la chiamata a `service://ai` sarebbe passata comunque. Corretto aggiungendo `expect(fakeOllama.prompts).toHaveLength(0)`.

    Test nuovi: `tests/integration/news-digest.test.ts` (nuovo file, 11 test — nessuna chiamata IA senza headline in cache, generazione+pubblicazione+aggiornamento cache, il prompt include ogni titolo in cache, titolo con newline collassato su una riga, titolo eccessivamente lungo troncato, propagazione dell'errore quando `service://ai` non è raggiungibile senza toccare la cache, `service://news` include/omette `digest`/`digestContentId` prima/dopo la prima generazione riuscita, `startDigestAutoRefresh()`/`stopDigestAutoRefresh()` su un timer reale, `onError` ripetuto quando ogni tentativo fallisce, la guardia anti-superamento — quest'ultimo con `service://ai` registrato localmente con un ritardo controllabile per indice di chiamata anziché tramite `FakeOllamaServer`, per determinismo). 398 test totali (suite intera) rieseguiti ripetutamente, sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti; typecheck ad-hoc su `gateway/nomad/*.ts` pulito.

38. **`callService()` ignorava `timeoutMs` per un provider locale** (trovato durante lo sviluppo indipendente di un digest AI equivalente alla voce #37 — vedi la nota di processo in fondo a questa voce per come le due sessioni sono state riconciliate): `NomadNode.callService()` applicava un timeout SOLO al percorso di invocazione remota (`SERVICE_REQUEST`/`SERVICE_RESPONSE`, via `invokeService()`), mai a un provider registrato *localmente* sullo stesso nodo (`localServices`) — il ramo `if (local && local.announcement.availability) return local.handler(payload, this.nodeId);` ignorava `options.timeoutMs` in silenzio. Un handler locale bloccato (es. un gateway HTTP come `AiGateway` in attesa di un backend upstream incagliato) lasciava il chiamante in attesa per sempre invece che per al massimo `timeoutMs` — esattamente la topologia che `gateway/nomad/cli.ts` monta ogni volta che due servizi (es. `service://ai` + un suo consumatore) sono registrati sullo stesso nodo gateway, non solo per il digest.

    Corretto in `node/src/node.ts`: `callService()` ora calcola `timeoutMs` prima del branch locale/remoto e lo applica a entrambi tramite un nuovo metodo privato `withServiceTimeout()`. Non delega al helper condiviso `raceTimeout()` (`node/src/async-timeout.ts`, nuovo — usato invece da `web-ui.ts`, vedi sotto): quel helper tiene il proprio timer privato, quindi nulla al suo esterno può farne `clearTimeout()` in anticipo — proprietà necessaria perché `stop()` possa liberare subito una chiamata locale ancora in corso invece di lasciarne vivo il timer (e le closure che trattiene) fino alla scadenza naturale di `timeoutMs`, anche dopo che il nodo si è già fermato. `withServiceTimeout()` gestisce quindi il proprio timer esplicitamente in linea, rispecchiando esattamente il pattern già usato da `pendingServiceCalls` per le chiamate remote (`clearTimeout()` esplicito su ogni percorso di uscita: successo, fallimento dell'handler, timeout naturale, rigetto forzato da `stop()`). Un nuovo `pendingLocalServiceCalls: Set<{reject}>` traccia le chiamate locali in corso; `stop()` ora rigetta e svuota anche queste con lo stesso messaggio già usato per le chiamate remote ("node stopped while the service call was still in flight").

    `node/src/web-ui.ts` aveva una funzione locale `callWithHardTimeout()` che duplicava lo stesso identico boilerplate "race tra una promise e un timer" dietro `POST /api/call` — estratta nel nuovo `async-timeout.ts` (`raceTimeout()`), consumata sia da `web-ui.ts` sia (in origine, prima di scoprire il vincolo sul `clearTimeout()` anticipato sopra) dal primo tentativo di fix in `node.ts`.

    La revisione (`code-review`) non ha trovato problemi di correttezza/concorrenza nel fix (verificato ogni percorso di uscita di `withServiceTimeout()` per doppia rimozione o entry orfane in `pendingLocalServiceCalls`, nessuna interazione con il codice di `messageHistory`/chat 1:1 mergiato in parallelo, `generateDigest()` della voce #37 esercitato senza regressioni) — un solo rilievo cosmetico (una riga vuota doppia lasciata dalla rimozione di `callWithHardTimeout()`), corretto.

    Test nuovi in `tests/integration/service-discovery.test.ts`: un handler locale che non risolve mai va in timeout invece di restare appeso indefinitamente; `stop()` rigetta anche una chiamata `callService()` locale in corso (non solo quelle remote, già coperte da un test preesistente), verificato che il rigetto arrivi entro <1s e non aspettando la scadenza naturale di un timeout di 5s. 400 test totali (suite intera) rieseguiti ripetutamente, sempre verdi; typecheck e build puliti.

    **Nota di processo — collisione con una sessione parallela sullo stesso repository**: questa voce e la #37 sopra sono nate dalla stessa causa — due sessioni Claude Code diverse hanno implementato indipendentemente e in parallelo il pezzo 3 di "Nomad News evoluto" (digest AI), con design differenti (la #37: digest cachato con refresh periodico integrato in `service://news`; questa sessione: un servizio separato `service://news-digest`, generato on-demand, mai cachato). A differenza delle collisioni precedenti di questa serie (voce #35, una semplice collisione di numerazione su file di sola documentazione), qui si trattava di due implementazioni complete e mutuamente esclusive della stessa feature — non risolvibile con un merge automatico. L'utente, informato della situazione, ha scelto esplicitamente di scartare la versione di questa sessione e tenere quella già su `main` (voce #37, già completa di revisione/test/documentazione propri). Il lavoro sul digest specifico di questa sessione è stato quindi abbandonato; il bug in `callService()` descritto sopra — trovato durante quello sviluppo ma genuinamente indipendente da quale design del digest sopravvive, dato che non era ancora stato corretto da nessuna delle due sessioni — è stato invece riapplicato come contributo autonomo su `main` aggiornato (che nel frattempo aveva ricevuto anche la voce #36, chat 1:1, da una terza sessione parallela — verificato che non ci fosse sovrapposizione).

39. **`service://emergency-news` — ultimo pezzo di "Nomad News evoluto"** (`docs/next-steps.md` Opzione I — chiude la proposta insieme al parser RSS #33, i livelli headline/summary #35 e il digest AI #37, tutti già fatti; costruito sopra `CONTENT_ANNOUNCE` #34): `NewsGateway` guadagna un classificatore `isEmergencyHeadline(headline): boolean` (opzione del costruttore, `NewsGatewayOptions`), di default un confronto case-insensitive del campo `category` di una headline contro un elenco di parole chiave (`emergenza`, `allerta`, `allarme`, `protezione civile`) — un'ipotesi ragionevole per il caso d'uso rifugio alpino/Protezione Civile che la proposta stessa indica come il più concreto, non uno standard di alcun formato feed; un operatore con un feed che etichetta le emergenze diversamente può sovrascrivere il classificatore. `syncNews()` ora, per ogni headline nuova/cambiata (mai una già nota e invariata) che il classificatore marca, la pubblica con `{ announce: true, priority: Priority.EMERGENCY }` invece del default — solo il livello headline, mai il corpo articolo, che resta recuperabile on-demand come sempre. Nuovo getter `emergencyHeadlines` (filtra `cachedHeadlines` al volo, nessuno stato aggiuntivo) e `registerEmergencyNewsService()` (`service://emergency-news`, stesso pattern "sempre dalla cache, mai una chiamata live" di `registerNewsService()`) — un servizio separato, non solo un campo in più nella risposta di `service://news`, così un chiamante interessato solo alle emergenze non deve tirarsi dietro l'intera lista headline. `cli.ts` registra ora entrambi i servizi nella demo.

    La revisione (`code-review`, due giri) ha trovato 4 problemi reali, tutti corretti. Primo giro (3 problemi):
    - **Nessun tetto sul numero di `CONTENT_ANNOUNCE` a priorità `EMERGENCY` che un singolo `syncNews()` poteva originare** — il rate limiting per-peer già esistente (`rate-limit.ts`) protegge solo i pacchetti *ricevuti* da un peer connesso, mai quelli che questo nodo origina da sé tramite `floodExcept()`; un feed `--news-url` ostile/malfunzionante (già una minaccia documentata altrove in questa classe, es. il bound di `publishedById`) che taggasse molti item come emergenza — o ne ruotasse gli id per farli sembrare "tutti nuovi" — avrebbe potuto far floodare la mesh alla priorità più alta senza alcun limite. Corretto con una nuova costante `MAX_EMERGENCY_ANNOUNCES_PER_SYNC` (5) e un contatore per-sync: solo l'annuncio proattivo viene limitato, non la classificazione — una headline oltre il tetto resta comunque in `emergencyHeadlines`/`service://emergency-news`, semplicemente scopribile solo per via pull (come ogni altro contenuto) invece che spinta subito.
    - **`publishedById` è solo in memoria** — un riavvio del processo fa sì che il primo `syncNews()` successivo tratti ogni headline ancora nel feed come "cambiata", incluso un bollettino di emergenza invariato che ogni peer conosceva già prima del riavvio, ri-annunciandolo una volta a priorità `EMERGENCY` come se fosse nuovo. Non risolto con codice (richiederebbe persistenza tra riavvii, assente ovunque in questa classe, non solo qui) — documentato esplicitamente come limite noto e accettato sul campo `publishedById`, con nota che il tetto del punto precedente ne limita comunque la gravità in caso di crash loop ripetuto.
    - **`isChanged`/`isEmergency` decisi da un `computeContentId()` ricalcolato localmente sui byte della headline**, invece che dal valore di ritorno reale di `publishContent()` — oggi provabilmente identico (stessa funzione, stesso buffer passato a entrambi), ma non garantito da nulla se in futuro `publishContent()` cambiasse come deriva il proprio content id, lasciando la decisione di annuncio silenziosamente sbagliata. Corretto aggiungendo un controllo di uguaglianza esplicito subito dopo la chiamata a `publishContent()`, che lancia un errore interno (fail loud, non silenzioso) se mai dovessero divergere — stesso pattern già usato da `putVerified()` in `node.ts` per un caso analogo.

    Secondo giro, mirato ai tre fix sopra: nessun problema di correttezza trovato (il contatore per-sync è correttamente locale a ogni chiamata, il nuovo test di regressione verificato deterministico su 8 esecuzioni ripetute, il messaggio d'errore del controllo di uguaglianza chiaro). Un solo rilievo di efficienza, non un bug: il fix del terzo punto fa calcolare `computeContentId()` due volte per headline a ogni tick (una volta qui, una seconda dentro `publishContent()` sugli stessi byte) — costo trascurabile per un piccolo blob JSON, evitarlo richiederebbe cambiare la firma di `publishContent()` stessa (fuori portata qui); documentato con un commento invece di essere "risolto" con una ristrutturazione non necessaria.

    Test nuovi: `tests/integration/news-emergency.test.ts` (nuovo file, 7 test — classificatore di default su categoria con parola chiave case-insensitive; un predicato personalizzato sostituisce interamente il default; `service://emergency-news` risponde solo col sottoinsieme mentre `service://news` resta invariato; lista vuota prima di un sync o quando nulla qualifica; una headline di emergenza raggiunge un nodo connesso a due hop di distanza immediatamente via `CONTENT_ANNOUNCE` mentre una headline ordinaria dallo stesso sync non viene mai annunciata proattivamente; un resync di una headline di emergenza invariata non la ri-annuncia, verificato contando l'evento `content:announced` sul nodo ricevente; il tetto sul numero di annunci per sync, verificato con 8 headline di emergenza in un solo sync — solo le prime 5 producono un annuncio, tutte e 8 restano comunque elencate). 407 test totali (suite intera) rieseguiti ripetutamente, sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti; typecheck ad-hoc su `gateway/nomad/*.ts` pulito.

    **Nota di processo**: prima di iniziare questa voce, `git fetch` ha rilevato che `main` era avanzato di un commit (voce #38, `callService()`/timeout per provider locale) non ancora presente sul branch di lavoro — riconciliato con un fast-forward pulito (`git merge origin/main --ff-only`, nessun conflitto: nessuna sovrapposizione di file con questa voce) prima di procedere, come da lezione ormai consolidata in questa sessione sulle collisioni tra sessioni parallele.

40. **Canali pubblici di chat** (`docs/next-steps.md` Opzione J — unico pezzo indipendente rimasto del "Piano d'insieme" dopo il completamento di "Nomad News evoluto"; costruito sopra `CONTENT_ANNOUNCE` #34 e con lo stesso impianto backend/UI della chat 1:1 #36): un messaggio di canale è un `content://` pubblicato con la convenzione sul nome `chat:<canale>` (`node/src/public-channels.ts`, nuovo file — `name` è già coperto dalla firma del publisher, `contentSigningPayload()`, quindi un relay non può ri-etichettare un messaggio in un altro canale, nessun cambio di protocollo) e `{ announce: true, priority: Priority.MESSAGING }`. Nuovo `NomadNode.publishChannelMessage(channel, text)` (normalizza il canale a minuscolo, valida, pubblica, registra subito nella propria cronologia locale — un `CONTENT_ANNOUNCE` originato localmente non torna mai indietro al mittente) e `considerChannelMessage()` privato (fire-and-forget: se il nome combacia con `chat:<canale>`, scarica i byte via `getContent()` e li registra se rispettano la forma attesa), agganciato sia a `handleContentAnnounce()` sia al loop di `handleSyncResponse()`. Nuovi `GET /api/channels` (lista canali noti) e `GET /api/channel-messages?channel=...` (storico) in `web-ui.ts`, **senza autenticazione** — stesso livello di `/api/peers`/`/api/services`/`/api/content`, perché un canale pubblico è per definizione stato pubblico della mesh, non "Private messages" spec §56 come invece `/api/messages`; `POST /api/channel-messages` (invio) resta dietro la stessa auth di `POST /api/call`. UI mobile: nuovo pannello "Canali" (lista + form "apri per nome") e un nuovo `#channel-panel` (thread di un canale aperto, stesso stile di `#chat-panel` ma con etichetta autore per i messaggi altrui, dato che è un canale di gruppo non una chat 1:1).

    La revisione (`code-review`, due giri) ha trovato 8 problemi reali in totale, tutti corretti. Primo giro (5 problemi):
    - **`considerChannelMessage()` chiamava `getContent()` senza alcun limite per ogni entry accettata con nome `chat:<canale>`** — il rate limiting esistente (`rate-limit.ts`) protegge solo i pacchetti *ricevuti* da un peer, mai quelli che il nodo *origina da sé* tramite `floodExcept()`; un burst di entry `chat:` fasulle (validamente firmate dalla stessa identità usa-e-getta, contentId distinti) poteva far floodare la mesh con un `CONTENT_QUERY` per ognuna, senza alcun limite — stessa classe di amplificazione già chiusa dalla voce #39 per `CONTENT_ANNOUNCE`, qui sul lato reattivo del fetch invece che su quello dell'annuncio. Corretto con un nuovo `Set` `pendingChannelFetches` limitato da `MAX_CONCURRENT_CHANNEL_FETCHES` (16): solo il fetch proattivo viene limitato, non la conoscenza — un'entry oltre il tetto resta comunque accettata in `remoteCatalog`, semplicemente non viene scaricata subito.
    - **`ChannelMessage.timestamp` veniva letto da `ContentMetadata.createdAt`**, un campo che `content.ts`'s `SignableContentFields` esclude deliberatamente dalla firma del publisher — un relay poteva quindi riscrivere `createdAt` in transito senza invalidare `verifyContentSignature()`, falsificando l'ordine di visualizzazione dei messaggi in un canale. Corretto spostando il timestamp dentro il payload firmato stesso (`{ text, timestamp }`, stesso precedente di `NewsHeadline.publishedAt`) — nuova `extractChannelMessagePayload()` in `public-channels.ts` (non più `extractChatText()`, che valida solo `{ text }` ed è rimasta riservata ai messaggi 1:1), `publishChannelMessage()` cattura `Date.now()` una volta e lo incorpora nel payload pubblicato.
    - **`PublicChannels` valutava l'eviction di un intero canale in base alla fiducia SOLO dell'autore più recente** — un singolo post a bassa fiducia in un canale altrimenti affidabile e consolidato ne azzerava istantaneamente la protezione, rendendo l'intera cronologia del canale il primo candidato all'eviction alla prossima occasione. Corretto calcolando il punteggio come il **massimo** tra le fiducie di tutti gli autori attualmente presenti nella cronologia del canale (`bestAuthorTrust()`), non solo l'ultimo — con un costo accettato e documentato (rescan fino a `maxMessagesPerChannel` voci a ogni valutazione di eviction, O(`maxChannels`×`maxMessagesPerChannel`) nel caso peggiore, non raggiungibile da un attaccante dato che sono opzioni di costruzione, non alimentate dalla rete).
    - **`PublicChannels.record()` poteva memorizzare un array vuoto per un canale** se `maxMessagesPerChannel` fosse configurato a un valore `<= 0` (nessuna validazione a monte), violando il contratto implicito "`list()` restituisce solo canali con ≥1 messaggio" su cui `buildChannelList()` (`web-ui.ts`) faceva affidamento con un accesso indicizzato `messages[messages.length - 1]` — un crash potenziale su `GET /api/channels`. Corretto non memorizzando mai un canale con array vuoto dopo il trimming — stesso fix applicato per analogia anche a `MessageHistory.record()`, che aveva lo stesso pattern.
    - **Duplicazione quasi verbatim** tra `MessageHistory.record()` e `PublicChannels.record()` dello stesso pattern "push poi trim" — estratta una nuova funzione condivisa `pushBounded()` in `bounded-map.ts`, usata da entrambi.

    Secondo giro, mirato ai 5 fix sopra: **3 problemi reali trovati**, il primo dei quali un bug serio introdotto dal fix stesso:
    - **`pushBounded()` andava in loop infinito (freeze dell'event loop) per un `maxLength` negativo** — confermato empiricamente dalla revisione stessa: una volta svuotato l'array, `0 > maxLength` resta vero per sempre quando `maxLength` è negativo, e `Array.prototype.shift()` su un array vuoto è un no-op silenzioso, quindi il ciclo `while` non termina mai. Il caso `maxLength === 0` funzionava già correttamente (il ciclo termina non appena l'array torna vuoto); solo i valori negativi erano rotti, contraddicendo il commento del fix #4 sopra che dichiarava il caso "`<= 0`" pienamente gestito. Non raggiungibile da un attaccante oggi (nessun flag CLI collega queste opzioni a input esterno), ma un difetto vivo nell'helper condiviso appena introdotto da questo stesso giro di revisione. Corretto con una guardia esplicita `if (maxLength <= 0) return;` in testa alla funzione, prima che il ciclo venga mai raggiunto — `item` non viene nemmeno più inserito in quel caso, coerente con la convenzione già di `BoundedFifoMap.set()` ("un `maxSize <= 0` non trattiene nulla").
    - Il costo O(`maxChannels`×`maxMessagesPerChannel`) di `bestAuthorTrust()` — non un bug, documentato esplicitamente come tradeoff accettato (vedi sopra) invece di introdurre una struttura a "massimo mantenuto incrementalmente" con la sua stessa logica di invalidazione quando il messaggio più fidato esce dall'array per il trimming — machinery sproporzionata per un costo non raggiungibile dalla rete.
    - Il test del burst di 30 entry contava le occorrenze di `CONTENT_QUERY` con una regex applicata indipendentemente a ogni chunk TCP in arrivo — un confine di chunk che avesse spezzato la stringa letterale a metà avrebbe potuto sottostimare silenziosamente il conteggio (basso rischio di flakiness, mai di sovrastima, dato che le asserzioni tolleravano già un sottoconteggio). Corretto accumulando tutti i chunk in un buffer e contando una sola volta sul totale.

    Test nuovi: `tests/unit/public-channels.test.ts` (21 test — inclusi 5 per `extractChannelMessagePayload()` e uno dedicato che distingue esplicitamente `bestAuthorTrust()` dal vecchio comportamento "solo ultimo autore", con tre livelli di fiducia per evitare un pareggio nel punteggio che avrebbe mascherato la differenza), `tests/integration/public-channels.test.ts` (10 test — propagazione a 2 hop, scoperta via catalog sync, normalizzazione minuscolo, validazione, contenuto non-canale mai scambiato per un messaggio di canale, un `CONTENT_ANNOUNCE` validamente firmato ma con `name` non-stringa non manda in crash il nodo, byte scaricati che non fanno parsing come `{ text, timestamp }` vengono ignorati silenziosamente, il tetto sul numero di fetch reattivi con un burst di 30 entry fasulle, coerenza del timestamp registrato con quello effettivamente incorporato nei byte pubblicati), `tests/integration/web-ui.test.ts` (12 nuovi test per i tre endpoint), `tests/unit/bounded-map.test.ts` (5 nuovi test per `pushBounded()`, incluso quello che avrebbe bloccato la suite se la guardia sul `maxLength` negativo fosse regredita). 454 test totali (suite intera) rieseguiti ripetutamente (22 volte durante questa slice, 1 sola volta un fallimento isolato e non riproducibile in un file estraneo a questa modifica — coerente con la flakiness da timing già nota e documentata altrove in questo file, non imputabile a questo lavoro); `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti. Verificato anche end-to-end con Playwright contro due gateway reali connessi (invio, ricezione sulla dashboard del secondo gateway con etichetta autore corretta, tema chiaro/scuro, apertura/chiusura pannello, aggiornamento della lista canali) sia prima sia dopo il giro di fix.

41. **`announcedEmergencyById` — la decisione di annuncio EMERGENCY disaccoppiata da `publishedById`** (follow-up mirato sulla voce #39, `service://emergency-news`, non una nuova feature — vedi la nota di processo in fondo a questa voce per l'origine): la voce #39 gated la decisione di annuncio (`isEmergency`) su `isChanged` (cioè `publishedById.get(id) !== contentId`), e documentava come limite noto e accettato che un riavvio del processo (che svuota `publishedById`, solo in memoria) potesse far ri-annunciare un bollettino invariato. Non notato dalla voce #39: `publishedById` è una FIFO **condivisa da ogni headline tracciato** (bound a `MAX_TRACKED_HEADLINES`, 4096, non solo quelli di emergenza) — un volume sufficiente di headline di **routine** non correlati, durante il normale funzionamento a lungo termine (`startAutoSync()`, **nessun riavvio necessario**), può far scadere per eviction FIFO la entry di un bollettino di emergenza invariato, facendo tornare `isChanged` vero e ri-annunciandolo mesh-wide a priorità `EMERGENCY` per un contenuto che non è mai realmente cambiato — lo stesso sintomo descritto dalla voce #39, ma raggiungibile molto più facilmente e senza il prerequisito "riavvio" che il suo commento lasciava intendere fosse necessario.

    Corretto con una nuova mappa dedicata `announcedEmergencyById: BoundedFifoMap<string,string>` (bound indipendente e più piccolo, `MAX_TRACKED_EMERGENCY_HEADLINES` = 512) che tiene solo gli headline id **effettivamente classificati come emergenza e realmente annunciati** — la decisione `isEmergency` ora confronta contro questa mappa (`!alreadyAnnouncedThisVersion`, per content id, non per sola presenza dell'id) invece che contro `isChanged`/`publishedById`, che restano invariati per tutto il resto (la lista "changed" di `service://news`). Il tetto esistente `MAX_EMERGENCY_ANNOUNCES_PER_SYNC` (5, voce #39) resta invariato e continua a funzionare insieme al nuovo controllo. Effetto collaterale positivo notato durante l'implementazione: un headline "rimandato" dal tetto per-sync ora non viene mai scritto in `announcedEmergencyById` (solo se `isEmergency` risulta vero), quindi resta eleggibile a un tentativo di annuncio in un sync futuro meno affollato, invece di restare permanentemente "solo pull" come nella voce #39 — un miglioramento comportamentale collaterale, documentato nei commenti.

    **Narrowed, non eliminato — limite accettato, come ogni altra struttura bounded in questo codebase (spec §57)**, corretto esplicitamente nel commento della nuova mappa dopo un giro di revisione: una distribuzione che accumula genuinamente più di 512 id distinti annunciati come emergenza nel corso della sua vita (es. un lungo periodo di allerta con molti bollettini regionali distinti) alla fine soffrirà la stessa classe di eviction che questo fix chiude per `publishedById` — solo a una soglia molto più alta e specificamente scoped alle sole emergenze, non un'eliminazione assoluta del problema. `MAX_EMERGENCY_ANNOUNCES_PER_SYNC` limita solo quanto velocemente un singolo sync può avvicinarsi a quella soglia, non la alza.

    La revisione (`code-review`, due giri) ha trovato 2 problemi reali sul solo follow-up (non sul resto della voce #39, già rivisto separatamente), entrambi corretti: il commento sulla nuova mappa non chiariva che il problema era "ristretto", non "risolto" — corretto con un paragrafo esplicito (vedi sopra); nessun test copriva il caso complementare — un headline già annunciato una volta il cui contenuto **cambia genuinamente** (nuovo `articleContentId`/testo, quindi nuovo `headlineContentId`) deve continuare a ri-annunciare correttamente, non solo il caso "invariato non deve ri-annunciare" — un refactor futuro plausibile (`announcedEmergencyById.has(id)` al posto di `.get(id) === contentId`) avrebbe rotto silenziosamente questa garanzia senza che alcun test se ne accorgesse.

    Test nuovi in `tests/integration/news-emergency.test.ts` (stesse convenzioni del file esistente — fixture `TestHeadline`, conteggio dell'evento `content:announced` sul nodo ricevente): un bollettino di emergenza annuncia una volta, poi 5000 headline di routine in 5 batch (>4096, il cap di `publishedById`) fanno scadere la sua entry da `publishedById`, poi lo stesso bollettino invariato viene ri-sincronizzato — verificato che il conteggio degli annunci resti a 1, non 2; un secondo test verifica il caso complementare (contenuto genuinamente cambiato → nuovo content id → ri-annuncio corretto, conteggio a 2). 409 test prima del merge con la voce #40 (canali pubblici di chat), 456 dopo (suite combinata, nessuna sovrapposizione di file tra le due voci) — rieseguiti ripetutamente in entrambi gli stati, sempre verdi; typecheck e build puliti in entrambi.

    **Nota di processo — terza collisione consecutiva sulla stessa proposta, stesso schema delle precedenti (voci #35, #38); più una seconda collisione, di semplice numerazione, con la voce #40 (canali pubblici di chat, feature indipendente e non sovrapposta nel codice)**: questa sessione aveva sviluppato una propria implementazione completa e rivista di `service://emergency-news` (design simile alla voce #39 ma con questa stessa mappa disaccoppiata già inclusa fin dall'inizio, oltre a un classificatore per categoria singola invece che per parole chiave multiple) quando `git fetch` ha rilevato che un'altra sessione aveva già pushato su `main` una versione completa ed equivalente (voce #39), nel frattempo. Su richiesta esplicita dell'utente ("scarta la mia versione, tieni la loro, poi proponi la mia protezione come follow-up"), il lavoro di questa sessione sulla feature stessa è stato scartato e il branch riallineato a `main` con un fast-forward pulito; la protezione contro l'eviction di `publishedById` — un miglioramento reale e indipendente dal design specifico scelto, non presente nella voce #39 — è stata poi riproposta e implementata separatamente come questa voce, sopra il codice già accettato. Subito dopo aver committato questa voce (numerata "40" in quel momento), un secondo `git fetch` prima del push ha rilevato che una terza sessione aveva nel frattempo pushato su `main` "Canali pubblici di chat" (sopra), anch'essa numerata "40" — a differenza delle collisioni precedenti, questa volta senza sovrapposizione di codice (file completamente distinti), risolta con un merge (non un fast-forward) e questa voce rinumerata a #41, seguendo lo stesso principio già usato per la collisione di numerazione della voce #35: la voce già pubblica/di riferimento su `main` mantiene il proprio numero, quella locale ancora da pushare si rinumera.


42. **Chat private/di gruppo cifrate** (`node/src/groups.ts`, nuovo modulo — docs/next-steps.md Opzione J, ultimo pezzo indipendente del piano di messaggistica dopo la chat 1:1 #36 e i canali pubblici #40; approvata dall'utente dopo una fase di planning esplicita, `/root/.claude/plans/purring-coalescing-scone.md`, con una decisione esplicita dell'utente sul ciclo di vita del gruppo — vedi sotto): membership fissa alla creazione (v1, nessuna operazione di rimozione/uscita membro, quindi nessuna rotazione di chiave da progettare — l'utente ha scelto esplicitamente questo ambito, più ristretto, invece del ciclo di vita completo con forward secrecy, per tenere la slice proporzionata; limite noto, estendibile in una slice futura indipendente).

    **Design**: un invito è una `PRIVATE_MESSAGE` 1:1 ordinaria (`sendPrivateMessage()` esistente, nessun cambiamento di protocollo) con un payload discriminato `{ type: "group-invite", groupId, name, groupKey, members, createdBy, createdAt }` — l'autenticità del mittente viene già dalla cifratura ECDH punto-a-punto di `PRIVATE_MESSAGE` (nota solo alle due parti), nessuna firma aggiuntiva necessaria per l'invito stesso. I messaggi di gruppo invece usano un **nuovo `MessageType.GROUP_MESSAGE`**, broadcast (nessun `destination`) che passa gratis attraverso `decideForward()`/`SeenCache` già esistenti (stesso schema di `CONTENT_ANNOUNCE`/canali pubblici) — la confidenzialità viene dalla cifratura (AES-256-GCM con la chiave simmetrica del gruppo, `encryptForPeer()`/`decryptFromPeer()` già esistenti, riusati identici su una chiave di gruppo invece che su una chiave derivata ECDH), non dalla mancata consegna: tutta la mesh riceve il ciphertext, solo i membri possono decifrarlo. **Oltre al GCM auth tag, ogni `GROUP_MESSAGE` porta una firma Ed25519 del mittente** (`groupMessageSigningPayload()`/`signGroupMessage()`/`verifyGroupMessage()`, stesso schema di `contentSigningPayload()`) — necessaria perché con una chiave di gruppo condivisa l'auth tag GCM da solo prova solo l'integrità rispetto alla chiave, non l'identità del mittente: senza la firma, qualunque membro del gruppo (chiunque conosca la chiave) potrebbe fabbricare un messaggio che sembra provenire da un altro membro. `handleGroupMessage()` verifica prima la firma (significativo anche per un non-membro: prova che il mittente dichiarato ha davvero prodotto il pacchetto), poi cerca il gruppo localmente — se non è noto (non si è membri, il caso ordinario per la maggior parte della mesh), scarta senza mai tentare la decifratura.

    `NomadNode.createGroup(name, memberIds)` valida che ogni membro abbia una chiave di cifratura nota (`peerDirectory`, altrimenti throw prima di inviare nulla, stesso stile di `sendPrivateMessage()`), genera `groupId`/chiave casuali, invia l'invito a ciascun membro, registra il gruppo localmente. `NomadNode.sendGroupMessage(groupId, text)` cifra+firma+flooda, registra subito la propria copia (un broadcast auto-originato non torna al mittente, stesso motivo già documentato per `publishChannelMessage()`). Nuovo modulo `Groups` (`groups.ts`): **una singola `BoundedFifoMap<groupId, {info, messages}>`, non due mappe separate** — scelta deliberata per evitare che l'eviction di una mappa "gruppi" e di una mappa "messaggi" divergano, lasciando uno stato orfano (messaggi per un gruppo non più noto, o un gruppo noto senza cronologia raggiungibile). Bounded su gruppi (`maxGroups`, eviction pesata sulla fiducia di chi ha invitato) e messaggi-per-gruppo (`maxMessagesPerGroup`), stessa doppia dimensione di `MessageHistory`/`PublicChannels`. Endpoint HTTP nuovi in `web-ui.ts`: `GET`/`POST /api/groups`, `GET`/`POST /api/group-messages` — **mai pubblici**, stessa auth a password di rete di `/api/messages` (a differenza dei canali pubblici, nome/membership di un gruppo sono informazione privata, spec §56); `toGroupSummary()` esclude sempre la chiave del gruppo dalla risposta HTTP. UI mobile: pannello "Gruppi" con selezione membri da checkbox (solo peer con `canMessage`), pannello thread condiviso stile chat 1:1/canali.

    **Nessun nuovo limite di rate/cap necessario per l'amplificazione self-originated** (a differenza delle voci #39/#40): `GROUP_MESSAGE` porta il ciphertext completo nel pacchetto stesso (a differenza di `CONTENT_ANNOUNCE`, solo metadata) — non c'è un fetch reattivo innescato dalla ricezione, e `sendGroupMessage()` è sempre invocato esplicitamente via l'endpoint HTTP autenticato, mai automaticamente in risposta a traffico di rete in arrivo, quindi nessuna catena di amplificazione self-originated da proteggere qui.

    La revisione (`code-review`, due giri) ha trovato 2 problemi reali sul primo giro, entrambi corretti, nessuno sul secondo:

    - **Nessuna protezione anti-replay per `GROUP_MESSAGE`**: `SeenCache` (routing.ts) deduplica solo per `packet.id` casuale, mai per contenuto — un payload `GROUP_MESSAGE` catturato e re-incapsulato in un nuovo pacchetto (nuovo `packet.id`, via `createPacket()`) bypassava del tutto la dedup e veniva ri-accettato indefinitamente da chi era membro, con `Groups.recordMessage()` che lo appendeva come un messaggio "nuovo" a ogni replay — rischio concreto di espellere messaggi genuinamente più recenti una volta raggiunto `maxMessagesPerGroup`. Corretto con un nuovo campo `GroupMessage.messageId`, popolato dalla firma Ed25519 del mittente (`payload.signature`) — la firma EdDSA è deterministica (stessa chiave + stessi byte firmati producono sempre la stessa firma), quindi un replay esatto produce lo stesso `messageId` e viene scartato come no-op da `recordMessage()`, stesso pattern di dedup-by-`contentId` già usato da `PublicChannels.record()`. **Fix solo lato memorizzazione, non lato forwarding**: l'amplificazione di banda (un replay con nuovo `packet.id` viene comunque re-floodato dalla mesh, perché la decisione di forward avviene in `decideForward()`/`SeenCache` prima che `handleGroupMessage()` veda mai il pacchetto) resta un limite architetturale accettato, non specifico di questa feature — condiviso da `CONTENT_ANNOUNCE`/`SERVICE_ANNOUNCE`/canali pubblici (nessuno di questi meccanismi ha una dedup di forwarding basata sul contenuto, solo su `packet.id`), quindi deliberatamente non affrontato qui: risolverlo richiederebbe una cache di anti-replay content-aware condivisa da ogni tipo di pacchetto broadcast, un cambiamento architetturale più ampio di questa slice.
    - **`considerGroupInvite()` si fidava di `invite.createdBy` (auto-dichiarato dentro il payload cifrato, mai autenticato di per sé) invece del parametro `senderId` (il vero mittente, autenticato dalla decifratura ECDH riuscita di `PRIVATE_MESSAGE`)** per popolare `GroupInfo.createdBy`, usato poi per l'eviction pesata sulla fiducia in `Groups`. Un contatto a bassa fiducia poteva forgiare quel campo con l'id di un nodo fidato, proteggendo il proprio gruppo fasullo dall'eviction a spese di uno genuinamente fidato — stessa classe di attacco già corretta per `PeerDirectory`/`RemoteCatalog` (bug #13 sopra). Corretto usando `senderId` invece di `invite.createdBy`; in questa v1 (autorità di invito solo del creatore) i due valori coincidono sempre nel caso onesto, quindi il fix non costa nulla al comportamento legittimo.

    Test nuovi: `tests/unit/groups.test.ts` (28 test — validazione `extractGroupInvite()`, firma/cifratura/decifratura di un messaggio di gruppo con payload malformati, `Groups` bounded su entrambi gli assi con eviction pesata sulla fiducia, un test dedicato che prova che l'eviction di un gruppo rimuove anche la sua cronologia senza lasciare stato orfano, un test di dedup per `messageId`), `tests/integration/group-chat.test.ts` (12 test — creazione gruppo e invito consegnato a nodi reali, messaggio di gruppo che raggiunge un membro a 2 hop mentre un nodo non-membro riceve il broadcast ma non impara nulla, `createGroup()`/`sendGroupMessage()` falliscono in modo pulito su input non valido, nessuna regressione su `sendPrivateMessage()`/`handlePrivateMessage()` esistenti, un tentativo di impersonificazione con firma forgiata scartato, un `GROUP_MESSAGE` per un `groupId` sconosciuto scartato senza intaccare la cronologia del gruppo reale, il replay dello stesso payload firmato una sola volta ma reinviato con due `packet.id` diversi non produce un duplicato, un invito con `createdBy` forgiato registra comunque il vero mittente autenticato). Nuovi casi anche in `tests/integration/malformed-packet-robustness.test.ts` (payload `GROUP_MESSAGE` mancante/con campi non validi/con una firma strutturalmente valida ma per un gruppo sconosciuto — nessuno di questi manda in crash il nodo). 497 test totali (suite intera, dopo il merge con la voce #41 già su `main`), rieseguiti ripetutamente (5 volte consecutive dopo i fix, sempre verdi); typecheck e build puliti. Verificato anche end-to-end con Playwright contro un gateway reale (creazione gruppo dalla UI mobile con selezione membri, invio/ricezione nel pannello di thread condiviso, comparsa immediata del nuovo gruppo nella lista laterale — corretto un piccolo bug UX trovato durante questa verifica, non dalla code-review: il nuovo gruppo non appariva nella lista finché non scadeva il refresh periodico di 5s, corretto con un refresh immediato non bloccante dopo la creazione).

43. **`service://translation` + redirect automatico alla home page (captive portal)** — richiesta esplicita dell'utente dopo aver verificato lo stato reale del progetto (nessun redirect automatico esisteva, nessun servizio di traduzione tra `service://ai`/`service://kiwix-search`/`service://news`). Due pezzi piccoli e indipendenti trattati come un'unica slice per proporzionalità.

    **`service://translation`** (`gateway/nomad/translate-gateway.ts`, nuovo modulo): compone `service://ai` tramite `node.callService()` — stesso pattern già usato da `NewsGateway.generateDigest()` (voce #37), mai un riferimento diretto ad `AiGateway`. `registerTranslateService(node, options?)` è una funzione, non una classe: nessuno stato proprio, nessuna cache — ogni chiamata è interattiva e delega interamente a `service://ai`. Payload richiesta `{ text, targetLanguage }`, risposta `{ translatedText, targetLanguage }`. Lista fissa di 5 lingue (`SUPPORTED_LANGUAGES`: italiano/inglese/tedesco/francese/spagnolo — le più comuni per un rifugio alpino, non un editor libero, per restare un servizio prevedibile/testabile e una tendina semplice in UI); `targetLanguage` fuori lista viene rifiutato con un errore esplicito, mai passato al prompt. `text` non vuoto, tetto `MAX_TRANSLATE_TEXT_CHARS` (2000, un messaggio non un documento). `sanitizeTextForPrompt()` collassa la sequenza `"""` (usata per isolare il testo utente dentro il prompt) in `'''`, per impedire che il testo possa "rompere" quel delimitatore — **limite dichiarato esplicitamente nel commento del codice**: chiude solo questo rischio strutturale specifico, non è protezione generale da prompt injection (un testo ostile che non contiene mai `"""` passa invariato, stessa cornice onesta già usata per `sanitizeTitleForPrompt()`, voce #37). `gateway/nomad/cli.ts` registra il servizio incondizionatamente subito dopo `AiGateway` (nessun nuovo flag: non ha un URL esterno proprio, gli basta che `service://ai` esista). UI mobile: nuova icona `icon-translate` (frecce di scambio) in `mobile/www/index.html`, `SERVICE_ICONS`/`SERVICE_LABELS` in `app.js` ("Traduttore"), `buildCallForm()` guadagna un ramo dedicato con textarea + tendina lingua invece del payload JSON generico.

    **Redirect automatico captive portal** (`node/src/web-ui.ts`): nuovo blocco di route, sempre attivo (nessuna nuova `WebUiOptions` — un redirect fisso su un pugno di path noti non ha implicazioni di sicurezza da gating come `allowServiceCalls`), per i path "sonda" che iOS/macOS, Android, Windows e Firefox/Linux interrogano automaticamente dopo essersi collegati a un Wi-Fi, per decidere se mostrare il proprio popup "Accedi alla rete": `/hotspot-detect.html`, `/library/test/success.html` (Apple), `/generate_204`, `/gen_204` (Android), `/connecttest.txt`, `/ncsi.txt` (Windows), `/success.txt` (Firefox/Linux) — nuova costante `CAPTIVE_PORTAL_PROBE_PATHS`. Tutti rispondono `302` con `Location: /`, instradati solo per `req.url` (mai `Host`, che dopo un DNS hijack lato hostapd/dnsmasq può essere qualunque dominio), solo su `GET`. `docs/guida-hardware-rifugio.md` sezione 2 estesa con gli snippet `hostapd.conf`/`dnsmasq.conf` concreti (il DNS hijack è il pezzo mancante perché gli OS raggiungano davvero questi path — spiegato perché serve, marcato esplicitamente come passo facoltativo).

    La revisione (`code-review`, due giri) ha trovato 3 problemi reali sul primo giro, tutti corretti, nessuno sul secondo:

    - **Bypass della validazione `targetLanguage` via proprietà ereditate di `Object.prototype`**: `targetLanguage in SUPPORTED_LANGUAGES` (un plain object) matcha anche `"constructor"`, `"toString"`, `"valueOf"`, `"hasOwnProperty"` — nessuna di queste è una lingua reale, ma passavano comunque la validazione e finivano stringificate dentro il prompt inviato a `service://ai` (es. `targetLanguage: "constructor"` → il prompt conteneva `Traduci il testo seguente in function Object() { [native code] }.`), esattamente il comportamento che la validazione doveva impedire. Riprodotto empiricamente prima del fix. Corretto con `Object.prototype.hasOwnProperty.call(SUPPORTED_LANGUAGES, targetLanguage)` al posto di `in`.
    - Il commento su `sanitizeTextForPrompt()` presentava la mitigazione come se prevenisse la prompt injection in generale, mentre chiude solo la rottura del delimitatore `"""` — riscritto per essere onesto sull'ambito reale (vedi sopra), nessun cambiamento di comportamento.
    - Un riferimento anticipato a questa stessa voce (`mobile/README.md`, "voce #43") scritto prima che la voce esistesse — risolto scrivendo questa voce subito dopo, numero verificato fresco via `git fetch` prima di committare.

    Test nuovi: `tests/integration/translate-gateway.test.ts` (9 test — traduzione riuscita con verifica del prompt inviato, testo vuoto/oltre il tetto rifiutati, `targetLanguage` sconosciuto o proprietà ereditata di `Object.prototype` rifiutati (5 varianti, incluso `__proto__`) senza mai chiamare `service://ai`, tentativo di rompere il delimitatore `"""` neutralizzato, `service://ai` non disponibile → errore propagato pulito, risposta AI malformata/vuota → errore esplicito), `tests/integration/web-ui.test.ts` (3 nuovi test — ogni path noto risponde `302`/`Location: /` senza autenticazione, un path simile-ma-diverso continua a fare `404`, solo `GET` viene rediretto). 508 test totali (suite intera), rieseguiti ripetutamente sempre verdi; typecheck e build puliti. Verificato anche end-to-end: redirect confermato via richieste HTTP dirette per ogni path noto; UI del traduttore verificata con Playwright contro un gateway reale (card "Traduttore" in dashboard, form dedicato, traduzione italiano→tedesco mostrata correttamente a schermo).

44. **Tracciamento posizione opportunistico** (`node/src/location-registry.ts`, nuovo modulo — docs/next-steps.md Opzione J, **ultimo candidato dell'intero piano d'insieme**) — preceduto da una conversazione esplicita con l'utente su consenso/conservazione/autorizzazione (la specifica stessa, §56, elenca la posizione tra i dati da minimizzare), non solo pianificazione tecnica. Tre decisioni raccolte prima di scrivere codice: (1) **consenso**: opt-in esplicito per ogni invio, mai un campionamento periodico automatico — un solo bottone "Condividi la mia posizione ora"; (2) **conservazione**: solo l'ultima posizione nota per mittente, nessuna cronologia — un nuovo invio sovrascrive sempre il precedente; (3) **accesso in lettura**: solo operatori fidati.

    **Punto tecnico emerso durante l'esplorazione, con conseguenza di design**: il modo più ovvio per implementare "solo operatori fidati" — un controllo sul `TrustLevel` del chiamante dentro un handler `service://...` — è risultato aggirabile: `fromNodeId` (passato a `ServiceHandler` per una `SERVICE_REQUEST`) non è autenticato crittograficamente, stesso limite già noto per `packet.source` (`CLAUDE.md`) — chiunque potrebbe forgiare un pacchetto dichiarandosi un nodo fidato. Presentato all'utente, che ha scelto l'alternativa più semplice e già collaudata invece di un nuovo protocollo di query mesh-wide autenticato: **il registro gira su un nodo dedicato e separato**, con la propria password di rete (stesso meccanismo HTTP già esistente e già verificato per `/api/groups`, voce #42) distribuita solo agli operatori fidati, mai agli ospiti — zero nuovo protocollo, nessun rischio di falsificazione del chiamante.

    **Design**: `node/src/location-registry.ts` (non `gateway/nomad/` — a differenza di `NewsGateway`/`AiGateway`/`translate-gateway.ts`, tutti adapter verso qualcosa di *esterno* alla mesh, il registro posizione è stato puramente interno alla mesh, come `groups`/`publicChannels`/`messageHistory`; risolve anche un vincolo architetturale reale, dato che `web-ui.ts` non può mai importare da `gateway/nomad/`, dipendenza a senso unico solo nell'altra direzione). `extractLocationReport()` valida un payload `PRIVATE_MESSAGE` discriminato `{ type: "location-report", lat, lon, accuracy?, timestamp }` (stesso pattern di `extractGroupInvite()`): `lat` in [-90,90], `lon` in [-180,180], `accuracy` opzionale non negativo, `timestamp` finito. `LocationRegistry` è una `BoundedFifoMap<reporterId, LocationReport>` con eviction pesata sulla fiducia, `record()` sempre in sovrascrittura (mai accumulo) e scadenza pigra opzionale (`maxReportAgeMs`, mai un timer di sweep — stessa convenzione di `ContentStore`/`RemoteCatalog`).

    **Scoperta**: `NomadNode.registerAsLocationRegistry()`, wrapper sottile su `registerService()` esistente, **mai chiamato automaticamente** — solo dal nodo dedicato scelto come registro. Il suo handler `service://location-registry` è discovery-only: rifiuta esplicitamente un `callService()` diretto invece di fare qualcosa in silenzio (i dati veri non passano mai da `SERVICE_REQUEST`, per il motivo di autenticazione sopra).

    **Scrittura**: `NomadNode.shareLocation({ lat, lon, accuracy? }, options?)` — stesso pattern "scopri poi invoca" di `callService()`: `discoverService("service://location-registry")` per trovare il provider corrente, poi `sendPrivateMessage(providerId, { type: "location-report", ... })`. `timestamp` è catturato una volta con `Date.now()` lato mittente, **mai accettato come argomento** (né da `shareLocation()` né, a valle, da `POST /api/location-report`) — stessa ragione già documentata per `publishChannelMessage()`/`sendGroupMessage()`. Nessuna firma aggiuntiva necessaria oltre alla cifratura ECDH punto-a-punto già esistente: a differenza di `GROUP_MESSAGE` (chiave condivisa tra più membri), qui la chiave è nota solo a mittente e registro, quindi la decifratura riuscita implica già in modo non falsificabile chi ha inviato.

    **Ricezione**: nuovo ramo in `handlePrivateMessage()` (node.ts) — `considerLocationReport(senderId, extractLocationReport(payload))` usa sempre `senderId` (cioè `packet.source`, autenticato dalla decifratura riuscita), **mai** un campo dentro il payload — stessa lezione già imparata e corretta per `GroupInfo.createdBy` (voce #42). Qualunque nodo può ricevere e memorizzare passivamente una segnalazione indirizzata a sé (`handlePrivateMessage()` decifra solo ciò che è indirizzato a questo nodo, quindi arriva solo se qualcuno ha scelto di segnalare proprio a lui) — nessun gate separato lato scrittura, la protezione vera è tutta lato lettura.

    **Lettura**: `WebUiOptions.exposeLocationRegistry` (nuovo, opt-in, richiede `networkPassword`, **indipendente da `allowServiceCalls`** — un nodo registro dedicato può averlo acceso senza offrire `POST /api/call`) — `GET /api/location-registry` (autenticato, stesso schema di `handleGetGroups()`, 404 se non attivo, esattamente come `/api/call` quando `allowServiceCalls` è spento). `POST /api/location-report` (autenticato come `/api/messages`, su **qualunque** nodo/gateway — è chi invia la segnalazione, non chi la legge — valida solo la forma di `lat`/`lon`/`accuracy` prima di chiamare `shareLocation()`, che fa la validazione di range vera e propria). `node/src/cli.ts` guadagna `--register-as-location-registry`/`--expose-location-registry` (nessun flag nel gateway NOMAD: il registro è stato mesh-interno, non un adapter — deviazione dal piano originario, che menzionava `gateway/nomad/cli.ts`, corretta durante l'implementazione una volta chiaro dove il codice doveva vivere).

    **UI mobile**: nuovo bottone "Condividi la mia posizione" (sempre visibile, mai dentro un `<details>` collassato — stesso principio "un utente comune deve vedere subito cosa può fare" già applicato al pannello Servizi), nuovo pannello "Posizioni condivise" (nascosto di default, visibile solo quando `GET /api/location-registry` non fa 404 — stesso degrado onesto già visto per il QR di `/api/pairing`). Nuova dipendenza `@capacitor/geolocation` (**la prima eccezione reale a "niente dipendenze esterne non necessarie"**: a differenza del QR, scritto da zero apposta, non c'è un modo ragionevole di leggere il GPS hardware nativo senza un plugin — usata via `window.Capacitor.Plugins.Geolocation` quando presente, con fallback a `navigator.geolocation` del browser, quest'ultimo il percorso davvero verificabile in questa sandbox, che non ha una build Android nativa).

    La revisione (`code-review`, tre giri) ha trovato 2 problemi reali sul primo giro e il secondo giro ne ha trovato un terzo introdotto dal fix del primo (più un problema di qualità dei test, corretto insieme):

    - **Nessuna guardia contro un report più vecchio che sovrascrive uno più recente**: `LocationRegistry.record()` sovrascriveva incondizionatamente. Questa è una mesh delay-tolerant con store-and-forward (spec §30): un report messo in coda perché nessuna rotta esisteva ancora poteva essere consegnato **dopo** un report più recente arrivato per una via più veloce, sovrascrivendolo silenziosamente — o, combinato con `maxReportAgeMs`, cancellandolo del tutto. Stessa classe di bug già corretta altrove in questo codebase per lo stesso motivo (`NewsGateway.startAutoSync()`'s `syncGeneration`). Corretto rifiutando un `record()` il cui timestamp non è strettamente più recente di quello già in archivio.
    - **Quel fix da solo apriva un buco nuovo**: un report con un timestamp futuro fabbricato (es. da un mittente malevolo che aggira `shareLocation()` e forgia direttamente un `PRIVATE_MESSAGE`) avvelenava permanentemente lo slot di quel mittente — ogni aggiornamento genuino successivo perdeva il confronto contro un `Date.now()` onesto che non può mai raggiungere un valore fittizio nel futuro, e la scadenza pigra (`Date.now() - timestamp`) restava per sempre negativa, quindi nemmeno `maxReportAgeMs` poteva più reclamare l'entry. Corretto con `Math.min(report.timestamp, Date.now())` — il timestamp è sempre riportato indietro verso "adesso", mai in avanti, quindi non può più essere usato per costruire un report che sembra più recente di quanto sia davvero.
    - **Limite accettato, documentato esplicitamente nel codice invece di essere "risolto"**: il clamping si appoggia al `Date.now()` di *questo* nodo, che Node non garantisce monotono — se l'orologio di sistema del registro stesso va indietro (correzione NTP, ripresa da uno snapshot VM), un report già registrato con la lettura precedente e più alta può rifiutare un report genuinamente più tardo il cui timestamp lato mittente è più piccolo di quella lettura ormai stantia, stesso sintomo "slot bloccato" del bug sopra, finché l'orologio locale non torna a superarlo. Più ristretto e auto-risolvente rispetto al bug corretto (limitato all'ampiezza del salto d'orologio, mai permanente, non si propaga oltre il mittente coinvolto) — e non risolvibile per intero nemmeno con un orologio monotono locale, perché il problema di fondo è la mancanza di sincronizzazione **tra** nodi, per cui questa feature non ha un equivalente NTP. Stessa classe di limite accettato e documentato di `packet.source` non vincolato crittograficamente (`CLAUDE.md`).
    - **Problema di qualità dei test, trovato insieme**: un test preesistente ("aggiornare il report di un mittente non fa mai evict di un altro mittente") era diventato silenziosamente un no-op dopo il primo fix (usava un timestamp vecchio fisso che la nuova guardia ora scarta) — corretto usando un timestamp genuinamente più recente e aggiungendo un'asserzione che verifica che l'aggiornamento sia stato davvero applicato, non solo che nessun altro mittente sia stato espulso. Un test scritto per il terzo fix (clamping del timestamp futuro) era a sua volta intermittentemente flaky nella sua prima stesura (due letture ravvicinate di `Date.now()` reale potevano cadere nello stesso millisecondo, facendo scattare la guardia anti-pareggio invece di verificare il comportamento sotto test) — corretto con `vi.useFakeTimers()`/`vi.setSystemTime()`, stabile su 8 run consecutivi dopo il fix.

    Test nuovi: `tests/unit/location-registry.test.ts` (19 test — validazione `extractLocationReport()` con range/tipi non validi, `LocationRegistry` bounded con eviction pesata sulla fiducia, sovrascrittura invece di accumulo, scadenza pigra opzionale, il test di regressione sulla guardia anti-fuori-ordine, il test di regressione sul clamping del timestamp futuro), `tests/integration/location-registry.test.ts` (13 test — nodi reali: `shareLocation()` end-to-end a 2 hop via discovery per `SERVICE_ANNOUNCE`, un secondo caso identico ma via fallback `SERVICE_QUERY` (registrato solo dopo che la connessione già esiste, quindi mai raggiunto da un annuncio proattivo), validazione locale di `shareLocation()` senza mai contattare la mesh, timeout pulito quando nessun registro è raggiungibile, l'handler discovery-only rifiuta un `callService()` diretto, un report malformato non manda in crash nulla e non impedisce a un report valido successivo di arrivare, endpoint HTTP `GET`/`POST` con tutte le combinazioni di gate — 404 quando il flag giusto è spento, 401 password sbagliata, CORS/preflight OPTIONS estesi a `exposeLocationRegistry` anche senza `allowServiceCalls`). Nessun caso nuovo in `tests/integration/malformed-packet-robustness.test.ts` — un `location-report` malformato attraversa lo stesso percorso decrypt-poi-dispatch già esercitato lì con payload spazzatura per la chat/gli inviti di gruppo, e `extractLocationReport()` ha già una copertura esaustiva a livello unit. 541 test totali (suite intera), rieseguiti ripetutamente (8+ volte durante i tre giri di fix, sempre verdi) senza alcuna flakiness residua; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti in ogni stato.

    Verificato anche end-to-end con Playwright contro due nodi reali connessi (un gateway ordinario A e un registro dedicato B con password separata): dashboard su A mostra il bottone condividi (sempre) e il pannello "Posizioni condivise" correttamente nascosto (`exposeLocationRegistry` spento su A); un tap sul bottone (geolocalizzazione del browser mockata via Playwright) porta lo stato a "Posizione condivisa."; ripaiando lo stesso telefono virtuale al registro dedicato B, il pannello diventa visibile e mostra la voce corretta (`NODE-<8 hex>`, coordinate, "adesso", `±20 m`) — confermando l'intera catena scoperta→invio→ricezione→lettura autenticata funzionante con un browser reale, non solo con `NomadNode` in-process.

    **Con questa voce, l'intero piano d'insieme di `docs/next-steps.md` è completo** — nessun candidato pianificato rimane aperto; restano solo le milestone bloccate su prerequisiti esterni (hardware fisico, Docker/Project NOMAD raggiungibile, accesso a internet reale) e la nota, non ancora pianificata in dettaglio, su un futuro restyling grafico della UI mobile (`CLAUDE.md`, "Stato del progetto").

45. **`service://internet-fetch` — "Internet senza Internet"** (proposta discussa live con l'utente il 25 agosto 2026, non una voce preesistente in `docs/next-steps.md` — vedi Opzione K lì per la discussione completa): l'utente ha chiesto se Nomad-Net potesse dare accesso a Internet vero instradando una richiesta attraverso la mesh fino a un nodo che ha connettività reale. Un proxy HTTP aperto sopra una mesh anonima è però il classico scenario di abuso (stesso rischio dei nodi di uscita Tor: chi espone il gateway rischia di comparire come responsabile di traffico altrui verso siti illegali, e paga di tasca propria banda per sconosciuti) — discusso con l'utente, che ha scelto di restringere lo scope a un piccolo menu curato di richieste invece di un fetch libero.

    `gateway/nomad/internet-gateway.ts` (nuovo), `InternetGateway`, stesso schema di `KiwixGateway`/`AiGateway`/`NewsGateway`: registra `service://internet-fetch`, payload `{ kind: "rss" | "text", url }`, due barriere di sicurezza fondamentalmente diverse per natura:
    - **`kind: "rss"`** — nessuna allowlist di domini: la barriera è che `parseFeed()` (`rss-feed.ts`, già indurito nel corso di "Nomad News evoluto") deve riconoscere davvero un RSS/Atom valido, altrimenti la richiesta fallisce e nulla viene mai pubblicato. Un contenuto che non ha la forma di un feed non può mai passare da qui, per costruzione.
    - **`kind: "text"`** — qui la forma del contenuto non basta (un testo può contenere qualunque cosa), quindi la barriera vera è `allowedTextHosts`, una allowlist di host passata esplicitamente dall'operatore al costruttore (`--internet-allowed-hosts` in `cli.ts`) — nessuna lista precompilata nel codice, stessa scelta già fatta per `emergencyCategory`/`feedUrl` di `NewsGateway`: l'operatore decide di cosa fidarsi, non il codebase.

    **Un problema aggiuntivo trovato progettando la feature, prima ancora della revisione**: anche `kind: "rss"` (senza allowlist) farebbe comunque una richiesta HTTP reale in uscita verso qualunque URL, anche quando poi la scarta per non essere un feed — un classico bersaglio per SSRF (far "bussare" il nodo dell'operatore verso la sua rete interna). Aggiunta quindi `gateway/nomad/url-safety.ts`, `isPubliclyRoutableUrl()`, applicata a **ogni** richiesta indipendentemente dal `kind`, prima di qualunque fetch: solo schema `http`/`https`; un hostname che è un IP letterale viene controllato direttamente contro una lista di intervalli privati/riservati (loopback, RFC1918, link-local incluso l'endpoint dei metadata cloud, multicast, ecc.); un hostname a dominio viene risolto (`dns.lookup`) e **ogni** indirizzo risolto viene controllato allo stesso modo — un solo indirizzo privato tra più risolti basta a rifiutare la richiesta. **Limite noto e dichiarato esplicitamente nel codice, non silenziosamente assunto risolto**: la risoluzione DNS fatta qui e quella che `fetch()` rifà da sé un istante dopo per connettersi davvero sono due lookup separati — un DNS che cambia risposta tra i due (DNS rebinding) può ancora far passare un indirizzo privato inosservato; risolverlo del tutto richiederebbe fissare l'indirizzo IP esatto per la richiesta successiva (un `Agent`/dispatcher personalizzato), fuori portata per questo prototipo — stessa onestà già usata altrove in questo file per `packet.source` non autenticato.

    **Le risposte non tornano nel pacchetto di risposta del servizio** (limite pratico di un solo pacchetto, ~2MB) — il contenuto scaricato viene pubblicato come `content://` (stesso schema di `NewsGateway`/`KiwixGateway`), la risposta del servizio è solo un riferimento (`contentId`/`mimeType`/`size`); il chiamante recupera i byte veri col normale ciclo `getContent()`/`CONTENT_CHUNK`, che porta con sé gratis chunking, limiti di dimensione e cache mesh-wide.

    **Rate limiting a due livelli**, entrambi necessari: un tetto per singolo chiamante (`maxRequestsPerPeerPerWindow`, default 10/min) e un tetto sulla somma di *tutti* i chiamanti insieme (`maxRequestsPerWindow`, default 60/min). Il solo tetto per chiamante non basterebbe: `fromNodeId` è il campo `source` di un pacchetto `SERVICE_REQUEST`, non autenticato crittograficamente (`CLAUDE.md`, "Binding crittografico") — un attaccante determinato può banalmente ruotare id fasulli per aggirare un limite legato all'identità. Il tetto globale è il vero argine: non aggirabile cambiando identità, limita direttamente il traffico reale in uscita che il nodo dell'operatore farà mai in totale.

    `gateway/nomad/fetch-bounded.ts` (nuovo): la funzione di fetch-con-tetto-di-byte, prima privata dentro `news-gateway.ts`, estratta in un modulo condiviso ora che serve una seconda volta (stesso principio già seguito per `BoundedFifoMap`/`loopback-http-server.ts`) — `news-gateway.ts` aggiornato per usarla, nessun cambiamento di comportamento.

    **Un bug reale trovato durante lo sviluppo dei test, prima ancora della prima revisione**: il primo tentativo di gestire un indirizzo IPv4-mappato dentro IPv6 (`::ffff:127.0.0.1`) cercava solo la forma puntata-decimale — ma `new URL(...)` normalizza sempre questi indirizzi nella forma esadecimale (`::ffff:7f00:1`, confermato empiricamente), quindi il controllo non scattava mai nella pratica e ogni indirizzo IPv4-mappato privato passava come "pubblico". Corretto riconoscendo anche la forma esadecimale.

    **Due giri di revisione** (`code-review`), 7 problemi reali trovati e corretti in totale:
    - **Giro 1** (4 problemi): (1) `checkRateLimit()` veniva chiamato per primo, prima di qualunque controllo — un chiamante poteva esaurire il tetto globale con richieste destinate comunque al rifiuto (kind sbagliato, host fuori allowlist, IP privato), senza che un solo fetch reale avvenisse mai, negando il servizio a chiamanti legittimi; corretto spostando `checkRateLimit()` dopo i controlli gratuiti. (2) Il rilevamento IPv4-mappato riconosceva solo `::ffff:a.b.c.d` — mancava la forma IPv4-compatible deprecata (`::a.b.c.d`, senza marcatore `ffff`), che passava come "pubblica" senza essere rilevata; corretto espandendo l'indirizzo a 8 gruppi esadecimali e controllando i prefissi numericamente invece che con regex sulla stringa compressa (riconosce ora entrambe le forme, e qualunque altra compressione equivalente). (3) `rateLimitState` (una `BoundedFifoMap` indicizzata per `fromNodeId`, non autenticato) non pesava l'eviction sulla fiducia, incoerente con la convenzione già seguita da `PeerDirectory`/`RemoteCatalog`/`MessageHistory`/`PublicChannels` (`CLAUDE.md`); corretto passando `evictionScore: (peerId) => trustRank(this.node.trust.get(peerId))`, spostando l'inizializzazione del campo nel corpo del costruttore (serve `this.node`, non ancora assegnato nell'inizializzatore di campo). (4) Nessun commento chiariva che la firma di un contenuto pubblicato via `kind: "rss"`/`"text"` attesta "questo gateway ha scaricato questi byte da quell'URL ora", mai un'attestazione del sito di origine (che non firma nulla) — aggiunta una nota esplicita sopra entrambe le chiamate a `publishContent()`.
    - **Giro 2, mirato ai 4 fix sopra** (3 problemi): (1) il fix del punto (1) sopra spostava `checkRateLimit()` dopo i controlli gratuiti, ma lasciava la guardia anti-SSRF (`isPubliclyRoutableUrl()`, che fa una vera risoluzione DNS per un hostname non letterale) **prima** del rate limit — una raffica di richieste verso hostname distinti poteva quindi ancora accodare risoluzioni DNS illimitate, mai contate da nessuno dei due tetti; corretto spostando `checkRateLimit()` dopo ogni controllo davvero gratuito (validazione payload, allowlist per `kind: "text"`) ma **prima** della guardia anti-SSRF, così sia la risoluzione DNS sia il fetch vero restano dietro il tetto. (2) Per `kind: "text"`, il controllo economico sull'allowlist (`Set.has()`) girava dopo la guardia anti-SSRF invece che prima, sprecando una risoluzione DNS per richieste che l'allowlist avrebbe comunque rifiutato a costo zero — corretto riordinando. (3) Il confronto "gruppo IPv6 tutto a zero" usava un insieme fisso di stringhe (`{"0", "0000"}`), perdendo le forme altrettanto valide `"00"`/`"000"` — non sfruttabile oggi attraverso i due veri punti di ingresso di questo modulo (il serializzatore di `URL` e `dns.lookup()`'s `inet_ntop` non producono mai quella forma), ma un'assunzione implicita non dichiarata; corretto confrontando il valore numerico del gruppo (`parseInt(g, 16) === 0`) invece della sua ortografia esatta.

    **Non testata su larga scala**: la eviction pesata sulla fiducia del punto (3) del giro 1 — richiederebbe migliaia di peer id fasulli per raggiungere `MAX_TRACKED_RATE_LIMIT_PEERS` (4096), costo giudicato sproporzionato per una wiring fix meccanica che replica un pattern già testato altrove in questo codebase per `BoundedFifoMap.evictionScore` — verificata solo con typecheck e lettura del codice. Allo stesso modo, la forma di gruppo IPv6 "00"/"000" del punto (3) del giro 2 non è testata direttamente: irraggiungibile attraverso l'API pubblica del modulo (`isPubliclyRoutableUrl(url: URL)`, che riceve sempre un hostname già normalizzato da `URL`), documentata nel codice come assunzione invece che verificata con un test.

    Test: `tests/unit/url-safety.test.ts` (25 test — ogni intervallo IPv4 riservato, IPv6 loopback/link-local/unique-local, indirizzi IPv4-mappati **ed** IPv4-compatible dentro IPv6, risoluzione DNS mockata con indirizzo singolo/multiplo/misto pubblico-privato/irrisolvibile, un test end-to-end reale su `localhost` senza mock), `tests/integration/internet-gateway.test.ts` (12 test — fetch RSS valido pubblicato e recuperabile, rifiuto di contenuto non-feed, rifiuto/accettazione per `kind: text` in base alla allowlist case-insensitive, risposta oltre il tetto di byte rifiutata senza bufferizzarla, payload malformati, rate limit per-peer e globale, una raffica di richieste destinate al rifiuto che non consuma il budget mentre una richiesta che raggiunge davvero la rete sì, più un secondo describe block **senza mock della guardia anti-SSRF** con due test — la guardia è davvero collegata, e il budget viene consumato anche da una richiesta che la guardia stessa rifiuta, non solo da un fetch riuscito). Verificato anche manualmente end-to-end contro la demo `cli.ts` reale (`--internet-fetch --internet-allowed-hosts ...`), un secondo `NomadNode` separato connesso via TCP reale che chiama il servizio e riceve il rifiuto atteso per un URL su loopback (prima e dopo il riordino del rate limiting).

    493 test totali prima del merge con le voci #42-44 già su `main`; 578 dopo il merge (suite combinata, nessuna sovrapposizione di file tra questa voce e le tre), rieseguiti ripetutamente in entrambi gli stati, sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti in entrambi; typecheck ad-hoc su `gateway/nomad/*.ts` pulito.

    **Nota di processo — collisione di numerazione con tre voci indipendenti** (stesso schema già visto per le voci #35, #38, #41): questa voce è stata sviluppata come "#42" in una sessione parallela a quella che nel frattempo pushava su `main` le voci #42 (chat private/di gruppo cifrate), #43 (`service://translation` + captive portal) e #44 (tracciamento posizione) — tre feature indipendenti e non sovrapposte nel codice con questa. Rilevato da `git fetch` prima del push, risolto con un merge (non un fast-forward) e questa voce rinumerata a #45, seguendo lo stesso principio delle collisioni precedenti: le voci già pubbliche su `main` mantengono il proprio numero, quella locale ancora da pushare si rinumera. Con questa voce, e la #42/#43/#44 già su `main`, l'intero "Piano d'insieme" di `docs/next-steps.md` risulta completo.

46. **Mappe topografiche offline** (`node/src/map-tiles.ts`, `MbtilesReader` — richiesta esplicita dell'utente il 27 agosto 2026, non una voce preesistente in `docs/next-steps.md`: "sarebbe importante aggiungere tra i servizi di nomad net le mappe offline sempre disponibili, in versione anche topografica"). Discussa e pianificata esplicitamente con l'utente prima di scrivere codice (stesso workflow di planning già usato per la voce #42/gruppi cifrati) — due decisioni tecniche emerse durante l'esplorazione del codebase, non nella discussione iniziale, che ne hanno corretto lo scope:

    **Dati**: tile raster pre-renderizzate (l'utente ha chiesto esplicitamente "la più semplice e leggera, con ragionevole certezza di rendering preciso") in un file **MBTiles** (SQLite, https://github.com/mapbox/mbtiles-spec) preparato una volta dall'operatore per una regione — stesso modello "un file preparato una volta" già usato per Kiwix (ZIM) e i feed RSS. Nessuna cartografia da costruire: la precisione del rendering è responsabilità del progetto a monte che genera le tile (es. OpenTopoMap), non di Nomad-Net — la stessa ragione per cui questa scelta è stata preferita a uno stack vettoriale che avrebbe richiesto di progettare uno stile topografico da zero.

    **Niente distribuzione via mesh (`content://`) — scoperto durante l'esplorazione, non nella discussione con l'utente**: `web-ui.ts` non aveva (e non ha tuttora) alcun endpoint che restituisca i byte di un contenuto via HTTP (solo `GET /api/content`, metadata soltanto) — instradare le tile via `service://maps`+`publishContent()` avrebbe richiesto anche di inventare un endpoint HTTP generico "dammi i byte di questo contentId", una feature a sé. Le tile vengono quindi servite **direttamente da un endpoint HTTP dedicato**, leggendo dal file MBTiles locale a ogni richiesta — mai pubblicate in `ContentStore` (una regione può avere decine di migliaia di tile, che avrebbero forzato `--max-content-entries` assurdamente alto o fatto evict tutto il resto). Limite accettato esplicitamente: le tile sono raggiungibili solo da chi arriva via HTTP a *questo* gateway (stesso perimetro di `/api/pairing`/dashboard), non si propagano nella mesh multi-hop come i contenuti Kiwix.

    `MbtilesReader` (`node/src/map-tiles.ts`, non `gateway/nomad/` — un file locale dell'operatore, mai dati mesh-fed né un adapter verso un backend NOMAD/internet esterno, stessa ragione di `location-registry.ts`/`groups.ts`) apre il file in sola lettura via **`node:sqlite`** (`DatabaseSync`, built-in di Node — nessuna nuova dipendenza esterna, coerente con la convenzione del progetto; ancora sperimentale su Node 22, un solo `ExperimentalWarning` all'avvio, pienamente funzionante). `getTile(z, x, y)` converte la convenzione di riga MBTiles (TMS, riga 0 a sud) in quella XYZ/"Google" usata da `mapview.js` e da ogni altro slippy map, un solo posto dove le due convenzioni si incontrano. `metadata` legge la tabella standard `metadata` (name/format/bounds/minzoom/maxzoom/attribution) in modo difensivo — un file MBTiles malformato non deve mai far crashare il nodo; formati diversi da png/jpg/jpeg/webp (es. tile vettoriali `pbf`) vengono rifiutati esplicitamente all'apertura, fuori scope per un visualizzatore scritto da zero che capisce solo raster.

    `web-ui.ts`: nuova opzione `WebUiOptions.mapTiles?: MbtilesReader` (istanza già aperta, passata da `cli.ts`), `GET /api/map-info` (metadata) e `GET /api/map-tiles/:z/:x/:y` (byte grezzi, nuovo `sendBinary()` in `loopback-http-server.ts` — la prima risposta non-JSON del progetto) — entrambi **deliberatamente non autenticati**: dati geografici del territorio, non personali (spec §56 non li elenca), stesso livello di `/api/content`/`/api/channels`; e servire una tile costa solo una lettura da disco locale contro un file che l'operatore ha già, mai una fetch live in uscita come per `InternetGateway`, quindi nessun problema di banda misurata da proteggere con una password. CORS esteso con la stessa condizione già corretta per `exposeLocationRegistry` (voce #44) — `mapTiles` da solo (senza `allowServiceCalls`/`exposeLocationRegistry`) deve comunque ottenere gli header CORS sul proprio endpoint. Cache-Control `immutable`/un anno sulle tile (un dato z/x/y risolve sempre agli stessi byte per la vita del file) — `mapview.js` si appoggia proprio a questo, ricostruendo la griglia di tile a ogni render invece di tenere una propria cache DOM.

    `cli.ts` (node/src/, non gateway/nomad/ — coerente con dove vive `MbtilesReader`): nuovo flag `--map-file <path>`, apertura in try/catch (file mancante/non valido → log chiaro, il nodo continua senza la feature, stesso non-fatale di `NewsGateway`), nessun flag di password dedicato (dati non sensibili, vedi sopra).

    `mobile/www/mapview.js` (nuovo, ~200 righe): **visualizzatore di tile raster scritto da zero, non Leaflet** — avevo raccomandato Leaflet in discussione con l'utente, ma questo ambiente non ha accesso a internet reale per scaricare/vendorizzare una libreria (stesso limite già documentato per Kiwix/News/Internet-fetch), e il progetto non ha mai introdotto una dipendenza di terze parti in `mobile/www/` oltre a `@capacitor/geolocation` (hardware GPS non altrimenti raggiungibile). Un pan/zoom di tile raster (matematica Web Mercator/slippy-map standard) è un problema ben delimitato, paragonabile per portata a `qrcode.ts`/`rss-feed.ts` — stessa convenzione "scrivilo tu invece di aggiungere una dipendenza". Pan via drag (trasformazione CSS live durante il trascinamento, griglia di tile ricostruita solo al rilascio — mai un ricalcolo/richiesta di rete per ogni pixel di movimento), zoom con bottoni +/- e wheel, "centra sulla mia posizione" (riusa `getCurrentPosition()` esistente). Bottone "Apri mappa" (`#map-panel`) capability-gated come `#location-registry-panel` — nascosto finché `GET /api/map-info` non risponde (mai un 404), mai dentro un `<details>` collassato.

    **Due giri di revisione** (`code-review`), 3 problemi reali trovati e corretti in totale:
    - **Giro 1** (1 problema, serio): `GET /api/map-tiles/:z/:x/:y` non autenticato, senza alcun rate limit, con `z`/`x`/`y` validati solo come interi non negativi (nessun limite legato al range reale del file) mentre `MbtilesReader.getTile()` è una query `node:sqlite` sincrona sul singolo thread di Node — la specifica MBTiles non garantisce un indice su `(zoom_level, tile_column, tile_row)`, quindi senza un tale indice una raffica di richieste fuori range (garantite a vuoto) forzerebbe una scansione completa della tabella ogni volta, potenzialmente bloccando l'intero event loop per chiunque. Corretto con due difese complementari: (1) `z`/`x`/`y` validati contro `minzoom`/`maxzoom` dichiarati dal file e contro `[0, 2**z)`, a costo zero di query, prima di toccare il database; (2) un rate limit dedicato per IP sorgente (`checkMapTileRateLimit()`, `MAX_MAP_TILE_REQUESTS_PER_WINDOW` = 300/10s, `BoundedFifoMap` senza `evictionScore` — un IP non è un'identità mesh fabbricabile a costo zero come un `fromNodeId`, quindi un limite per-IP da solo è già significativo, a differenza del solo tetto per-`fromNodeId` di `InternetGateway`).
    - **Giro 2, mirato al fix sopra** (2 problemi): (1) il client (`mapview.js`'s `clampZoom()`) non aveva un tetto assoluto di zoom indipendente da `mapInfo.maxzoom` — un valore anomalo nella tabella `metadata` del file (file malformato, o un attaccante on-path su HTTP semplice) avrebbe permesso allo zoom di salire abbastanza da far traboccare `Math.pow(2, zoom)` a `Infinity`, mandando in loop infinito il ciclo di rendering delle tile (stessa classe di overflow che il tetto `z<=30` lato server esiste apposta a prevenire, mai applicata lato client). Corretto con `ABSOLUTE_MAX_ZOOM = 22`, indipendente da qualunque valore dichiarato dal file. (2) Il parsing del path in `handleGetMapTile()` trattava un segmento vuoto (es. `/api/map-tiles/10/512/`, slash finale) come `y=0` invece che come malformato — `Number("")` è `0`, non `NaN`, lo stesso "difetto" già corretto una volta in `map-tiles.ts`'s `parseFiniteInt()` ma non riusato qui, in un file diverso. Corretto con un controllo esplicito su segmenti vuoti prima della conversione numerica. Trovato anche autonomamente, durante lo sviluppo (prima di qualunque revisione): lo stesso identico difetto `Number("")===0` in `parseBounds()` (una virgola vuota nel valore `bounds` del file veniva letta come `0` invece di essere respinta).

    Test: `tests/unit/map-tiles.test.ts` (16 test — lettura di una tile nota, tile assente, flip TMS↔XYZ verificato non per coincidenza ma leggendo a un indice TMS *non* flippato e controllando che fallisca, parsing di metadata con valori mancanti/malformati incluso il caso `Number("")`, formati raster riconosciuti, formato vettoriale `pbf` rifiutato, file senza tabella `tiles`/`metadata`, file inesistente, file non-SQLite), `tests/integration/map-tiles-web-ui.test.ts` (21 test — `/api/map-info`/`/api/map-tiles` 404 quando non configurato, byte esatti serviti con `Content-Type`/`Cache-Control` corretti, tile assente, path malformati incluso lo slash finale, bounds z/x/y fuori range dal file senza mai toccare il database, rate limit per-IP raggiunto dopo `MAX_MAP_TILE_REQUESTS_PER_WINDOW` richieste, CORS con `mapTiles` da solo). 615 test totali (suite intera) rieseguiti ripetutamente sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti.

    Verificato anche manualmente end-to-end contro `node/src/cli.ts --map-file ...` reale: byte esatti recuperati via `curl` su un file MBTiles fittizio costruito ad-hoc, poi con Playwright contro un `NomadNode`/`WebUiServer` reali e la UI mobile reale — bottone "Apri mappa" nascosto/visibile correttamente in base a `--map-file`, overlay a schermo intero che si apre/chiude, pan via trascinamento e zoom via bottone senza errori, e (dopo aver corretto un mismatch nella fixture di test stessa, non nel codice — le coordinate della tile fittizia non corrispondevano al centro geografico dei `bounds` dichiarati) una tile reale confermata caricata con `200` esattamente alle coordinate attese (`z=10/x=539/y=366`, calcolate indipendentemente dalla stessa formula di `mapview.js` e verificate combaciare) dopo aver navigato la mappa fino a quella zona, non solo osservando `<img>` generici in pagina. Come per Kiwix/News/Internet-fetch: non verificabile contro un file MBTiles regionale reale (nessun accesso a internet reale in questa sandbox), solo contro fixture minimali.

47. **Bacheca "drop" mesh-native** (`node/src/drops.ts` — richiesta esplicita dell'utente il 29 agosto 2026: "possiamo pensare di implementare una funzionalità come la 'drop' di bitchat, per lasciare dei messaggi o contenuti in un luogo fisico preciso [...] verifica le repo dei progetti Nomad e Bitchat [...] cita sempre le fonti"). Concetto ispirato a `BoardManager` di BitChat (`bitchat/Services/Board/BoardManager.swift`, https://github.com/permissionlesstech/bitchat, licenza Unlicense/dominio pubblico) — **non** `LocationNotesManager`, la loro seconda feature con un nome simile ma che dipende da relay Nostr esterni + Tor, scartata esplicitamente come riferimento perché contraria alla premessa "nessuna dipendenza da Internet" di Nomad-Net; vedi `docs/reuse-vs-new.md` per la voce dedicata nella tabella di attribuzione. Repository verificate entrambe di persona (clonate localmente, LICENSE letta) prima di scrivere codice — Project NOMAD (Apache 2.0) non ha una feature analoga, nessuna citazione dovuta lì. **Nessuna riga di codice riusata, solo il concetto**, poi rimappato quasi interamente su primitive che Nomad-Net aveva già costruito per altre feature — questa non è nata da zero, è per lo più wiring:

    | Pezzo di `BoardManager` | Equivalente Nomad-Net riusato |
    |---|---|
    | Post firmato, propagato in mesh | `publishContent()` + `CONTENT_ANNOUNCE` (nessun nuovo `MessageType`) |
    | Scadenza automatica | `ContentMetadata.expiresAt`, scadenza pigra (stessa convenzione di `ContentStore`/`RemoteCatalog`) |
    | Flag "urgent" che salta la coda | `Priority.EMERGENCY`, stesso meccanismo di `service://emergency-news` (voce #39) |
    | Convenzione di naming | Stesso schema di `channelContentName()` (voce #40), qui un nome fisso `"drop"` (non un prefisso — l'identificatore è già il `contentId`, non serve un nome per istanza) |
    | Tombstone/cancellazione firmata | **Non implementato in v1** — il modello di contenuto di Nomad-Net è immutabile per costruzione ovunque, nessun precedente di "delete" nel codice; un drop scade e basta |

    Due decisioni di design chiuse con l'utente prima di scrivere codice (`AskUserQuestion`): (1) posizione = lat/lon dirette nel payload firmato (come `location-registry.ts`, voce #44) + etichetta libera, niente encoder geohash — la mesh stessa è già il filtro geografico, a differenza di BitChat che usa il geohash per instradare su relay Nostr globali; (2) i drop urgenti usano `Priority.EMERGENCY` sulla mesh (non solo un badge visivo lato UI), scelta che ha reso necessario un rate limit dedicato (sotto).

    **Backend** (`node/src/drops.ts`, nuovo, mirror di `public-channels.ts`): `DROP_CONTENT_NAME = "drop"`, `extractDropPayload()` (stesso schema difensivo di `extractChannelMessagePayload()`/`extractLocationReport()` — text 1-`MAX_MESSAGE_TEXT_LENGTH`, lat/lon negli stessi bound di `shareLocation()`, label opzionale bounded, urgent booleano, timestamp finito), `Drops` (`BoundedFifoMap` con eviction pesata sulla fiducia, scadenza pigra in `list()`, un solo asse bounded — a differenza di `PublicChannels` non c'è raggruppamento per canale). `NomadNode.publishDrop()` (`node.ts`): valida text/lat/lon/label con gli stessi bound, `expiresInMs` di default `DEFAULT_DROP_TTL_MS` (24h) tetto `MAX_DROP_TTL_MS` (72h) — un drop è legato a un momento, mai permanente come un canale; se `urgent`, consuma un rate limit dedicato (`MAX_URGENT_DROPS_PER_WINDOW` = 3 per `URGENT_DROP_RATE_LIMIT_WINDOW_MS` = 5 minuti, contatore a finestra scorrevole **non partizionato per chiamante** — a differenza del limiter per-IP di `InternetGateway`, qui il chiamante è già autenticato dalla password di rete su `POST /api/drops`, quindi un contatore unico per nodo è già sufficiente, nessuna identità fittizia a costo zero da ruotare). `considerDrop()`: fetch reattivo bounded (`MAX_CONCURRENT_DROP_FETCHES` = 16, budget indipendente da `MAX_CONCURRENT_CHANNEL_FETCHES` — un burst su una feature non deve mai affamare l'altra), agganciato sia a `handleContentAnnounce()` sia a `handleSyncResponse()`. `web-ui.ts`: `GET /api/drops` non autenticato (contenuto pubblico della mesh, stesso livello di `/api/channels`), `POST /api/drops` dietro password di rete (stesso schema di `POST /api/channel-messages`), 429 (non 400) quando scatta il rate limit sugli urgenti. UI mobile (`mobile/www/`): nuovo pannello "Bacheca" sempre visibile (mai capability-gated, a differenza dei pannelli mappa/registro posizioni — `GET /api/drops` è offerto incondizionatamente su ogni nodo), form di creazione con posizione sempre presa da `getCurrentPosition()` (mai inserita a mano, stessa scelta di `shareLocation()`), lista con badge urgente in cima e distanza dalla propria posizione (nuovo `haversineDistanceMeters()`, formula standard). `mapview.js` guadagna un secondo layer (`#map-pins-layer`), sincronizzato con la trasformazione di pan/zoom del layer tile esistente, che disegna i drop noti come pin sulla mappa offline.

    **Prima revisione** (`code-review --level xhigh`), 2 problemi reali trovati e corretti:
    - **`publishDrop()` consumava il rate limit sui drop urgenti PRIMA di validare `expiresInMs`**: un `expiresInMs` non positivo/non finito produceva un `ttlMs` altrettanto non valido, che faceva fallire silenziosamente `publishContent()` (l'`expiresAt` risultante era già scaduto, `ContentStore.putVerified()` lo rifiuta) — ma solo *dopo* che il contatore `urgentDropWindow` era già stato incrementato. Un chiamante già autenticato (la password di rete, esattamente la minaccia che il commento del rate limit nomina esplicitamente: "a misbehaving or compromised paired client in a tight loop") poteva quindi esaurire l'intero budget `MAX_URGENT_DROPS_PER_WINDOW` con richieste che non pubblicavano mai nulla, bloccando per 5 minuti anche segnalatori di emergenza legittimi. Corretto spostando la validazione di `expiresInMs` (finito, positivo) prima del blocco del rate limit. Nuovi test di regressione in `tests/integration/drops.test.ts` (validazione estesa con i casi `expiresInMs`; nuovo test dedicato che esaurisce 10 tentativi con `expiresInMs: -1` e prova che il budget urgente resta intatto per 3 drop genuinamente validi) e un caso aggiunto in `tests/integration/drops-web-ui.test.ts`.
    - **Il gate CORS non copriva il nuovo endpoint sempre-attivo**: `Access-Control-Allow-Origin`/la risposta al preflight `OPTIONS` erano condizionati a `allowServiceCalls || exposeLocationRegistry || mapTiles !== undefined` — le tre feature opzionali che finora erano le uniche a giustificare un client cross-origin. `GET`/`POST /api/drops` rompe quell'assunzione: è offerto incondizionatamente su ogni nodo, senza un proprio flag di opt-in — un nodo avviato senza nessuna delle tre flag (forma di deployment legittima, es. un nodo bacheca di sola lettura) avrebbe comunque bloccato silenziosamente via CORS il proprio pannello Bacheca sempre visibile per l'unico client cross-origin che conta (l'app mobile) — stessa classe di bug già trovata e corretta due volte in precedenza per `exposeLocationRegistry` (voce #44) e `mapTiles` (voce #46) quando *loro* sono stati aggiunti. Corretto rendendo incondizionati sia l'header CORS sia la risposta al preflight `OPTIONS` (204 per qualunque path, dato che esiste sempre almeno un endpoint pensato per essere cross-origin) — CORS non è mai stato un confine di sicurezza per gli endpoint di scrittura (protetti indipendentemente da `isAuthorized()`/confronto a tempo costante sul Bearer token), quindi renderlo incondizionato non indebolisce nulla. Tre test preesistenti che asserivano il vecchio comportamento condizionato aggiornati per riflettere il cambiamento intenzionale (`tests/integration/web-ui.test.ts` × 2, `tests/integration/map-tiles-web-ui.test.ts` × 1).

    Verificato anche end-to-end con Playwright: due nodi `NomadNode`/`WebUiServer` reali connessi via mesh (uno con `MbtilesReader` configurato su una fixture MBTiles minimale), un drop urgente creato dalla UI mobile su un nodo compare quasi subito nella bacheca del secondo nodo (propagazione via `CONTENT_ANNOUNCE`, nessuna query esplicita), badge "Urgente" corretto su entrambi, pin visibile sulla mappa offline del primo nodo, pannello mappa correttamente nascosto sul secondo nodo (nessun `--map-file`). 647 test totali (suite intera) rieseguiti ripetutamente (3 volte dopo l'implementazione, di nuovo dopo i fix di revisione), sempre verdi; `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti in entrambi gli stati.

## Nota sui test di scala: soglia TTL ricalibrata due volte

`tests/network/simulate.test.ts`'s "a TTL shorter than the mesh's depth leaves the farthest nodes legitimately unreachable" non verifica una proprietà di sicurezza, ma è comunque rilevante qui: il suo margine numerico è dovuto essere aumentato due volte in questa sessione (prima per il routing a costo, poi per il retry verso il prossimo fornitore) perché entrambe le funzionalità rendono la propagazione del contenuto più efficiente, spostando il punto in cui un nodo lontano riesce comunque a trovare una copia in cache entro il budget TTL. Ora usa una distanza volutamente enorme rispetto al TTL (~5x) invece di un margine "appena sufficiente", proprio per non dover essere ricalibrato una terza volta a ogni miglioramento futuro dell'efficienza della mesh.

## Direzione per l'implementazione futura

Da studiare come riferimento di design (non da importare come codice, vedi §52 e `reuse-vs-new.md`): il modello di identità a chiavi crittografiche e sessioni sicure basate su Noise usato da BitChat (https://github.com/permissionlesstech/bitchat/blob/main/WHITEPAPER.md).

**Rotazione del peer ID** (nota aggiunta il 30 agosto 2026 su richiesta esplicita dell'utente — "esplora se ci sono aspetti da approfondire", non un'implementazione): letto `docs/PEER-ID-ROTATION.md` di BitChat (`https://github.com/permissionlesstech/bitchat`, licenza Unlicense/dominio pubblico, stessa attribuzione di ogni altro riferimento BitChat in questo documento). Il problema che risolve: un osservatore passivo (una radio BLE in ascolto, senza partecipare attivamente) può assegnare a un telefono un identificatore stabile e ricostruire *chi era vicino a chi* nel tempo, perché BitChat annuncia in chiaro un id derivato dalla chiave statica (`SHA-256(chiave)[0..8]`, mai ruotato) insieme a una lista di fino a dieci vicini diretti. La soluzione proposta (non ancora spedita nemmeno in BitChat, solo una bozza di design con codice di supporto già scritto) fa ruotare l'id ogni ora, deriva la rotazione dalla chiave **privata** (mai dalla pubblica, altrimenti chiunque abbia visto la chiave potrebbe calcolare ogni id passato e futuro), e — punto centrale — deve *reintrodurre* un meccanismo di legame tra il nuovo id rotante e la chiave a lungo termine, dimostrato dentro un handshake Noise completato, perché altrimenti un id rotante non è collegabile a nessuna identità verificabile.

**Perché portare questo in Nomad-Net non è un'operazione equivalente, e probabilmente più grande**: in questo progetto `nodeId` **è** la chiave pubblica Ed25519 grezza in hex (`identity.ts`, `Identity.fromKeyObjects()`), non un hash troncato — ancora più direttamente esposto del già-permanente id di BitChat — ed è usato come chiave primaria in praticamente ogni struttura dati del codice (routing, fiducia, directory dei peer, `publisherId` di ogni contenuto/drop/messaggio di canale, cronologia messaggi, gruppi, registro posizioni...). `packet.source`, inoltre, è già documentato come **mai autenticato crittograficamente** (vedi "Binding crittografico" più sopra) — un limite noto e accettato, diverso dal punto di partenza di BitChat, dove `peerID == hash(chiave statica)` è invece un invariante *rinforzato* in due punti del codice prima di questo lavoro. BitChat può permettersi di far ruotare l'id perché ha già, oggi, un vero handshake (Noise XX) dentro cui reintrodurre un legame firmato tra id rotante e chiave stabile; Nomad-Net non ha un passaggio equivalente — l'HELLO di `transports/tcp.ts` non è un handshake di autenticazione, è solo lo scambio iniziale di `nodeId`. Una rotazione dell'id qui richiederebbe prima di progettare quel passaggio mancante, non solo la rotazione in sé.

**Un parallelo più economico, già presente (dormiente) in Nomad-Net, degno di nota a parte**: la sezione 9 del documento BitChat separa esplicitamente la rotazione (grande, costosa) da mitigazioni "a parte, indipendenti, più economiche" — tra cui **eliminare la lista dei vicini diretti dall'annuncio**, perché rivela l'intero grafo di adiacenza a chiunque ascolti. `NomadNode.sharePeerList()`/`MessageType.PEER_LIST` (`node.ts`) *potrebbe* fare esattamente questo se mai venisse invocato in broadcast (`destination` omesso): annuncerebbe l'elenco dei peer conosciuti **con il loro indirizzo di rete**, non solo l'id — stesso identico rischio nominato da BitChat, per di più con un dato aggiuntivo (l'indirizzo) che BitChat stesso non include. **Verificato per scrivere questa nota**: `sharePeerList()` non ha oggi alcun chiamante interno né in `node.ts` né altrove nel codice — è un metodo pubblico esposto ma mai agganciato automaticamente a nessun trigger della mesh, quindi il rischio è latente/potenziale, non un comportamento osservabile nella rete così com'è. Non è detto sia comunque un problema per gli scenari prioritari di Nomad-Net (rifugio, evento, scuola — contesti dove chi si connette alla stessa rete Wi-Fi/mesh è già visibile fisicamente, diverso dal caso di un manifestante intercettato da un ascoltatore radio anonimo, il caso per cui BitChat è stato progettato) — ma se in futuro qualcosa iniziasse a chiamare `sharePeerList()` in broadcast, varrebbe la pena rivalutare se includere l'indirizzo di rete di ogni peer sia davvero necessario a chi riceve l'annuncio.

**Non pianificato, nessuna decisione presa**: entrambi i punti restano riferimento di design per una sessione futura, da riprendere solo dopo una discussione esplicita con l'utente su se il modello di minaccia di Nomad-Net (diverso da quello di BitChat) giustifichi il costo — coerente con la disciplina già in uso in questo repository di non implementare mai codice ispirato a BitChat senza prima una decisione esplicita e una citazione della fonte.

Modello zero-trust previsto (§52):

```
Sender -> encrypt -> Relay (opaque packet) -> Relay -> Receiver -> decrypt
```

Un relay instrada senza poter leggere il payload di un `DATA` o `CONTENT_CHUNK` privato. Questo resta da fare: oggi l'autenticità (firma) è garantita, la riservatezza no.
