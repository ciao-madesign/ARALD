# Nomad-Net — contesto per Claude Code

Questo file viene letto automaticamente a ogni nuova sessione in questo repository. Obiettivo: far ripartire un nuovo sviluppatore (umano o Claude) senza dover rileggere l'intera cronologia della chat.

## Cos'è questo progetto

Nomad-Net è un prototipo software di rete distribuita, **content-centric** e **delay-tolerant**: dispositivi condividono contenuti, servizi e messaggi senza infrastruttura Internet, tramite mesh locali, store-and-forward, caching opportunistico e sincronizzazione automatica al ritorno della connettività. Ispirato concettualmente a BitChat (mesh BLE) e pensato per integrarsi con Project NOMAD come "service provider" locale (Kiwix, Ollama, ecc.), ma senza dipendere da nessuno dei due.

**Specifica completa e single source of truth**: [`docs/SPECIFICATION.md`](docs/SPECIFICATION.md) (in italiano, numerata per paragrafi §N — tutti i riferimenti nel codice e nei commenti usano questa numerazione). Se qualcosa nel codice sembra contraddire questo file, la specifica vince; se manca un dettaglio, cercalo lì prima di indovinare.

**Repository GitHub**: `ciao-madesign/nomad-net`. **Branch di lavoro**: `claude/nomad-net-project-spec-lcnbqt`.

## Regola permanente: push su main

**Istruzione permanente data dall'utente in questa sessione: ogni commit va pushato sia sul branch di lavoro sia direttamente su `main`.** Non è implicita nel workflow standard di Claude Code (che normalmente lavora solo sul branch dedicato) — è stata data esplicitamente e va rispettata per ogni futuro commit, non solo per quelli già fatti. Verifica sempre che `main` sia allo stesso commit del branch di lavoro prima di pushare (fast-forward), altrimenti chiedi come procedere invece di forzare.

## Stato del progetto

Milestone 0-7, 12, 13, 15, 16, 20 della roadmap sono complete (identità crittografica, protocollo a pacchetti, transport TCP, routing multi-hop con controlled flooding + distance-vector a costo, content discovery/cache, store-and-forward, partition sync, firma dei contenuti, trust levels, rate limiting, E2E encryption, relay policy legata a batteria, simulatore di rete). Dettaglio completo in [`docs/roadmap.md`](docs/roadmap.md).

Dopo il completamento delle milestone sopra è stato eseguito un **audit tecnico completo** (test, sicurezza, qualità del codice — vedi [`docs/audit-report.html`](docs/audit-report.html), un Artifact pubblicato e aggiornabile) che ha identificato ulteriori miglioramenti realizzabili in puro software. Sono stati realizzati come una sequenza di **9 slice pianificate, più una decima aggiunta successivamente su richiesta esplicita dell'utente**, ciascuna con lo stesso workflow a "doppio check" (vedi sotto), tracciate in `docs/security.md` sotto "Bug corretti nel seguito dell'audit":

| # | Slice | Stato |
|---|---|---|
| 1 | Retry al prossimo content provider quando quello attivo tace | ✅ Fatto |
| 2 | `BoundedFifoMap` condivisa (rimossa duplicazione 5x) | ✅ Fatto |
| 3 | Eviction pesata sulla fiducia per `PeerDirectory`/`RemoteCatalog` | ✅ Fatto |
| 4 | Scheduling reale basato su priorità in `TcpTransport` | ✅ Fatto |
| 5 | Pacchetto `CONTENT_NOT_FOUND` + scadenza dei contenuti | ✅ Fatto |
| 6 | Protocollo di service discovery (`SERVICE_*`) | ✅ Fatto |
| 7 | Interfaccia web locale di stato/ricerca (spec §59) | ✅ Fatto |
| 8 | Transport BLE simulato | ✅ Fatto |
| 9 | Gateway NOMAD mockato (contro un fake server locale) | ✅ Fatto |
| 10 | Gateway AI mockato (`service://ai`, spec §37 — contro un fake Ollama locale) | ✅ Fatto |

**Le 9 slice pianificate sono tutte completate, più la decima.** Ogni slice è documentata in dettaglio in `docs/security.md` (numerate progressivamente, bug #11-20) e spesso anche in `docs/protocol.md`. **Leggi quelle voci prima di toccare il codice corrispondente** — spiegano non solo cosa è stato fatto ma perché, inclusi i bug trovati dalla revisione e corretti prima di considerare ogni slice conclusa.

Dopo la chiusura delle 10 slice, il lavoro è proseguito con una serie di follow-up sempre su richiesta esplicita dell'utente, stesso workflow a doppio check, tutti documentati in `docs/security.md` con voci numerate progressive (#21-47): interfaccia web ampliata (#21), endpoint di scrittura `POST /api/call` (#22), app mobile Fase 1 (#23), pairing in stile Wi-Fi (#24), pairing via QR code (#25), una passata libera di qualità/sicurezza su `node/src/` che ha corretto un DoS non autenticato reale (#26), `service://news` (`NewsGateway`) + una dashboard mobile in stile "motore di ricerca" (#27), una passata di design completa sulla UI mobile (#28, icone SVG coerenti, gerarchia visiva, transizioni, skeleton, feedback tattile, layout tablet, accessibilità), lo spostamento del pannello Servizi in cima alla dashboard con rimozione dei collegamenti rapidi (#29, follow-up immediato di #28), la correzione di un bug reale di icone tagliate (viewBox mancante) più tile dei servizi uniformi via CSS Grid e sezioni tecniche collassabili con `<details>` (#30, terzo giro sulla stessa passata), una nuova identità visiva completa "Waypoint" — brand book (Artifact pubblicato, non nel repository) più applicazione concreta a `mobile/www/`: palette topografica con un colore d'azione usato con parsimonia, nuovo marchio, angolo tagliato come firma strutturale, quadrante "condizione del sentiero" al posto della griglia di statistiche (#31, rifacimento del linguaggio visivo su richiesta esplicita dell'utente, non un'altra iterazione sulle voci #28-30), il limite di dimensione su `ContentStore` (#32, `node/src/content.ts`, `BoundedFifoMap` con eviction pesata sulla fiducia più un caso speciale per il contenuto pubblicato localmente — con ricadute corrette anche su `gateway/nomad/` (`KiwixGateway`/`NewsGateway`/`cli.ts`), i cui commenti assumevano un `ContentStore` illimitato), un parser RSS/Atom scritto da zero (#33, `gateway/nomad/rss-feed.ts`) a sostituzione dell'API JSON ad-hoc mai reale di `NewsGateway` — pezzo 1 di 4 della proposta "Nomad News evoluto" (`docs/next-steps.md` Opzione I), quattro giri di revisione, l'ultimo dei quali ha trovato un vero DoS a tempo quadratico nella gestione di sezioni CDATA annidate, il meccanismo di annuncio broadcast per contenuto singolo (#34, `MessageType.CONTENT_ANNOUNCE`, parallelo a `SERVICE_ANNOUNCE` — prerequisito condiviso da "Nomad News evoluto" pezzo 4 e dai canali pubblici di chat, sviluppato seguendo un piano d'esecuzione a slice pianificato ed esplicitamente approvato con l'utente prima di iniziare, in una sessione parallela a questa), i livelli headline/summary per `NewsGateway` (#35, `gateway/nomad/news-gateway.ts` — pezzo 2 di 4 della stessa proposta, sviluppato in questa sessione in parallelo alla #34: headline piccola sempre sincronizzata più un corpo articolo separato recuperato on-demand via `CONTENT_REQUEST`, motivato dall'efficienza su link a banda stretta), la chat 1:1 (#36, `node/src/message-history.ts` + `GET`/`POST /api/messages` in `web-ui.ts` + UI mobile sopra "Waypoint" — Slice 2a dello stesso piano che ha prodotto la #34; il backend di rete/cifratura esisteva già, mancava solo una cronologia persistita lato gateway e la UI), il digest AI per `service://news` (#37, `NewsGateway.generateDigest()` — pezzo 3 di 4, ultimo pezzo backend della proposta news, compone `NewsGateway` con `service://ai` tramite `node.callService()` senza mai un riferimento diretto ad `AiGateway`; due giri di revisione, 7 problemi reali trovati e corretti in totale, incluso l'assenza di una guardia anti-superamento come `syncGeneration` e un titolo di headline con newline che poteva iniettare una riga falsa nel prompt), un fix indipendente in `NomadNode.callService()` (#38, `node/src/node.ts`: il percorso locale ignorava `options.timeoutMs` in silenzio, corretto con un nuovo `withServiceTimeout()`/`pendingLocalServiceCalls` — trovato durante uno sviluppo parallelo del digest AI andato poi scartato in favore della #37, vedi la nota di processo in fondo alla voce #38 di `docs/security.md`), `service://emergency-news` (#39, `NewsGateway`, ultimo pezzo di "Nomad News evoluto" — un classificatore `isEmergencyHeadline` (default: parola chiave nel campo `category`, sovrascrivibile) marca una headline nuova/cambiata per la pubblicazione con `{ announce: true, priority: Priority.EMERGENCY }` invece del default, "saltando la coda" via `CONTENT_ANNOUNCE`; due giri di revisione, 4 problemi reali trovati e corretti, incluso un tetto mancante sul numero di annunci per sync — senza, un feed ostile avrebbe potuto far floodare la mesh a priorità EMERGENCY senza alcun limite, dato che il rate limiting esistente protegge solo i pacchetti ricevuti, mai quelli che un nodo origina da sé), i canali pubblici di chat (#40, `node/src/public-channels.ts`, ultimo pezzo indipendente dell'Opzione J prima del tracciamento posizione/chat cifrate — convenzione sul nome `chat:<canale>` invece di un cambio di protocollo, stesso impianto backend/UI della chat 1:1 sopra `CONTENT_ANNOUNCE`; due giri di revisione, 8 problemi reali trovati e corretti in totale, incluso un tetto mancante sui fetch reattivi per un burst di entry fasulle — stessa classe di amplificazione della #39 ma sul lato ricezione — e un vero loop infinito nell'helper condiviso `pushBounded()` per un `maxLength` negativo, introdotto dal fix di un altro problema dello stesso giro), un follow-up mirato sulla #39 (#41, `announcedEmergencyById`: la decisione di annuncio era gated su `publishedById`, condiviso con ogni headline di routine e non solo quelli di emergenza — un volume sufficiente di headline ordinari poteva far scadere per eviction FIFO la entry di un bollettino invariato e ri-annunciarlo, anche senza il riavvio del processo che la voce #39 aveva documentato come unico trigger; corretto con una mappa dedicata più piccola), le chat private/di gruppo cifrate (#42, `node/src/groups.ts` — ultimo pezzo indipendente dell'Opzione J, approvato dopo una fase di planning esplicita con l'utente: membership fissa alla creazione, nessuna operazione di uscita/rimozione membro né rotazione di chiave in questa v1, scelta esplicita dell'utente tra questo ambito ristretto e un ciclo di vita completo con forward secrecy; inviti come `PRIVATE_MESSAGE` 1:1 riusando l'infrastruttura esistente, messaggi come nuovo `MessageType.GROUP_MESSAGE` broadcast cifrato con AES-256-GCM più una firma Ed25519 del mittente — necessaria perché una chiave di gruppo condivisa rende l'auth tag GCM da solo insufficiente contro l'impersonificazione interna al gruppo; due giri di revisione, 2 problemi reali trovati e corretti, incluso un vero buco di replay — `GROUP_MESSAGE` non aveva alcuna dedup per contenuto, solo `SeenCache` per `packet.id` casuale), `service://translation` più il redirect automatico captive portal (#43, `gateway/nomad/translate-gateway.ts` + `node/src/web-ui.ts` — due pezzi piccoli e indipendenti scelti insieme dall'utente: un servizio di traduzione che compone `service://ai` con una lista fissa di 5 lingue, e un blocco di route sempre attivo in `web-ui.ts` per i path "sonda" che iOS/Android/Windows/Firefox interrogano dopo essersi collegati a un Wi-Fi, così l'app/pagina del nodo si apre da sola come su un Wi-Fi da hotel — la configurazione DNS-hijack lato hostapd/dnsmasq che lo rende possibile è documentata in `docs/guida-hardware-rifugio.md`, fuori da questo npm workspace; due giri di revisione, 3 problemi reali trovati e corretti, incluso un bypass della validazione `targetLanguage` via proprietà ereditate di `Object.prototype` che finivano stringificate nel prompt inviato all'AI), il tracciamento posizione opportunistico (#44, `node/src/location-registry.ts` — preceduto da una conversazione esplicita con l'utente su consenso/conservazione/autorizzazione: opt-in per invio, solo l'ultima posizione nota per mittente, lettura riservata a operatori fidati; un controllo TrustLevel per l'ultimo punto si è rivelato aggirabile durante l'esplorazione tecnica — `fromNodeId` di una `SERVICE_REQUEST` non è autenticato — risolto con un nodo registro dedicato e password di rete propria, riusando il meccanismo HTTP già esistente per `/api/groups` invece di un nuovo protocollo mesh-wide; report scritti come `PRIVATE_MESSAGE` (mai `publishContent()`), `LocationRegistry` a sovrascrittura con scadenza pigra opzionale, nuova UI mobile "Condividi la mia posizione"/"Posizioni condivise", prima dipendenza esterna mobile genuinamente necessaria (`@capacitor/geolocation`, a differenza del QR scritto da zero); tre giri di revisione, 3 problemi reali trovati e corretti — mancanza di una guardia contro un report più vecchio che sovrascrive uno più recente (rilevante per store-and-forward fuori ordine), un timestamp futuro fabbricato che poteva avvelenare permanentemente uno slot (introdotto dal fix precedente), un clock-rollback locale documentato come limite accettato invece di essere risolto), e `service://internet-fetch` (#45, `gateway/nomad/internet-gateway.ts`, `InternetGateway` — "Internet senza Internet", Opzione K: su richiesta esplicita dell'utente di poter raggiungere Internet reale attraverso la mesh, valutata l'idea di un proxy HTTP aperto e scartata esplicitamente per il rischio di abuso/legale (paragonabile a un exit node Tor), sostituita con esattamente due `kind` di richiesta curati e diversamente sbarrati — `"rss"`, senza allowlist di dominio perché la barriera è il contenuto stesso (deve superare `parseFeed()`, già indurito in #33, altrimenti la richiesta fallisce e non pubblica nulla), e `"text"`, dietro un allowlist di host configurato solo dall'operatore del gateway, mai una lista precotta nel codice; nuovo `gateway/nomad/url-safety.ts` (`isPubliclyRoutableUrl()`) come guardia SSRF condivisa da entrambi i `kind` e applicata prima di ogni fetch in uscita — anche una richiesta `"rss"` senza allowlist di dominio farebbe comunque una sonda di rete reale dalla macchina dell'operatore se non fosse bloccata prima; limite noto e documentato (mai risolto): la risoluzione DNS della guardia e quella successiva di `fetch()` sono due lookup separati, un TOCTOU/DNS-rebinding teoricamente possibile, stessa postura di onestà già usata per `packet.source`; risposta come riferimento a contenuto (`publishContent()`, stesso pattern di `NewsGateway`) invece che inline, per restare dentro il limite pratico di un singolo pacchetto; rate limiting a due livelli, per chiamante e globale sulla mesh — il livello globale esiste perché `fromNodeId` non è autenticato crittograficamente, un limite per-identità da solo sarebbe aggirabile ruotando identità fittizie; nuovo `gateway/nomad/fetch-bounded.ts` (`fetchTextBounded()`, estratto da `news-gateway.ts` al secondo bisogno, stesso pattern già visto per `BoundedFifoMap`/`loopback-http-server.ts`); due giri di revisione, 7 problemi reali trovati e corretti in totale, incluso un ordine dei controlli sbagliato che permetteva a un flood di richieste strutturalmente destinate a fallire di esaurire il budget di rate limit prima di qualunque lavoro reale (corretto due volte, la seconda perché anche la guardia SSRF stessa costa un lookup DNS reale e va quindi dietro il rate limit, non solo il fetch finale) e un gap reale nel riconoscimento IPv6 della forma "IPv4-compatible" deprecata (`::a.b.c.d`, senza marcatore `ffff`) che il primo giro aveva lasciato passare come pubblica; sviluppata in una sessione parallela a quella che completava #42-44, integrata via merge, numerata #45 (non #42 come inizialmente assegnata) per la collisione — vedi la nota di processo in fondo a quella voce di `docs/security.md`, e le mappe topografiche offline (#46, `node/src/map-tiles.ts`, `MbtilesReader` — richiesta esplicita dell'utente, Opzione L: tile raster pre-renderizzate (es. OpenTopoMap, la precisione del rendering è responsabilità del progetto a monte, non nostra) in un file MBTiles/SQLite preparato una volta dall'operatore, letto via `node:sqlite` built-in di Node, nessuna nuova dipendenza; servite da un endpoint HTTP dedicato non autenticato (`GET /api/map-tiles/:z/:x/:y`, mai via `content://`/mesh — `web-ui.ts` non ha alcun modo di restituire byte di un contenuto via HTTP, solo metadata); `mobile/www/mapview.js`, visualizzatore di tile raster scritto da zero invece di Leaflet — niente accesso a internet reale in questa sandbox per vendorizzare una libreria, e nessuna dipendenza di terze parti in `mobile/www/` finora a parte `@capacitor/geolocation`; due giri di revisione, 3 problemi reali trovati e corretti, incluso un rischio di blocco dell'event loop (query `node:sqlite` sincrona dietro un endpoint non autenticato e senza rate limit) risolto con bound su z/x/y contro il range reale del file più un rate limit per IP sorgente, e la bacheca "drop" mesh-native (#47, `node/src/drops.ts` — richiesta esplicita dell'utente, Opzione M: una funzionalità ispirata a `BoardManager` di BitChat (`bitchat/Services/Board/BoardManager.swift`, https://github.com/permissionlesstech/bitchat, licenza Unlicense/dominio pubblico — verificata clonando la repository e leggendo la LICENSE prima di scrivere codice, mai `LocationNotesManager`, la loro seconda feature con nome simile ma dipendente da relay Nostr esterni + Tor, scartata come riferimento perché contraria alla premessa "nessuna dipendenza da Internet"; vedi `docs/reuse-vs-new.md` per la voce di attribuzione dedicata) per lasciare messaggi/contenuti legati a un luogo fisico preciso, mostrati a chi si connette alla mesh di quella zona: nessuna riga di codice riusata, solo il concetto, rimappato quasi interamente su primitive già esistenti — `publishContent()`+`CONTENT_ANNOUNCE` per il post firmato (zero nuovo `MessageType`), `ContentMetadata.expiresAt`/scadenza pigra per la scadenza automatica, `Priority.EMERGENCY` (stesso meccanismo di `service://emergency-news`, #39) per il flag "urgente" che salta la coda, la convenzione di naming di `channelContentName()` (#40) per riconoscere un drop; niente tombstone/cancellazione in questa v1, scelta esplicita coerente con l'immutabilità del modello di contenuto ovunque nel codice; nuovo pannello "Bacheca" sempre visibile in `mobile/www/` (mai capability-gated, a differenza di mappa/registro posizioni) con pin sui drop noti sulla mappa offline (`mapview.js`, #46); un giro di revisione, 2 problemi reali trovati e corretti — il rate limit sui drop urgenti (`MAX_URGENT_DROPS_PER_WINDOW`, non partizionato per chiamante perché `POST /api/drops` è già dietro la password di rete) veniva consumato prima di validare `expiresInMs`, permettendo di esaurirlo con richieste che non pubblicavano mai nulla; il gate CORS di `web-ui.ts` non copriva il nuovo endpoint sempre-attivo (stessa classe di bug già corretta due volte per `exposeLocationRegistry`/`mapTiles`), risolto rendendo CORS incondizionato su ogni risposta. Le voci #34 e #35 sono state sviluppate in parallelo da due sessioni diverse sullo stesso repository e integrate via merge — vedi la nota di processo in fondo alla voce #35 di `docs/security.md` per il dettaglio; le voci #37/#38, #39/#41 e #42/#45 condividono un'origine simile (collisioni tra sessioni parallele sulla stessa proposta o numerazione) — vedi le note di processo in fondo alle voci #38, #41 (anche per la collisione di numerazione con la #40, risolta rinumerando) e #45. **Leggi la voce corrispondente in `docs/security.md` prima di toccare il codice relativo** — stessa raccomandazione di sopra, vale per tutte le voci, non solo le prime 10.

**Nota per il futuro (annotata su richiesta esplicita dell'utente dopo l'approvazione di #31, non ancora pianificata in dettaglio)**: l'identità "Waypoint" applicata in #31 è approvata come direzione, ma l'utente ha segnalato che in futuro è previsto un **restyling grafico per rendere l'aspetto più moderno** — non una richiesta immediata, solo un punto da tenere presente per una prossima sessione di lavoro sull'estetica. Nessun dettaglio ulteriore raccolto finora (non è chiaro se si tratti di un affinamento della stessa identità Waypoint o di una direzione nuova) — da chiarire con l'utente quando si riprende questo filone.

Lo stato ad oggi (29 agosto 2026): **il piano d'insieme di `docs/next-steps.md` è completo per intero — non resta alcun candidato pianificato aperto.** Il debito di design della UI mobile è stato affrontato (#28-31, incluso il rebrand "Waypoint"); `ContentStore` è stato limitato per dimensione (#32); **"Nomad News evoluto" è completo per intero** (parser RSS/Atom + schema arricchito #33, meccanismo di priorità/broadcast #34, livelli headline/summary #35, digest AI #37, `service://emergency-news` #39 con un follow-up mirato #41); un bug indipendente sul timeout dei servizi locali è corretto (#38); **la messaggistica e il tracciamento posizione dell'Opzione J sono ora completi per intero** — chat 1:1 (#36), canali pubblici (#40), chat private/di gruppo cifrate (#42) e tracciamento posizione (#44) tutte fatte; **l'Opzione K ("Internet senza Internet", `service://internet-fetch`) è completa per intero** (#45, sviluppata in parallelo alle voci #42-44 in una sessione diversa, integrata via merge); un servizio di traduzione e il redirect automatico del captive portal sono stati aggiunti su richiesta esplicita dell'utente (#43, `gateway/nomad/translate-gateway.ts` + `node/src/web-ui.ts`, non parte del piano originario); **mappe topografiche offline** (#46, Opzione L, `node/src/map-tiles.ts` + `mobile/www/mapview.js`) aggiunte su richiesta esplicita dell'utente, anch'esse non parte del piano originario; **la bacheca "drop" mesh-native** (#47, Opzione M, `node/src/drops.ts` — ispirata a `BoardManager` di BitChat, Unlicense, vedi sopra) aggiunta su richiesta esplicita dell'utente, anch'essa non parte del piano originario. `docs/next-steps.md`, sezione "Piano d'insieme", conferma questo stato. Le uniche milestone ancora aperte nella loro forma *reale* (8/9/10/11/14/17/18/19, vedi `docs/roadmap.md`) restano bloccate su prerequisiti esterni (hardware fisico, Docker + Project NOMAD raggiungibile, accesso a internet reale) non disponibili in questo ambiente — e resta annotata, non pianificata in dettaglio, la nota sul futuro restyling grafico della UI mobile (vedi sotto).

## Priorità per chi riprende questo lavoro (handoff)

`docs/next-steps.md`, sezione "Piano d'insieme", è la fonte di verità — **leggila per intero prima di iniziare qualunque lavoro nuovo**. **Il piano non ha più candidati pianificati aperti**: tutte le fasi originarie sono state completate (#28-47, `docs/security.md`), incluso l'ultimo pezzo aggiunto — la bacheca "drop" mesh-native (#47).

Non c'è quindi un prossimo passo predeterminato da questa sessione. Chi riprende il lavoro dovrebbe:
1. **Backlog aperto immediato, dato dall'utente il 29 agosto 2026** ("procedi con tutto quello che hai detto", dopo la voce #47): peer-ID rotation, idea di routing Tor per `InternetGateway`, audit di `RequestSyncManager` (tutti e tre riferimenti di design BitChat, lette le rispettive `docs/*.md` clonate localmente), gateway CyberChef/FlatNotes (feature vere, richiedono pianificazione esplicita prima di scrivere codice). **Fatto**: la nota sulla rotazione peer-ID (`docs/security.md`, sezione "Direzione per l'implementazione futura", e `docs/reuse-vs-new.md`) — nessun codice scritto, solo analisi: conclude che portarla in Nomad-Net è un lavoro più grande che in BitChat (`nodeId` è già la chiave pubblica grezza usata come chiave primaria ovunque, e manca un handshake di autenticazione dentro cui reintrodurre un legame id-rotante/chiave-stabile), e segnala un parallelo più economico e dormiente (`NomadNode.sharePeerList()`/`PEER_LIST`, mai chiamato oggi ma includerebbe indirizzi di rete oltre agli id se mai attivato). **Ancora da fare**: gli altri tre punti, uno alla volta con ok esplicito tra l'uno e l'altro, stesso workflow di sempre.
2. **Hardware del "Nomad Node" — discussione in chat ora scritta nei documenti** (29-30 agosto 2026): l'utente preferirebbe un unico dispositivo tutto-in-uno (es. Orange Pi Zero 2W con Ollama incluso), ma è stato spiegato che la CPU (Cortex-A53, senza NPU/GPU utilizzabile) è probabilmente troppo debole per un LLM utilizzabile, non solo la RAM — punto **non ancora verificato con un benchmark hardware reale** (nessun accesso a hardware fisico in questo ambiente), vedi `docs/deployment.md` sezione "Non ancora pianificato in dettaglio". Un modello a due livelli (gateway economico sempre acceso + un "PC"/mini-PC separato per l'AI e i contenuti pesanti, comunicano sulla stessa rete locale del rifugio) è stato spiegato in dettaglio e piace come piano B; esplorate anche alternative più economiche di "gateway + PC intero" (scheda con NPU incorporato tipo Orange Pi 5/RK3588, complessità software reale perché Ollama non sa usare l'NPU così com'è; un mini-PC *usato/ricondizionato* invece che nuovo, l'unica vera opzione con RAM davvero espandibile via SO-DIMM; chiavette USB acceleratrici, scartate o dubbie — Google Coral non adatto a un LLM, Hailo-8 costoso). **Fatto**: `docs/deployment.md` (nuove sezioni "Il modello a due livelli per un rifugio" e "Alternative hardware più economiche", terminologia della "Configurazione target" del pilot rifugio riconciliata) e `docs/guida-hardware-rifugio.md` (nuova sezione 1c, un secondo dispositivo opzionale per l'AI, più una nota in "Uso quotidiano" su cosa succede se è spento) — entrambi scritti come riferimento/piano, non come scelta definitiva: il benchmark reale resta il prerequisito per decidere tra "tutto in uno" e i due livelli.
3. Considerare il punto annotato ma non ancora pianificato in dettaglio: un **restyling grafico della UI mobile** per un aspetto più moderno (segnalato dall'utente dopo #31, vedi sopra) — non è chiaro se sia un affinamento dell'identità "Waypoint" esistente o una direzione nuova, da chiarire con l'utente prima di iniziare.
4. In assenza di indicazioni, le uniche milestone rimaste in `docs/roadmap.md` (8/9/10/11/14/17/18/19) richiedono tutte un prerequisito esterno non disponibile in questo ambiente (hardware fisico, Docker + Project NOMAD raggiungibile, accesso a internet reale) — non pianificabili qui.

Qualunque lavoro sostanziale futuro (nuova feature, passata di qualità, restyling) resta comunque soggetto allo stesso workflow a doppio check sotto e a un ok esplicito dell'utente prima di iniziare.

## Workflow da riusare per lavoro futuro (slice, feature, o passate di qualità)

Il workflow concordato con l'utente, usato per ognuna delle 10 slice sopra e da riapplicare identico a qualunque lavoro sostanziale futuro in questo repository:

1. Implementa la feature.
2. Scrivi/aggiorna i test (unit + integration, seguendo le convenzioni esistenti — vedi sotto).
3. Fai girare l'intera suite più volte di fila (`npx vitest run`, ripetuto 3-15x a seconda della sensibilità a timing/race) per scovare flakiness prima che la trovi la revisione.
4. Invoca la skill `code-review` con un prompt dettagliato che spiega cosa è cambiato e dove concentrare l'attenzione — non un generico "rivedi questo diff".
5. Correggi i problemi reali trovati (non solo quelli comodi), aggiungi test di regressione dedicati per ciascuno, ri-verifica.
6. Aggiorna `docs/security.md` (nuova voce numerata) e `docs/protocol.md`/altri doc pertinenti.
7. `npx tsc --noEmit -p node/tsconfig.json` e `npm run build -w node` puliti.
8. Commit con messaggio dettagliato (cosa, perché, cosa ha trovato la revisione), push su **sia** `claude/nomad-net-project-spec-lcnbqt` **sia** `main`.
9. Report all'utente in italiano, **poi fermati e aspetta un "ok" esplicito prima di iniziare il prossimo pezzo di lavoro** — istruzione esplicita data dall'utente all'inizio dell'arco di 9 slice ("Dopo ogni step aggiornami e chiedimi ok per proseguire"), non legata alla singola serie di slice ormai conclusa: vale per qualunque lavoro sostanziale futuro allo stesso modo.

Non saltare il passaggio di code-review nemmeno quando il codice "sembra ovviamente corretto": in ogni slice fatta finora ha trovato almeno un problema reale (spesso più di uno) prima di considerare la slice conclusa — inclusi almeno due crash-DoS reali (accesso non protetto a un campo di payload potenzialmente assente) e diverse race condition/edge case di conteggio.

## Struttura del repository

```
nomad-net/
├─ docs/            specifica (SPECIFICATION.md) e documentazione tecnica
├─ node/src/        nomad-node: il runtime di rete (unico package con codice reale)
├─ tests/           unit/, integration/, network/ (vitest)
├─ tools/simulator/ simulatore di rete a scala (usato anche da `npm run simulate`)
├─ gateway/nomad/   gateway NOMAD (Slice 9-10 + voce #27/#33/#43/#45): KiwixGateway, AiGateway, NewsGateway, TranslateGateway, InternetGateway, rss-feed.ts, url-safety.ts, fetch-bounded.ts, fake server, cli.ts — mockato, non nel workspace npm
├─ mobile/          client mobile Capacitor verso un gateway (Opzione H Fase 1) — vedi mobile/README.md; mobile/ios/ ancora solo segnaposto
└─ protocol/        segnaposto definizioni di protocollo condivise — non contiene codice reale
```

Tutto il codice reale vive sotto `node/src/`. `gateway/nomad/` e `mobile/` (Android) hanno codice reale ma non sono nel workspace npm della radice (progetti separati con proprio `package.json`, come sopra). `protocol/` e `mobile/ios/` restano placeholder dalla specifica, non toccarli aspettandosi codice.

### App mobile (`mobile/`, Opzione H Fase 1)

Client Capacitor (JS/TS vanilla, no framework) verso un `WebUiServer` esistente via HTTP: setup/pairing poi dashboard (vicini/servizi/contenuti, chiamata servizi). Verificato end-to-end con un browser reale (Playwright) contro un `NomadNode` vero — non su un telefono fisico o un emulatore, entrambi non disponibili in questo ambiente. La build Android nativa (`mobile/android/`, generata da `npx cap add android`) **non compila in questa sessione**: richiede l'Android Gradle Plugin da `dl.google.com`, bloccato dalla policy di rete (403 dal proxy, verificato — non un problema di codice, non ritentare). Revisione dedicata con 6 problemi reali trovati e corretti (incluso un bug bloccante mai osservabile in un browser desktop: Android blocca di default il traffico HTTP non cifrato, corretto con un `network_security_config.xml`) — vedi `docs/security.md` voce #23 e `mobile/README.md` per il dettaglio completo.

Su richiesta esplicita dell'utente, il pairing è stato poi rifatto per sembrare una rete Wi-Fi invece di "indirizzo + token": "nome rete" (SSID-like) + "password" breve e digitabile (`generateNetworkPassword()`), indirizzo del gateway inserito una tantum e mai più mostrato in UI, credenziali esposte anche sulla pagina web del gateway (`GET /api/pairing`, deliberatamente non autenticato) oltre che in console. Stesso workflow a doppio check, 4 problemi reali trovati e corretti dalla revisione (incluso un bias reale nell'alfabeto della password, 31 simboli invece dei 32 dichiarati) — vedi `docs/security.md` voce #24.

Sempre su richiesta esplicita dell'utente ("implementa anche opzione con QR code"), aggiunta un'opzione di pairing via QR: `node/src/qrcode.ts` è un encoder QR (ISO/IEC 18004) scritto da zero — nessuna libreria QR nel progetto, coerente con la convenzione "niente dipendenze esterne non necessarie" — verificato durante lo sviluppo e la revisione contro un decoder indipendente (`jsQR`, solo in una directory di scratch, mai come dipendenza) su 100+ round-trip, trovando e correggendo 2 bug reali nel posizionamento di format/version-info (mai nella logica dati/Reed-Solomon). La dashboard del gateway mostra ora un QR accanto a nome-rete/password (`GET /api/pairing` esteso con `qrDataUri`, nuovo `WebUiOptions.publicHost` con auto-rilevamento LAN via `node:os.networkInterfaces()`); l'app mobile ha un bottone "Scansiona QR" dietro feature-detection di `BarcodeDetector` (nativo del browser, Chrome/Android WebView — non verificabile end-to-end in questa sessione, che gira su un Chromium headless senza quell'API). Stesso workflow a doppio check, 2 problemi reali trovati e corretti dalla revisione (una race di cancellazione durante `getUserMedia()` in sospeso che poteva lasciare la fotocamera accesa dopo "Annulla"; un campo estratto ma mai usato) — vedi `docs/security.md` voce #25.

Su richiesta esplicita dell'utente ("passata di qualità/sicurezza sul codice esistente", non seguito di una feature specifica), eseguita una revisione libera (`code-review --level xhigh`, senza un diff da rivedere) sull'intero `node/src/`. Ha trovato 3 problemi reali, tutti corretti: **un DoS non autenticato reale** — ogni handler `CONTENT_*` in `node.ts` accedeva a `packet.payload` senza alcuna guardia (a differenza di ogni altro handler), un singolo pacchetto malformato da un peer connesso bastava a crashare il processo, riprodotto empiricamente prima del fix; lo stesso bug nel case `PEER_LIST`; e `RoutingTable` senza eviction pesata sulla fiducia (a differenza di `PeerDirectory`/`RemoteCatalog`, Slice 3), corretta con un nuovo `RoutingTableOptions.trustRank` più un tetto `MAX_ROUTES_PER_ANNOUNCE` sulle entry di un singolo pacchetto. Stesso workflow a doppio check (inclusa una seconda revisione sul fix stesso, che ha trovato l'aggiornamento mancante di questo file/docs/security.md prima che venisse fatto) — vedi `docs/security.md` voce #26.

Su richiesta esplicita dell'utente ("un interfaccia che mostri i servizi disponibili come fosse un motore di ricerca, stile Google, con collegamenti rapidi ai servizi principali [...] esiste un servizio per le news?"), aggiunti `service://news` (`gateway/nomad/news-gateway.ts`, `NewsGateway` — terzo sotto-servizio NOMAD accanto a Kiwix/Ollama, senza fake server per scelta esplicita dell'utente, e non verificabile contro un backend reale in questa sandbox: confermato che l'accesso a internet reale è bloccato dalla policy di rete) e una dashboard mobile ristrutturata in stile "motore di ricerca" (wordmark + barra di ricerca + collegamenti rapidi ai servizi disponibili, `mobile/www/`). Stesso workflow a doppio check, 5 problemi reali trovati e corretti dalla revisione (il più serio: `publishedById` era una `Map` semplice illimitata, corretta con `BoundedFifoMap` — `ContentStore` stesso ha lo stesso limite ma non è stato toccato in questa slice, portata troppo ampia, resta candidato aperto in `docs/next-steps.md`) — vedi `docs/security.md` voce #27.

Tracciamento posizione (voce #44, ultimo pezzo dell'Opzione J): nuovo bottone sempre visibile "Condividi la mia posizione" (mai dentro un `<details>` collassato — stesso principio "un utente comune deve vedere subito cosa può fare" del pannello Servizi) e nuovo pannello "Posizioni condivise" (nascosto di default, visibile solo quando `GET /api/location-registry` non fa `404` — stesso degrado onesto già usato per il QR di `/api/pairing`). **Prima vera eccezione a "niente dipendenze esterne non necessarie"**: nuova dipendenza `@capacitor/geolocation` — a differenza del QR (scritto da zero apposta), non c'è un modo ragionevole di leggere il GPS hardware nativo senza un plugin; usata via `window.Capacitor.Plugins.Geolocation` quando presente (nessun bundler in `mobile/www/`, quindi nessun `import` statico possibile — lo stesso motivo per cui `SERVICE_ICONS`/`SERVICE_LABELS` restano duplicati a mano tra `gateway/nomad/` e qui), con fallback a `navigator.geolocation` del browser — quest'ultimo il percorso davvero verificabile in questa sandbox (nessuna build Android nativa disponibile), e verificato end-to-end con Playwright (geolocalizzazione mockata) contro due nodi reali connessi: dashboard su un gateway ordinario mostra il bottone e il pannello correttamente nascosto, un tap porta lo stato a "Posizione condivisa.", e ripaiando lo stesso telefono virtuale a un nodo registro dedicato (password separata) il pannello diventa visibile con la voce corretta.

Mappe topografiche offline (voce #46, Opzione L): nuovo `mapview.js`, un visualizzatore di tile raster (matematica Web Mercator/slippy-map standard) scritto interamente da zero — niente Leaflet/MapLibre/altra libreria, perché questo ambiente non ha accesso a internet reale per scaricarne/vendorizzarne una, e il progetto non ne ha mai introdotta una in `mobile/www/` a parte `@capacitor/geolocation` (eccezione motivata dall'hardware, non applicabile qui). Bottone "Apri mappa" (`#map-panel`) capability-gated come `#location-registry-panel` (nascosto finché `GET /api/map-info` non risponde), overlay a schermo intero (`#map-overlay`, stesso pattern di `#scanner-overlay`) con pan via trascinamento (trasformazione CSS live durante il drag, griglia di tile ricostruita solo al rilascio — mai un ricalcolo per ogni pixel), zoom bottoni +/-/wheel con un tetto assoluto (`ABSOLUTE_MAX_ZOOM = 22`) indipendente da `mapInfo.maxzoom` — trovato da code-review: un valore anomalo nel file MBTiles avrebbe altrimenti potuto far traboccare `Math.pow(2, zoom)` a `Infinity` e bloccare la scheda in un loop infinito di rendering. Verificato end-to-end con Playwright contro un nodo reale con `--map-file`: bottone nascosto/visibile in base alla configurazione, overlay che si apre/chiude, e — dopo aver corretto un mismatch nella fixture di test (non nel codice) — una tile reale confermata caricata esattamente alle coordinate geografiche attese, calcolate indipendentemente e verificate combaciare con quelle richieste dal viewer.

Bacheca "drop" (voce #47, Opzione M — concetto ispirato a `BoardManager` di BitChat, Unlicense/dominio pubblico, vedi sopra): nuovo pannello "Bacheca" (`#drops-panel`), **sempre visibile, mai capability-gated** — a differenza dei pannelli mappa/registro posizioni, `GET /api/drops` è offerto incondizionatamente su ogni nodo, quindi non c'è nulla da rilevare prima di mostrarlo. Form di creazione (etichetta opzionale, testo, checkbox "urgente"), posizione sempre presa da `getCurrentPosition()` al momento dell'invio (mai inserita a mano, stessa scelta di `shareLocation()`). Lista drop urgenti-prima (`renderDrops()`, badge `.tag.urgent`) con distanza dalla propria posizione quando nota (nuovo `haversineDistanceMeters()`, formula standard). `mapview.js` guadagna un secondo layer (`#map-pins-layer`, sincronizzato con la trasformazione di pan/zoom del layer tile esistente) che disegna i drop noti come pin sulla mappa offline. Verificato end-to-end con Playwright contro due nodi reali connessi via mesh: un drop urgente creato su un nodo compare quasi subito sull'altro (propagazione via `CONTENT_ANNOUNCE`), pin visibile sulla mappa del nodo con `--map-file` configurato.

### File chiave in `node/src/` e a cosa servono

- `identity.ts` — identità Ed25519 per nodo (`Identity`), firma/verifica.
- `packet.ts` — `MessageType` enum, formato pacchetto, `Priority` enum (spec §50), encode/decode newline-JSON.
- `transport.ts` — interfaccia `Transport` astratta; `transports/tcp.ts` è l'unica implementazione reale finora; `transports/ble.ts` (Slice 8) ne è una simulata, zero radio reale, dietro la stessa interfaccia.
- `routing.ts` — `SeenCache` (dedup) + `decideForward()`, il cuore del controlled flooding (spec §21).
- `routing-table.ts` — routing a costo distance-vector (spec §22), alternativa/complemento al flooding per traffico unicast.
- `content.ts` — `ContentStore` (limitata per dimensione, `BoundedFifoMap`, voce #32 — eviction pesata sulla fiducia, con un rank dedicato per il contenuto pubblicato localmente), firma/verifica dei contenuti, `ChunkAssembler`, scadenza (`expiresAt`).
- `catalog.ts` — `RemoteCatalog`, contenuto noto ma non ancora scaricato (sync tra segmenti di rete).
- `service.ts` / `service-directory.ts` — service discovery (spec §35-37), stesso schema di firma di `content.ts`.
- `store-and-forward.ts` — coda per pacchetti unicast non consegnabili subito.
- `trust.ts` — `TrustLevel`/`TrustManager` (spec §54).
- `rate-limit.ts` — budget di pacchetti per peer per finestra temporale.
- `relay-policy.ts` — quando un nodo fa da relay in base a batteria/carica auto-dichiarata.
- `encryption.ts` — E2E per messaggi privati (X25519 + AES-256-GCM), `IdentityAnnouncement`.
- `peer-directory.ts` — propagazione delle chiavi di cifratura tra peer.
- `message-history.ts` — `MessageHistory` (voce #36), cronologia locale (mai firmata/propagata) dei messaggi 1:1 inviati/ricevuti via `sendPrivateMessage()`/`handlePrivateMessage()`, bounded su conversazioni (`maxPeers`, eviction pesata sulla fiducia) e messaggi-per-conversazione (`maxMessagesPerPeer`). Esporta anche `MAX_MESSAGE_TEXT_LENGTH`, il limite canonico di lunghezza di un singolo messaggio — importato sia da `node.ts` (`extractChatText()`, applicato sia in invio che in ricezione, solo per i messaggi 1:1) sia da `web-ui.ts` (`POST /api/messages`) sia da `public-channels.ts` (`extractChannelMessagePayload()`), un solo posto per non farli divergere.
- `public-channels.ts` — `PublicChannels` (voce #40), vista locale (mai firmata indipendentemente — vive sopra `content://` già firmato) dei canali pubblici di chat non cifrati, popolata da `NomadNode.publishChannelMessage()`/`considerChannelMessage()` (node.ts). Convenzione sul nome `chat:<canale>` per `ContentMetadata.name` (`channelContentName()`/`parseChannelFromContentName()`, quest'ultima difensiva contro un `name` non-stringa — `verifyContentSignature()` prova solo che la firma corrisponda, mai la forma). Il payload firmato è `{ text, timestamp }` (`extractChannelMessagePayload()`) — il timestamp vive nel payload, non in `ContentMetadata.createdAt`, che non è coperto dalla firma del publisher. Bounded su canali (`maxChannels`, eviction pesata sul **massimo** tra le fiducie di tutti gli autori visti nel canale, non solo l'ultimo — `bestAuthorTrust()`) e messaggi-per-canale (`maxMessagesPerChannel`).
- `drops.ts` — `Drops` (voce #47, bacheca "drop" — concetto ispirato al `BoardManager` di BitChat, Unlicense/dominio pubblico, mai `LocationNotesManager`, vedi `docs/reuse-vs-new.md`), mirror di `public-channels.ts` ma su un solo asse bounded (`maxDrops`, non c'è raggruppamento per canale) — nome fisso `DROP_CONTENT_NAME = "drop"` (non un prefisso: l'identificatore è già il `contentId`). `extractDropPayload()` valida `{ text, lat, lon, label?, urgent, timestamp }` con gli stessi bound di `extractChannelMessagePayload()`/`extractLocationReport()`. Popolata da `NomadNode.publishDrop()`/`considerDrop()` (node.ts). Scadenza pigra in `list()`, come ogni altra struttura di questa famiglia.
- `bounded-map.ts` — `BoundedFifoMap<K,V>`, struttura dati limitata condivisa (con eviction opzionale pesata su uno score, es. la fiducia) usata da quasi tutte le strutture sopra che vengono alimentate dalla rete. Esporta anche `pushBounded()` (voce #40), l'helper "push poi trim" condiviso da `MessageHistory.record()`/`PublicChannels.record()`/`Groups.recordMessage()` — guardia esplicita su `maxLength <= 0` per evitare un loop infinito con un valore negativo.
- `groups.ts` — `Groups` (voce #42), chat private/di gruppo cifrate end-to-end (membership fissa alla creazione, v1 — nessuna uscita/rimozione membro né rotazione di chiave, scelta esplicita dell'utente). `GroupInfo` (chiave AES-256-GCM del gruppo solo in memoria, mai persistita), `extractGroupInvite()` (valida un payload `PRIVATE_MESSAGE` discriminato `type: "group-invite"`), `groupMessageSigningPayload()`/`signGroupMessage()`/`verifyGroupMessage()`/`decryptGroupMessage()` (cifra+firma un `GROUP_MESSAGE`: AES-256-GCM con la chiave di gruppo via `encryptForPeer()`/`decryptFromPeer()` già esistenti, più una firma Ed25519 del mittente via `Identity.sign()`/`Identity.verifyWithNodeId()` — necessaria perché con una chiave condivisa l'auth tag GCM da solo non impedisce a un membro di impersonarne un altro). `GroupMessage.messageId` = la firma del mittente (deterministica per EdDSA), usato da `Groups.recordMessage()` per scartare un replay dello stesso payload firmato reinviato con un `packet.id` diverso — `SeenCache` deduplica solo per `packet.id`, mai per contenuto. `Groups` tiene info+cronologia di ogni gruppo in un'unica `BoundedFifoMap` (mai due mappe separate, per non rischiare stato orfano tra eviction indipendenti), bounded su gruppi (`maxGroups`, eviction pesata sulla fiducia di chi ha invitato) e messaggi-per-gruppo (`maxMessagesPerGroup`).
- `location-registry.ts` — `LocationRegistry` (voce #44), stato locale mesh-interno (come `groups.ts`/`public-channels.ts`, non un adapter esterno) di posizioni condivise opportunisticamente. `extractLocationReport()` valida un payload `PRIVATE_MESSAGE` discriminato `type: "location-report"` (lat/lon/accuracy?/timestamp, stesso pattern di `extractGroupInvite()`). `LocationRegistry.record()` **sovrascrive sempre** l'entry esistente per lo stesso `reporterId` (mai accumula, "solo l'ultima posizione nota") **a meno che** il nuovo report non abbia un timestamp strettamente più recente di quello già in archivio — guardia anti-fuori-ordine per store-and-forward (un report vecchio in coda non deve mai regredire uno più recente già arrivato per una via più veloce), con `report.timestamp` clampato a `Math.min(report.timestamp, Date.now())` per impedire che un timestamp futuro fabbricato avveleni permanentemente uno slot (entrambe le guardie trovate da code-review, vedi voce #44 per il dettaglio, incluso il limite accettato e documentato sul clock rollback locale). Scadenza pigra opzionale (`maxReportAgeMs`, mai un timer di sweep). Wired in `node.ts`: `NomadNode.shareLocation()`/`registerAsLocationRegistry()`/`considerLocationReport()`.
- `map-tiles.ts` — `MbtilesReader` (voce #46), lettura in sola lettura di un file MBTiles (SQLite) locale via `node:sqlite` built-in di Node (nessuna nuova dipendenza esterna). Non `gateway/nomad/` — un file locale dell'operatore, mai dati mesh-fed né un adapter verso un backend NOMAD/internet esterno, stesso principio di `location-registry.ts`/`groups.ts`. `getTile(z, x, y)` converte la convenzione di riga MBTiles (TMS) in quella XYZ usata da `mobile/www/mapview.js`. `metadata` legge la tabella standard `metadata` in modo difensivo (file malformato non deve mai crashare il nodo); formati non-raster (es. `pbf`) rifiutati esplicitamente all'apertura.
- `priority-queue.ts` — coda a bucket per priorità (6 livelli), usata da `transports/tcp.ts` per lo scheduling reale degli invii.
- `qrcode.ts` — encoder QR (ISO/IEC 18004) scritto da zero, nessuna dipendenza esterna: solo byte mode, versioni 1-10, EC level M con fallback a L. `encodeQr(text)` (`QrCode | undefined`) + `qrToSvg(qr, opts)`. Usato da `web-ui.ts` per `GET /api/pairing`'s `qrDataUri`. Verificato durante lo sviluppo contro un decoder indipendente (`jsQR`, solo in scratch, mai una dipendenza) — vedi `docs/security.md` voce #25 per i 2 bug reali trovati così (posizionamento format/version-info, non logica dati).
- `web-ui.ts` — interfaccia web locale di stato/ricerca (spec §59), server `node:http` minimale. Endpoint di sola lettura: `/api/status` (include `relaying`, da `NomadNode.canRelayNow()`), `/api/peers` (`canMessage` per peer, voce #36), `/api/services`, `/api/content` (tutto il conosciuto), `/api/search?q=...` (filtrato), `/api/pairing` (nome rete/password/indirizzo/QR di pairing, non autenticato), `/api/channels`/`GET /api/channel-messages` (voce #40, canali pubblici — mai autenticati in lettura, un canale pubblico è stato pubblico della mesh per definizione). Più `POST /api/call`, `GET`/`POST /api/messages`, `POST /api/channel-messages` e `GET`/`POST /api/groups`+`GET`/`POST /api/group-messages` (voce #42 — endpoint dietro password di rete via un `isAuthorized()` condiviso, come `/api/messages`: nome/membership di un gruppo sono informazione privata, mai pubblici come i canali; `toGroupSummary()` esclude sempre la chiave del gruppo dalla risposta) — scrittura disattivata di default; `/api/messages`/`/api/groups`/`/api/group-messages` restano le uniche coppie che richiedono auth anche in lettura, perché il loro contenuto è "Private messages" spec §56, a differenza dei canali pubblici — base server-side per l'app mobile, `docs/next-steps.md` Opzione H, vedi `docs/security.md` voci #22/#24/#25/#36/#40/#42. `WebUiOptions.exposeLocationRegistry` (voce #44) — opt-in **indipendente da `allowServiceCalls`** (un nodo registro dedicato può averlo acceso da solo), `GET /api/location-registry` (autenticato, 404 se spento, stesso schema di `handleGetGroups()`); `POST /api/location-report` (autenticato come `/api/messages`, su qualunque gateway — è il lato mittente, non il lato lettura) → `NomadNode.shareLocation()`. Il gate CORS/preflight OPTIONS (`Access-Control-Allow-Origin`) è **incondizionato** (voce #47, applicato a ogni risposta indipendentemente da `allowServiceCalls`/`exposeLocationRegistry`/`mapTiles`) — reso tale perché `GET`/`POST /api/drops` è offerto su ogni nodo senza un proprio flag di opt-in, a differenza di ogni altro endpoint cross-origin-rilevante finora: un nodo con nessuna delle tre flag avrebbe altrimenti bloccato via CORS il proprio pannello Bacheca sempre visibile per l'app mobile — stessa classe di bug già trovata e corretta due volte in precedenza (prima per `exposeLocationRegistry`, poi per `mapTiles`) quando erano loro a mancare dalla condizione. `GET /api/drops` (voce #47) — non autenticato, contenuto pubblico della mesh, stesso livello di `/api/channels`; `POST /api/drops` — dietro password di rete come `POST /api/channel-messages`, delega a `NomadNode.publishDrop()`, 429 (non 400) se scatta il rate limit sui drop urgenti. `CAPTIVE_PORTAL_PROBE_PATHS` (voce #43) — blocco di route sempre attivo, nessuna opzione di configurazione: risponde `302`/`Location: /` ai path che iOS/Android/Windows/Firefox interrogano da soli dopo essersi collegati a un Wi-Fi, per decidere se mostrare il proprio popup "Accedi alla rete" — instradato solo per `req.url`, mai `Host` (che dopo un DNS hijack lato hostapd/dnsmasq può essere qualunque dominio); la configurazione di rete che lo rende effettivo è fuori da questo npm workspace, vedi `docs/guida-hardware-rifugio.md`. `WebUiOptions.mapTiles` (voce #46) — `GET /api/map-info`/`GET /api/map-tiles/:z/:x/:y`, mai autenticati (dati geografici non personali, spec §56 non li elenca); CORS esteso alla stessa condizione di `exposeLocationRegistry`; `GET /api/map-tiles` valida z/x/y contro `minzoom`/`maxzoom`/`[0, 2**z)` dichiarati dal file prima di query e ha un rate limit dedicato per IP sorgente (`checkMapTileRateLimit()`, `MAX_MAP_TILE_REQUESTS_PER_WINDOW`) — trovato da code-review: query `node:sqlite` sincrona dietro un endpoint non autenticato, senza questi due, una raffica di richieste fuori range avrebbe potuto bloccare l'event loop.
- `loopback-http-server.ts` — bootstrap HTTP condiviso (`start()`/`stop()`/`port` getter, `sendJson()`, `readRequestBody()`/`BodyTooLargeError` con timeout opzionale) estratto da `web-ui.ts` e `gateway/nomad/fake-nomad-server.ts` (Slice 9), poi riusato anche da `gateway/nomad/fake-ollama-server.ts` (Slice 10) e di nuovo da `web-ui.ts`'s `/api/call` (Opzione H) man mano che ciascun pezzo di boilerplate HTTP è comparso una volta di troppo. `sendBinary()` (voce #46) — controparte di `sendJson()` per byte grezzi, prima risposta non-JSON del progetto, usata da `GET /api/map-tiles`.
- `node.ts` — `NomadNode`, la classe che orchestra tutto il resto; qui vivono i pacchetti-handler (`handleContentQuery`, `handleServiceRequest`, `handleGroupMessage`, ecc.) e i metodi pubblici (`getContent`, `callService`, `publishContent`, `createGroup`, `sendGroupMessage`, ...). `considerGroupInvite()` (voce #42) usa sempre il `senderId` autenticato di un `PRIVATE_MESSAGE` per `GroupInfo.createdBy`, mai il campo `createdBy` auto-dichiarato dentro il payload dell'invito stesso. `shareLocation()`/`registerAsLocationRegistry()`/`considerLocationReport()` (voce #44) seguono lo stesso schema: `considerLocationReport()` usa sempre il `senderId` autenticato per la chiave del registro, mai un campo del payload; `shareLocation()` stampa `timestamp` con `Date.now()` lato mittente, mai accettato come argomento (né da sé né, a valle, da `web-ui.ts`'s `POST /api/location-report`). `publishDrop()`/`considerDrop()` (voce #47) seguono lo stesso schema di `publishChannelMessage()`/`considerChannelMessage()`: `publishDrop()` valida text/lat/lon/label/`expiresInMs` (quest'ultimo **prima** di consumare il rate limit sui drop urgenti — ordine corretto da code-review, vedi `docs/security.md` voce #47) e pubblica via `publishContent()` con `{ announce: true, priority: urgent ? Priority.EMERGENCY : Priority.CONTENT, ttlMs }`; `considerDrop()` è un fetch reattivo bounded (`MAX_CONCURRENT_DROP_FETCHES`, budget indipendente da quello dei canali), agganciato sia a `handleContentAnnounce()` sia a `handleSyncResponse()`.
- `cli.ts` — entry point eseguibile (`npm run dev -w node --`); `--register-as-location-registry`/`--expose-location-registry` (voce #44, `--expose-location-registry` richiede la stessa password di rete di `--allow-service-calls` ma non richiede quel flag: un nodo registro dedicato può averlo acceso da solo); `--map-file <path>` (voce #46 — apre `MbtilesReader` in try/catch, file mancante/non valido logga un errore chiaro e il nodo continua senza la feature, nessun flag di password dedicato).

`gateway/nomad/` (Slice 9-10 + voce #27/#33/#37/#39/#41/#43/#45, non nel workspace npm — importa `node/src/*` con percorsi relativi, stesso precedente di `tools/simulator/`): `translate-gateway.ts` (`registerTranslateService()`, voce #43 — registra `service://translation` componendo `service://ai` tramite `node.callService()`, mai un riferimento diretto ad `AiGateway`, stesso pattern di `generateDigest()` sotto; funzione, non classe, nessuno stato proprio; `SUPPORTED_LANGUAGES` lista fissa di 5 lingue, validata con `Object.prototype.hasOwnProperty.call()` — mai il solo operatore `in`, che matcherebbe anche proprietà ereditate di `Object.prototype` come `"constructor"`; `sanitizeTextForPrompt()` neutralizza solo la rottura del delimitatore `"""` usato nel prompt, non è protezione generale da prompt injection, dichiarato esplicitamente nel commento), `kiwix-gateway.ts` (`KiwixGateway`, traduce `content://...`/`service://kiwix-search` verso l'HTTP di NOMAD), `fake-nomad-server.ts` (`FakeNomadServer`, sostituisce Project NOMAD/Docker reale nei test/demo), `ai-gateway.ts` (`AiGateway`, registra `service://ai` — spec §37, proxy live a un backend Ollama), `fake-ollama-server.ts` (`FakeOllamaServer`, sostituisce un'istanza Ollama reale nei test/demo — anche `prompts`/`lastPrompt`, voce #37, per ispezionare in test cosa un chiamante le ha effettivamente inviato), `rss-feed.ts` (`parseFeed()`, parser RSS 2.0/Atom scritto da zero — nessuna dipendenza, stesso precedente di `node/src/qrcode.ts`; CDATA-aware su ogni scansione di tag, validazione atomica per feed, voce #33 per il dettaglio dei 4 giri di revisione, incluso un DoS a tempo quadratico trovato e corretto), `news-gateway.ts` (`NewsGateway`, registra `service://news` rispondendo sempre dalla cache, mai da una chiamata live — ingerisce un vero feed RSS/Atom via `rss-feed.ts` (non più un'API JSON ad-hoc, voce #33) e pubblica ogni notizia come contenuto via `publishContent()`; nessun fake server per scelta esplicita dell'utente, `docs/security.md` voce #27; `generateDigest()`/`startDigestAutoRefresh()`/`stopDigestAutoRefresh()`, voce #37 — compone `service://ai` tramite `node.callService()`, mai un riferimento diretto ad `AiGateway`, pubblica il digest come proprio contenuto e lo riporta nella risposta di `service://news`; `registerEmergencyNewsService()`/`emergencyHeadlines`/`isEmergencyHeadline`, voce #39 — una headline nuova/cambiata che il classificatore (default: parola chiave nel campo `category`) marca come emergenza viene pubblicata con `{ announce: true, priority: Priority.EMERGENCY }`, con un tetto `MAX_EMERGENCY_ANNOUNCES_PER_SYNC` a protezione da un feed ostile; `announcedEmergencyById`, voce #41 — mappa dedicata e più piccola che decide l'annuncio indipendentemente da `publishedById`, così un volume di headline di routine non correlati non può far scadere per eviction la entry di un bollettino di emergenza invariato e farlo ri-annunciare), `internet-gateway.ts` (`InternetGateway`, voce #45 — registra `service://internet-fetch`, due `kind` curati (`"rss"`, barriera = deve superare `parseFeed()`; `"text"`, barriera = allowlist di host configurato solo dall'operatore), rate limiting a due livelli per-chiamante/globale, risposta come riferimento a contenuto via `publishContent()`), `url-safety.ts` (voce #45 — `isPubliclyRoutableUrl()`, guardia SSRF condivisa da entrambi i `kind` di `InternetGateway`, applicata prima di ogni fetch in uscita; limite noto e documentato di TOCTOU/DNS-rebinding tra la propria risoluzione DNS e quella di `fetch()`), `fetch-bounded.ts` (`fetchTextBounded()`, fetch testuale con limite di dimensione in streaming — estratto da `news-gateway.ts` al secondo bisogno, voce #45, riusato ora anche da `internet-gateway.ts`), `cli.ts` (demo eseguibile, `npm run gateway:demo`, registra `service://translation` incondizionatamente subito dopo `service://ai` — nessun flag proprio, voce #43; registra `service://news` solo se `--news-url` è dato — ora un URL di feed RSS/Atom, non più una base API NOMAD; genera e aggiorna periodicamente anche il digest AI, voce #37, e registra `service://emergency-news`, voce #39; `--internet-fetch` registra `service://internet-fetch`, voce #45, con `--internet-allowed-hosts` per l'allowlist `"text"` e override numerici opzionali per rate limit/finestra/dimensione massima; `--max-content-entries` dimensiona il `ContentStore` del nodo gateway, voce #32 — default 8192, con validazione dell'input contro `NaN`/valori non positivi).

## Convenzioni consolidate (da rispettare per coerenza, non da riscoprire)

- **Ogni struttura dati alimentata dalla rete è limitata per dimensione** (spec §57): usa `BoundedFifoMap` con `maxSize`, e quando la fiducia dell'origine è nota passa `evictionScore`/`trustRank` così l'eviction preferisce rimuovere la voce meno fidata invece della più vecchia — altrimenti un peer può fabbricare identità usa-e-getta per sfrattare voci legittime (vedi bug #13 in `security.md`). Strutture puramente locali (mai alimentate da pacchetti di rete, es. `localServices` in `node.ts`) non hanno bisogno di questo limite.
- **Ogni claim che arriva dalla rete e verrà ri-propagato ad altri peer viene firmato dal suo autore** (Ed25519, stesso pattern in `content.ts`/`encryption.ts`/`service.ts`): un campo `SignableXFields`, una funzione `xSigningPayload()` con ordine esplicito dei campi, una `verifyX()` che riverifica contro la chiave pubblica dichiarata. Un relay non fidato può inoltrare ma mai fabbricare un claim valido per un'identità che non controlla.
- **Il payload di un pacchetto non è mai fidato quanto il suo tipo dichiarato**: `decodePacket()` valida solo l'involucro (id/type/source/ttl), mai la forma del payload — ogni handler deve accedere ai campi del payload in modo difensivo (`packet.payload?.campo`, controlli `typeof`/`Array.isArray`), perché un singolo pacchetto malformato da un peer connesso non deve mai poter far crashare il processo. Questo ha causato almeno un vero DoS non autenticato scoperto dalla revisione (Slice 6, bug #16) — vedi `tests/integration/malformed-packet-robustness.test.ts` per il pattern di test dedicato, da estendere per ogni nuovo tipo di pacchetto.
- **Scadenza "pigra", mai un timer di sweep in background**: `ContentStore`/`RemoteCatalog`/`ServiceDirectory` trattano una voce scaduta come assente ed eliminano al momento dell'accesso (`get`/`has`/`list`/`size`), non con un `setInterval`.
- **`packet.source` non è mai autenticato crittograficamente** (limite noto e documentato in `security.md` sotto "Binding crittografico") — chiunque può dichiararsi qualunque node id nel campo `source`. Ciò che *non* può fare è produrre una firma valida per un'identità che non controlla. Ogni nuovo meccanismo che si affida a `packet.source` per decidere qualcosa di sensibile (es. quale risposta accettare per una richiesta pendente) deve comunque vincolarsi al mittente atteso (es. `packet.source === entry.activeProvider`), non fidarsi ciecamente — pattern già usato in `handleContentChunk`/`handleContentComplete`/`handleServiceResponse`.
- **Nessuna nuova dipendenza esterna senza necessità reale**: `web-ui.ts` usa `node:http` puro invece di aggiungere Express; `tcp.ts` non usa framework di parsing. Il solo devDependency oltre a TypeScript/vitest/tsx è quanto già presente.
- **Test**: unit in `tests/unit/` (logica pura, nessuna rete reale), integration in `tests/integration/` (istanze reali di `NomadNode`/`TcpTransport` su localhost, a volte con socket grezzi via `node:net` per controllo deterministico del timing quando serve simulare un comportamento avversariale/di rete inaffidabile — vedi `content-provider-retry.test.ts` per il pattern). Non usare mock per `NomadNode` stesso: la suite esistente preferisce nodi reali connessi via TCP reale su porta `0` (assegnata dal SO), anche nei test più intricati.
- **Race di timing nota e ricorrente nei test**: `connect()` che si risolve garantisce solo che il lato che ha iniziato la connessione abbia identificato il peer, non che l'altro lato abbia già processato l'HELLO in arrivo. Se un test fa qualcosa sul lato "server" subito dopo che `connect()` si risolve sul lato "client" (es. registrare un servizio e aspettarsi che venga floodato al peer appena connesso), potrebbe fallire in modo intermittente — aggiungere un `waitFor()` esplicito sullo stato effettivamente osservabile (es. `provider.node.peers.has(caller.node.nodeId)`) prima di procedere.

## Comandi utili

```bash
npm install                                    # alla radice, installa anche il workspace node/
npx vitest run                                 # intera suite di test
npx vitest run tests/integration/xyz.test.ts   # un file specifico
npx tsc --noEmit -p node/tsconfig.json         # typecheck senza emettere output
npm run build -w node                          # build del package node/
npm run dev -w node -- --id A --port 9001      # avvia un nodo (aggiungi --web-port 8080 per l'UI web)
npm run simulate -- --nodes 50 --topology random  # simulatore di rete a scala
```

## Cosa NON fare

- Non commitare/pushare senza che i test passino ripetutamente, il typecheck e la build siano puliti.
- Non saltare la fase di code-review su una slice "perché sembra semplice".
- Non procedere alla slice successiva senza l'ok esplicito dell'utente, anche se la precedente sembra ovviamente completa.
- Non introdurre dipendenze esterne per problemi risolvibili con la libreria standard di Node.
- Non fidarsi di `packet.source` o della forma del `payload` di un pacchetto in arrivo senza validazione difensiva.
